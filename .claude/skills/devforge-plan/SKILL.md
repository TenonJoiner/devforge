---
name: product/plan
description: 制定迭代计划，智能识别当前所处阶段，按顺序推进两阶段规划（里程碑 → 迭代执行计划），每阶段独立评审（architect-reviewer + product-reviewer + project-reviewer 三重视角）
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent]
---

# product/plan — 规划性思考

## 概述

**两阶段规划，按顺序执行；每阶段完成前，不展开下一阶段。**

两阶段规划顺序：

1. **第 1 阶段**：里程碑规划 + Backlog 清单（milestone-plan.md）
2. **第 2 阶段**：迭代执行计划（iteration-m*-i*.md）— **第 1 阶段完成后才能进入**，一次一迭代

独立评审：每阶段完成后，`architect-reviewer` + `product-reviewer` + `project-reviewer` 三个 Agent 分别执行**独立评审**，评审通过后进入下一阶段。

**核心原则**：
- **线性推进**：按第 1 阶段 → 第 2 阶段的顺序执行，不跳跃
- **一次一文件**：每次执行只产出一个计划文件
- **出口标准驱动**：每阶段有明确的**出口标准**，达成后**确认后推进**进入下一阶段
- **计划是活的**：评审通过后如需调整，执行滚动更新

### 两阶段分工

- 第 1 阶段：里程碑规划 + Backlog 清单（有什么活要干）
- 第 2 阶段：迭代执行计划（这两周具体怎么干）

**关键规则**：后续里程碑的 Backlog 展开是**更新 milestone-plan.md**，不是创建新文件。一个项目始终只有一个 milestone-plan.md。两阶段各管什么详见 `.claude/templates/plan-milestone.md` 模板前言。

## 复杂度判定

> **判定前必须读取对应 Template**：下表中各阶段的复杂度是基于当前项目 template 的内容要求评估的。主会话在判定复杂度前，必须先读取对应阶段的 template，了解其章节结构和内容要求。若更换项目 template，复杂度可能随之变化。

本 skill 各阶段的内容类型与基准复杂度判定如下：

| 阶段 | 内容类型 | 基准复杂度 | 判定依据 |
|------|---------|-----------|---------|
| 第 1 阶段：里程碑规划 | 结构化规划 | **高** → 上调至 **极高** | 基于已有需求/架构文档进行里程碑结构化编排；基准为「高」（结构化规划+跨子系统依赖分析），上调属性：跨子系统边界+不可逆决策 |
| 第 2 阶段：迭代执行计划 | 结构化规划 | **中等** → 上调至 **高** | 基于里程碑计划分解迭代任务；基准为「中等」（结构化规划，结构固定），上调属性：性能/一致性关键路径编排 + 多角色交叉验证 |

**决策属性上调**（辅助判定）：
- 第 1 阶段基准复杂度为「高」（结构化规划 + 跨子系统依赖分析）
- 上调属性 1：「跨子系统边界」（proposal 依赖分析通常都有），复杂度 +1 级
- 上调属性 2：「不可逆决策」（里程碑划分影响后续全部迭代），复杂度 +1 级
- 综合判定：高 + 跨子系统 + 不可逆 = **极高**
- 第 2 阶段涉及「关键路径」上的 proposal 编排（性能 / 一致性关键路径，复杂度 +1 级），且需要多角色交叉验证（product / architect / project 各自侧重容量均衡、技术依赖、执行可行性），复杂度上调至「高」

**篇幅异常检测**（辅助复核）：
- 高/极高复杂度文档篇幅 <100 行 → 红旗，需复核分析深度是否不足
- 低复杂度文档篇幅 >400 行 → 红旗，需复核是否存在冗余或信息密度过低
- 篇幅与同类文档历史均值偏差 >50% → 提示复核，确认是合理差异还是质量问题

**复杂度与 agent 配置对照**：

| 阶段 | 复杂度 | 并行产出 agent | 独立评审 agent |
|------|--------|---------------|---------------|
| 第 1 阶段 | 极高 | ≥5 个 agent（不同角色，详见阶段文件） | ≥3 个 reviewer（覆盖技术/产品/项目管理） |
| 第 2 阶段 | 高（关键路径编排 + 多角色交叉验证） | ≥3 个 agent（不同角色，详见阶段文件） | ≥3 个 reviewer（architect-reviewer + product-reviewer + project-reviewer） |
| 维护模式 | 低 | 1 个对应角色 agent | 按需 |

## 启动检测

`/df:plan` 启动时，执行**启动检测**判断当前所处阶段，**只提示唯一下一步动作**：

| 检测结果 | 当前阶段 | 唯一下一步动作 |
|---------|---------|--------------|
| 上游文档（`docs/architecture/` 或 `docs/requirements/`）缺失 | **上游未就绪** | 提示先执行 `/df:design` 和 `/df:define` 完成架构设计和需求定义 |
| `milestone-plan.md` 不存在 | **第 1 阶段** | 开始里程碑规划（需确认上游文档已就绪） |
| `milestone-plan.md` 存在（含 Backlog），第一个未规划的迭代（如 M1-I1）的 `iteration-m1-i1.md` 不存在 | **第 2 阶段** | 开始 M1-I1 的迭代执行计划（一次只做这一个迭代） |
| 所有当前目标已完成评审 | **下一目标** | 继续执行 `/df:plan`，自动进入下一个迭代的规划 |
| 当前里程碑所有迭代已规划完毕，下一个里程碑 Backlog 尚未展开 | **里程碑推进** | 更新 milestone-plan.md，展开下一个里程碑的详细 Backlog + 迭代总览（滚动更新） |
| 全部迭代均已规划完毕 | **计划完备** | 建议执行 `/opsx:new` 启动特性开发，或滚动更新 |

## 执行流程

### 第 1 阶段：里程碑规划 + Backlog

**准入条件**：无（首次执行）
**产出文件**：`docs/iteration-plan/milestone-plan.md`
**详细规则**：📖 读取 `01-milestone-planning.md`

> 主会话进入本阶段时，必须先读取 `01-milestone-planning.md` 获取详细执行规则。

### 第 2 阶段：迭代执行计划

**准入条件**：第 1 阶段完成
**产出文件**：`docs/iteration-plan/iteration-m<x>-i<y>.md`
**详细规则**：📖 读取 `02-iteration-planning.md`

> 主会话进入本阶段时，必须先读取 `02-iteration-planning.md` 获取详细执行规则。

### 维护模式

**准入条件**：第 1-2 阶段完成
**详细规则**：📖 读取 `03-maintenance.md`

> 主会话进入维护模式时，必须先读取 `03-maintenance.md` 获取详细执行规则。

## 文档写入铁律

**每一阶段迭代的产出必须写入项目文件，禁止仅在对话中输出。**

详细规则见 `common.md`。

---

## 通用规范

本 skill 遵守 `common.md` 中定义的通用规范，包括：
- 评审与修正记录规范
- 通用术语（问题分级、评审结论三态、修正工作量分级）
- 标准评审修正循环
- 主会话职责边界

> 主会话启动时或进入阶段时，如果 `common.md` 未加载，必须先读取。

## 红旗信号

- 主会话直接编写计划文件内容（应委托 project Agent）
- 跳过里程碑规划直接进入迭代执行计划
- 所有 proposal 都在 Wave 0（未识别迭代内依赖关系）
- 评审未通过就推进到下一阶段
- CRITICAL 问题修正后未重新评审
- 评审记录章节篇幅超过 200 字
- 未收集项目背景信息就开始里程碑规划
- 未确认迭代上下文就开始迭代执行计划
- 在无 agent 执行记录的情况下声称某阶段/步骤 已完成
- 高/极高复杂度文档篇幅 < 100 行（可能分析深度不足，需检查是否遗漏关键决策依据）
- 低复杂度文档篇幅 > 400 行（可能存在冗余或信息密度过低）
- 篇幅与同类文档历史均值偏差 > 50%（需复核确认合理差异）

## 输出位置

### 本 Skill 产出（仅限 `docs/iteration-plan/`）

- `docs/iteration-plan/milestone-plan.md` — 里程碑计划 + Backlog 清单
- `docs/iteration-plan/iteration-m<x>-i<y>.md` — 各迭代执行计划

### 本 Skill 不产出

| 目录/文件 | 归属命令 |
|-----------|---------|
| `docs/architecture/` | `/df:design` |
| `docs/requirements/` | `/df:define` |
| `docs/test-strategy.md` | `/df:test-design` |

---

## 禁止事项

- **禁止**主会话直接编写计划文件内容（应委托 project Agent）
- **禁止**主会话修改 agent 产出核心内容（只能原样搬运或明确标注修改）
- **禁止**在没有 agent 执行记录的情况下声称某阶段已完成
- **禁止**修改 plan agent 的调度方案（如有疑问应确认或重新派遣）
- **禁止**跳过启动检测直接假设从零开始

---

## 与用户协作模式

详细规则见 `common.md`。
