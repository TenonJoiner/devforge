---
name: devforge-define
description: 定义产品需求、制定验收标准，产出 Actor-Feature 清单与 Scenario 规格。采用多 agent 深度思考模式，标杆研究先行、Feature-Scenario 分层展开、独立评审验证。适用于需要深度思考的复杂需求定义场景，支持螺旋式迭代完善。当用户提到需求定义、产品规格、Feature 拆解、验收标准、用户故事设计、Actor 识别时应使用此 skill。
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent]
---

# devforge-define — 定义性思考

## 概述

需求定义是**长期、深度、迭代**的过程，不是单次对话能完成的。本 skill 采用**多 agent 协作模式**，通过持续多个阶段的思考-研究-发散-评审-完善循环，产出高质量的需求规格文档。

> **内容规范强牵引**：需求交付件的所有内容结构、章节要求、自检清单**以 `.claude/templates/*.md` 为唯一准绳**。本 skill 只负责定义**努力程度、写作风格、质量门槛和流程控制**，不再重复模板中已有的内容要求。

**核心原则**：
1. **模板牵引**：所有产出必须严格遵循对应模板，写前先读模板是铁律
2. **标杆先行**：不研究业界同类产品的需求规格，不允许定义自研 Feature
3. **Actor 驱动**：先识别所有交互角色，再围绕角色定义场景
   - **Actor 严格定义**：从产品黑盒视角看，与**运行中的系统**发生**持续、直接交互**的外部实体。一次性行为（选型评估、Benchmark 测试）不构成 Actor；通过另一个 Actor 间接交互也不构成独立 Actor
   - **类型限定**：人类 / 外部系统 / 时间触发源（3 类，**禁止内部组件**），详见模板判定决策树
   - **合并 4 法则**：同类合并 / 技能边界 / 机制边界 / SLA 边界（详见下表）
4. **分层产出**：先产出 Actor-Feature 清单并评审定稿，再进入 Scenario 挖掘
5. **可验收性**：每个 Scenario 可独立验证，验收标准必须量化
6. **产品级颗粒度**：需求文档是产品级交付件，用于指导后续特性级（OpenSpec）细化——只定义 Feature-Scenario 结构和验收标准，不深入特性级实现细节（字段级 API 签名、配置参数枚举、UI 布局等）。该在 `/opsx:*` 里做的事，不要在 `/df:define` 里提前做
7. **长时间迭代**：需求文档需要 3-10 个阶段才能定稿，禁止一次对话定需求

### Actor 合并 4 法则

| 法则 | 判定 | 行动 |
|------|------|------|
| **同类合并** | 类型相同 + 关注点相似 + 交互方式接近 | ✅ 合并 |
| **技能边界** | 技能栈/知识领域显著不同（如应用开发者 vs DBA）。注意：同一技能栈在不同环境下操作（如云端运维 vs 边缘运维）不算技能边界不同 | ❌ 不合并 |
| **机制边界** | 交互机制根本不同（推 vs 拉、同步 vs 异步、长连接 vs 短请求） | ❌ 不合并 |
| **SLA 边界** | 时延/规模 SLA 差 4 个数量级以上（如交互式 BI 秒级 vs 离线 ETL 天级） | ❌ 不合并 |

### Actor 数量复审触发条件（质量信号驱动，非数量门槛）

| 信号 | 触发条件 | 复审动作 |
|------|---------|---------|
| **A：关注点重叠** | 任意 2 个 Actor 的关注点 ≥ 70% 重合 | 触发"是否该合并"复审 |
| **B：游离 Actor** | 某 Actor 在所有 Feature 中均无关联 | 触发"是否该删除"复审 |
| **C：类型违规** | Actor 类型不属于 {人类 / 外部系统 / 时间触发源} | **强制剔除**（硬约束） |
| **D：基础类缺失** | 人类 Actor < 2 或外部系统 Actor < 2 | 触发"是否漏识别"复审 |

---

## 通用规范

### 通用术语

#### 问题分级

**核心原则**：问题级别基于对整个产品系统级的影响判定，而非对单个文档所描述的局部影响判定。

| 级别 | 含义 | 判定基准 | 处理方式 |
|------|------|---------|----------|
| **CRITICAL** | 阻塞性问题，会导致系统方向错误 | **系统级影响** | 必须修正，阻塞推进 |
| **HIGH** | 重要缺陷，显著影响局部质量 | **文档/模块级影响** | 默认接受修正 |
| **MEDIUM** | 一般问题，有改善空间 | **优化建议** | 评估后决定 |
| **LOW** | 轻微问题，可延后处理 | **非功能性** | 可延期 |

**判定标准**：CRITICAL 须同时满足「影响核心方向 + 不修正会推倒重来 + 不可逆/高成本」。HIGH 影响局部质量但不改整体方向。MEDIUM 为优化建议。LOW 为非功能性问题。

**常见误判**：子系统内部结构混乱 / Feature 不完整 → HIGH（可独立修复）；核心协议选型错误 → CRITICAL（系统级方向决策）。
3. 核心一致性协议选型错误 → **CRITICAL**。理由：系统级方向性决策，影响多个子系统且难以回退

### 标准评审修正循环

各阶段遵循统一的评审修正循环。具体门槛值（缺陷密度阈值、评估对象数计算方式）见各阶段规则。

1. **独立评审**：并行启动 ≥2 个 reviewer，按模板自检清单提出 CRITICAL/HIGH/MEDIUM/LOW 分级问题
2. **计算缺陷密度**：所有问题分数之和 / 评估对象数

   问题分值：**CRITICAL=10分, HIGH=3分, MEDIUM=1分, LOW=0.1分**

3. **判断退出条件**（两步判定）：
   - **先判断 CRITICAL 问题**（一票否决）：有 CRITICAL → 进入修正循环；无 CRITICAL → 继续
   - **再判断缺陷密度**：达标 → 成功退出；超标 → 进入修正循环
4. **修正策略**（此为唯一定义，各阶段文件不得重复）

   基于问题数量决定 agent 配置：
   - < 10 处：1 个 agent 读取文件 + review 文件后 Edit 修正
   - 10-30 处：2 个 agent 按产出对象/维度分块并行修正，各自 Edit 对应部分
   - \> 30 处：按各阶段规则判断是否回退到对应步骤重新执行多 agent 并行发散（回退规则见各阶段，各阶段不重复定义此处 3 档策略本身）
   - 分工原则：问题集中在某个产出对象 → 针对该对象；问题分散 → 按产出对象并行；问题集中在某个维度 → 按维度分工
   - 修正 agent 直接 Edit 文件，主会话只确认修正完成状态，不中转修正内容

5. **复核**：修正完成后回到步骤 1（独立评审）
6. **循环上限**：最多 3 次
7. **降级规则**：3 次循环后仍未达标 → 进入人工决策（见步骤 8）
8. **人工决策返回路径**：3 次循环未达标时，用户可选：接受现状（标记 `⚠️ ACCEPTED-WITH-DEBT` 并记录残留问题）、重启循环（按提示重新评审）、回退重做（按阶段规则回退到对应步骤）。
9. **评审状态标记契约**（机器可解析，启动检测依此判定阶段过渡）：

   ```markdown
   **评审状态**: ✅ PASS（缺陷密度 X/评估对象，无 CRITICAL，循环 N/3）
   ```

   - 仅当无 CRITICAL + 缺陷密度达标时写入 `✅ PASS`；评审中或未通过写入 `❌ FAIL` 或不写；接受带债放行写入 `⚠️ ACCEPTED-WITH-DEBT`（不视为 PASS，但允许阶段过渡）

### 缺陷密度门槛标定依据

> 此节记录各阶段缺陷密度门槛的设定逻辑，供后续校准和质疑参考。数字不是经验拍脑袋的值，是基于以下假设：

| 阶段 | 门槛 | 估算逻辑 |
|------|------|---------|
| 第 1 阶段（标杆研究） | 2.0 分/标杆 | 总分 ÷ 标杆数 ≤ 2.0。研究报告信息不完整（outside-in），适度容忍在研 gap。例 N=3 累计 ≤ 6 分（1 个 HIGH + 3 个 MEDIUM 已逼近），CRITICAL 单个否决 |
| 第 2 阶段步骤 1（Actor） | 1.5 分/Actor | 总分 ÷ Actor 数 ≤ 1.5。Actor 内容简单（4 字段）、列表短（5-15），且是 Feature 的基础——需要比原 2.0 更严。例 N=10 累计 ≤ 15 分 |
| 第 2 阶段步骤 2（Feature） | 1.5 分/Feature | 总分 ÷ Feature 数 ≤ 1.5。与 Actor 持平：Feature 内容更复杂但对象数更多（15-50），统计上均衡 |
| 第 3 阶段（Scenario） | 1.5 分/**Feature** | 总分 ÷ Feature 数（非 Scenario 数）≤ 1.5。Scenario 粒度更细但评审维度收窄，与 Feature 门槛对齐 |
| 第 4 阶段（维护模式） | 3.0 分/修正点 | 总分 ÷ 修正点数 ≤ 3.0。快速通道范围有限（单文件单章节），N=1 时 ≤ 3 分（1 个 HIGH 到顶，不可叠加 MEDIUM） |

**校准规则**：
- 门槛值被持续验证 10 轮后若无误判（假阳性/假阴性），可定为稳定值
- 任一阶段连续 2 轮以底线通过（刚好不超标，修正 1 次即通过），应考虑下调门槛
- 任一阶段连续 2 轮在第一轮通过（无任何 HIGH 问题），可讨论该阶段是否仍需评审循环

### 文档写入铁律

**每一阶段迭代的产出必须写入项目文件，禁止仅在对话中输出。**

- **原始 agent**（researcher / product）：将完整产出写入 `-draft-` 标记的中间文件，向主会话返回结构化摘要，包含以下字段（缺失项跳过，不超过 10 行）：
  - `产出`：写入的文件路径 + 核心产出数（Actor 数/Feature 数/Scenario 数等）
  - `数字`：覆盖率、完整度等可量化指标
  - `异常`：覆盖缺口、数据矛盾、视角间分歧（附 1-2 句定位，供后续合并/评审参考）
  - `编排提示`：如"合并时需关注 X 与 Y 视角在 Z 上的分歧"、"建议 reviewer 侧重验证 Actor 合并合理性"
- **合并 agent**（同类型新实例）：读取 draft 文件，执行合并/去重，写入最终文件，返回结构化摘要
- **Reviewer agent**：将评审意见追加到 `-review.md` 文件，返回数字摘要（issues 数、缺陷密度、CRITICAL 数）
- 所有文件写入后用 `ls` 验证存在，完成后才能汇报"本阶段完成"

**文件命名规范**：
- 所有非最终产出统一用 `-draft-` 标记，如 `actors-draft-users.md`
- 评审意见统一追加到单 `-review.md` 文件，如 `product-spec-review.md`
- 最终文件不含 `-draft-` 和 `-review` 标记

**特性域文件命名**：
- 文件名使用 **kebab-case 英文标识**（如 `data-query.md`、`security-compliance.md`），禁止使用中文字符
- 中文特性域名称仅用于文档内部的标题和阅读显示
- `product-spec.md#Feature 总览` 必须包含「英文标识」列，主会话据此确定文件路径
- 示例：`数据查询` → `data-query.md` / `data-query-review.md`

**例外**：步骤内部的工作指令（如调度方案、盲区分析）属于执行中间件，可按对应阶段规则豁免落盘。

### 主会话职责边界

**主会话只负责**（必须由主会话亲自执行）：
- 调度 agent（决定派遣哪些 agent、视角切分、prompt 内容、并发节奏）
- 状态跟踪（记录各 agent 完成状态、文件路径、轻量摘要数字）
- 检查出口标准（文件存在性、✅ PASS 标记、数量门槛）
- 缺陷密度判定（从 reviewer 返回的数字摘要中读取，做通过/修正/回退决策）
- 与用户交互（确认、汇报、等待人工决策）

**已下放给 agent 的职责**（主会话不再执行）：
- 文件写入：agent 自行 Write/Edit 到 draft/review/最终文件
- 内容整合：由**同类型新 agent 实例**执行合并去重（researcher 合并 researcher 的 draft，product 合并 product 的 draft）
- 评审记录：reviewer 追加到 `-review.md`，合并 agent 将 PASS 标记写入最终文件

**禁止**：
- 主会话不得读取 agent 完整产出或实质性修改其核心内容
- 禁止无记录声称完成或用单 agent 内部发散替代多 agent 独立产出
- 合并 agent 须与原始 agent 类型一致

**agent 不可用时的降级约束**：当派遣的 agent 因基础设施故障（非超时、非内容纠偏）而不可用时，禁止主会话降级为替代执行研究或评审分析。处理方式：(a) 尝试修复派遣参数（视角、prompt、model 等）重发，(b) 仍不可用则暂停当前交付件并向用户报告阻塞原因，等待人工决策后方可调整执行方案。

### Agent 并发控制

**核心参数**：`agent.max_concurrent` = **5**（单一 `Agent` 工具调用计为 1 个并发单元）

**滑动窗口策略**：初始启动 min(N, 5) 个 agent，每完成一个立即补位下一个，始终保持运行中 agent 数 = min(剩余, 5)。有依赖关系的 agent 必须等上游完成后才能入队。各阶段"并行启动"指令均受此限，禁止以并发限制为由压缩多 agent 步骤为单 agent。

### 评审纪要写作约束

> 此节是写作风格约束，**不计入评审判定项**。状态标记契约见上方「标准评审修正循环」步骤 9。

**写作要求**：
- 写入评审记录前先读对应模板（`ref-requirements.md` / `req-product-spec.md` / `req-feature.md`）的「评审记录」示例，模板定义了表格列、日期格式等具体要求
- 日期格式精确到分钟，如 `2026-05-06 14:30`
- 评审记录表格的「缺陷密度」列每一轮（含修正后的复核轮次）都必须有数值，不可留空
- 评审记录章节 ≤ 200 字
- 详细评审意见（逐条分析）留存对话上下文，不写入正式文档

---

## 复杂度判定

| 阶段 | 内容类型 | 复杂度 | agent 配置 |
|------|---------|-------|-----------|
| 第 0 阶段 | 交互式信息收集 | 低 | 主会话直接执行（豁免） |
| 第 1 阶段 | 标杆研究（调研报告） | 高 | 每个标杆 2-3 个 researcher + 整体 2 个 reviewer |
| 第 2 阶段 | Actor-Feature 定义 | 极高 | 多 agent 分步协作（详见 `02-actor-feature.md`） |
| 第 3 阶段 | Scenario 挖掘 | 中等（基准） | 每个 domain 独立判定（综合 Actor 数量、依赖密度、规模与并发、质量关键路径等因素，非二维布尔，详见 `03-scenario.md`）：高复杂度 → ≥3 个 product + ≥2 个 reviewer；中等 → 1-2 个 product + 1-2 个 reviewer |
| 第 4 阶段 | 维护模式 | 低 | 1 个对应角色 agent |

**说明**：
- 第 1 阶段统一采用高复杂度标准，符合 devforge 定位（大型复杂基础软件）
- 第 3 阶段基准为「中等」（Scenario / 用例设计），由主会话基于 domain 多因素综合判断是否上调至「高」（详见 `03-scenario.md`）

---

## 启动检测

**核心原则**：状态汇报必须以 `ls`/`wc`/`Read` 扫描文件系统的**实时结果**为准。

**状态判定**（按执行流程顺序）：

> **判定原则**：文件存在性只是必要条件，**`✅ PASS` 评审状态标记才是阶段过渡的充分条件**。

1. domain-config.yaml 不存在 → 第 0 阶段
2. domain-config.yaml 存在，但 reference/ 下无标杆研究文件（或 < 2 个）→ 第 1 阶段（步骤1-2：标杆选择+调研产出）
3. reference/ 下标杆文件数 ≥ 2：
   - **逐个读取**每个标杆文件末尾，搜索 `**评审状态**:` 标记
   - 任一标杆文件无 `✅ PASS` 标记（或无标记 / `❌ FAIL`）→ **第 1 阶段步骤3（评审修正循环）**
   - 所有标杆文件均有 `✅ PASS` → 第 2 阶段准入通过
4. product-spec.md 不存在 → 第 2 阶段（步骤1-2：Actor 识别+Feature 定义产出）
5. product-spec.md 存在：
   - 读取文件末尾评审状态，无 `✅ PASS` 标记 → **第 2 阶段步骤3（评审修正循环）**
   - 有 `✅ PASS` 标记 → 进入特性域文档检查
6. 逐个检查各特性域文档（名单来自 product-spec.md#Feature 总览的「英文标识」列，kebab-case，如 `data-query.md`）：
   - 文档不存在 → 第 3 阶段（该特性域 Scenario 挖掘）
   - 文档存在但无 `✅ PASS` 标记 → **第 3 阶段步骤2（该特性域评审修正循环）**
   - 所有特性域文档均有 `✅ PASS` 标记 → 第 4 阶段（维护模式）

**行为规则**：主流程未完成时，根据文档状态直接判定并提示唯一的下一步动作，不罗列多个选项让用户选择；主流程已完成后（维护模式），允许给出多个改进方向供用户选择。

---

## 执行流程

> 主会话进入每个阶段时，必须先读取对应的详细规则文件（`0X-*.md`）。

### 第 0 阶段：产品定位确认

- **准入**：无
- **产出**：`.claude/domain-config.yaml`
- **规则**：`00-positioning.md`

> **两 skill 协作**：/df:define 和 /df:design 共用 T1-T6 主题清单。define 主导 T1-T5，design 主导 T6，双方互相校验。场景 B 时 define 跳过已闭合主题，从需求视角校验 design 已填的 T6；场景 C 时 define 完整执行 T1-T6。

### 第 1 阶段：标杆研究

- **准入**：第 0 阶段完成
- **产出**：`docs/requirements/reference/<product>.md`（至少 2-3 个）
- **规则**：`01-benchmarking.md`

### 第 2 阶段：Actor-Feature 识别与定稿

- **准入**：标杆研究通过评审（无 CRITICAL 问题 + 缺陷密度达标，门槛见「缺陷密度门槛标定依据」）
- **产出**：`docs/requirements/product-spec.md`（Actor-Feature 章节定稿）
- **规则**：`02-actor-feature.md`

### 第 3 阶段：Scenario 挖掘与特性域定稿

- **准入**：Actor-Feature 已定稿（无 CRITICAL 问题 + 缺陷密度达标，门槛见「缺陷密度门槛标定依据」）
- **产出**：`docs/requirements/<feature-domain-en>.md`（kebab-case 英文标识）
- **规则**：`03-scenario.md`

### 第 4 阶段：需求维护与扩展

- **准入**：所有特性域文档已定稿（无 CRITICAL 问题 + 缺陷密度达标，门槛见「缺陷密度门槛标定依据」）
- **产出**：按需修正现有文档
- **规则**：`04-maintenance.md`

---

## 输出位置

### 本 Skill 产出

- `.claude/domain-config.yaml` — 产品定位配置
- `docs/requirements/product-spec.md` — 全局需求总纲
- `docs/requirements/<feature-domain-en>.md` — 按特性域组织的需求规格（kebab-case 英文标识）
- `docs/requirements/reference/<product>.md` — 标杆需求分析

### 本 Skill 不产出

| 目录/文件 | 归属命令 |
|-----------|---------|
| `docs/architecture/` | `/df:design` |
| `docs/iteration-plan/` | `/df:plan` |
| `docs/test-strategy.md` | `/df:test-design` |

---

## 红旗信号

- 未做产品定位确认就进入标杆研究
- 标杆研究文档 < 2 个
- Feature 只有名称没有"为什么"论证
- 验收标准无法量化
- 产出文档无评审记录即直接定稿
- Agent 产出内容未写入项目文件
- 需求文档未经独立评审直接定稿
- 高/极高复杂度文档篇幅 < 100 行或低复杂度 > 400 行
- **主会话直接产出原本应由 agent 产出的内容**（如自己写标杆研究、自己设计 Actor、自己实质性修改 agent 产出）
- **Actor 中含内部组件**（写入网关/查询协调器/存储引擎/副本管理器/选主器等）— 必须剔除并归 design.md
- **Actor 类型不属于** {人类 / 外部系统 / 时间触发源} — 强制剔除
- **同类 Actor 未经合并法则压缩**（如多个数据消费者未合并、多个 SRE 子角色未压缩）
- **关系图中出现内部组件节点** — 关系图只能画 Actor，禁止画内部模块

---

## 与用户协作模式

**核心边界**：自动推进与人工确认的切换点按**交付件是否完成闭环**划分，不按"阶段内/阶段间"简单切分。

**三种协作节奏**：
1. **自动推进**：交付件内部，产出→评审→修正循环全自动闭环，不中途询问用户
2. **确认后推进**：交付件闭环后，向用户汇报产出概况，等待确认后再进入下一交付件
3. **等待人工决策**：缺陷密度超过门槛、信息缺失、或需用户做不可逆决策时停止

**阶段衔接规则**：阶段 N 结束汇报时只汇报本阶段产出摘要 + 告知下阶段名称并等待确认。禁止预演下阶段步骤、提前执行或展示下阶段待确认内容。

> 例如：阶段 0 结束只能汇报 domain-config.yaml 已生成 + 下一步进入阶段 1，不可列出候选标杆或让用户确认标杆选择。

**正常流程**：汇报状态 → 提出执行计划 → 自动执行 → 汇报结果摘要

### 对话语言约束

Skill 内部的组织语汇（T1-T6 主题编号、字段名、枚举字面值、"锚点/闭合/评估对象/缺陷密度"等术语）是 agent 执行流程的施工图，**不出现在与用户的任何输出中**（提问、确认、推断展示、阶段汇报）。

- **提问**：把"信息槽要填什么"翻译成"为什么、在什么场景、为谁、放弃什么换什么"这类业务话术
- **展示推断**：用业务语言（如"几十节点的云端集群、单客户百 TB"），不展示"字段名 + 枚举值"两栏表
- **内部翻译**：用户回答 → 枚举值的映射由 agent 自己完成，不亮枚举字面值给用户
- **阶段汇报**：可提阶段名，但不展开内部主题清单或把 T 编号当 todo 进度条
