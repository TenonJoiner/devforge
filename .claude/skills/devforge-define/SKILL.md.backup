---
name: product/define
description: 定义产品需求、制定验收标准。采用多 agent 深度思考模式，强调标杆研究先行、Feature-Scenario 分层展开、独立评审验证。禁止快速产出，强制螺旋式迭代完善
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent]
---

# product/define — 定义性思考

## 概述

需求定义是**长期、深度、迭代**的过程，不是单次对话能完成的。本 skill 采用**多 agent 协作模式**，通过持续多个阶段的思考-研究-发散-评审-完善循环，产出高质量的需求规格文档。

> **内容规范强牵引**：需求交付件的所有内容结构、章节要求、自检清单**以 `.claude/templates/*.md` 为唯一准绳**。本 skill 只负责定义**努力程度、写作风格、质量门槛和流程控制**，不再重复模板中已有的内容要求。

**核心原则**：
1. **模板牵引**：所有产出必须严格遵循对应模板，写前先读模板是铁律
2. **标杆先行**：不研究业界同类产品的需求规格，不允许定义自研 Feature
   > **与 `/df:design` 的关系**：本 skill 的标杆研究聚焦**需求视角**（用户场景、功能边界、验收标准、商业模式），与 design 的**技术架构视角**互补。两者研究对象可重叠，但分析维度不同，最终选择的标杆不必一致。
3. **Actor 驱动**：先识别所有交互角色，再围绕角色定义场景
4. **Feature 为主**：正常场景是需求的核心，故障/异常/运维场景从完整性维度补充，不应喧宾夺主
5. **分层产出**：先产出 Actor-Feature 清单并评审定稿，再进入 Scenario 挖掘，避免一次性产出过大文档无法评审
6. **可验收性**：每个 Scenario 可独立验证，验收标准必须量化
7. **长时间迭代**：需求文档需要 3-10 个阶段才能定稿，禁止一次对话定需求


## 复杂度判定

本 skill 各阶段的内容类型与基准复杂度判定如下：

| 阶段 | 内容类型 | 基准复杂度 | 判定依据 |
|------|---------|-----------|---------|
| 第 0 阶段：产品定位确认 | 交互式信息收集 | 低 | 引导式问答，无深度分析产出 |
| 第 1 阶段：标杆研究 | 调研报告 | **高** | 多视角深度分析（功能特性 vs 商业模式），产出直接指导 Feature 设计 |
| 第 2 阶段：Actor-Feature 识别 | 需求核心 Feature / Actor 定义 | **极高** | 定义产品边界、Actor 权责、验收标准 |
| 第 3 阶段：Scenario 挖掘 | Scenario / 用例设计 | **高**（默认）/ 中等（降级） | 基于已确定 Feature 的展开；分布式存储 domain 默认涉及跨子系统边界 + 性能/一致性关键路径 |
| 第 4 阶段：维护模式 | 单点修正 / 补充 | 低 | 结构固定、内容基于已有信息组合 |

**决策属性上调**（辅助判定）：
- 第 1 阶段若发现「无现成方案」（需原创设计），复杂度上调一级
- 第 3 阶段基准复杂度「中等」，但 teamskills 为分布式存储系统，domain 默认涉及「跨子系统边界」+「性能/一致性关键路径」，复杂度上调至「高」，默认配置 >=3 个 agent
- 仅当 domain 为极简单场景（纯 UI 展示、单 Actor 无交互）时才允许降级为「中等」，需明确说明降级理由

**篇幅异常检测**（辅助复核）：
- 高/极高复杂度文档篇幅 <100 行 → 红旗，需复核分析深度是否不足
- 低复杂度文档篇幅 >400 行 → 红旗，需复核是否存在冗余或信息密度过低
- 篇幅与同类文档历史均值偏差 >50% → 提示复核，确认是合理差异还是质量问题

**复杂度与 agent 配置对照**：

| 阶段 | 复杂度 | 并行产出 agent | 独立评审 agent |
|------|--------|---------------|---------------|
| 第 1 阶段 | 高 | 每个标杆 2 个 researcher | pm-reviewer + architect-reviewer |
| 第 2 阶段 | 极高 | >=5 个 product-manager | >=3 个 reviewer（pm-reviewer + architect-reviewer + 按需补充） |
| 第 3 阶段 | 高 | 每个 domain >=3 个 product-manager | >=2 个 reviewer（pm-reviewer + architect-reviewer） |
| 第 3 阶段（降级） | 中等 | 每个 domain 1 个 product-manager | 1-2 个 reviewer |
| 第 4 阶段 | 低 | 1 个对应角色 agent | 按需 |

**与特性级的衔接**：
- `/df:plan` 产出的 `docs/iteration-plan.md` 中的 proposal 清单，是 `/opsx:new` 的输入
- `/opsx:new` 时拷贝对应 proposal 作为变更单元
- 写 spec 时参考 `docs/requirements/<feature-domain>.md` 中的具体需求定义

## 文档写入铁律

**每一阶段迭代的产出必须写入项目文件，禁止仅在对话中输出。**

原因：需求定义是长期迭代过程，跨越多次对话。如果产出仅存在于对话中，下次对话将丢失所有中间成果，导致重复劳动或方向偏移。

**执行规则**：
1. Agent 产出内容后，**主会话必须立即将内容写入对应文件**
2. 写入完成后才能向用户汇报"本阶段完成"
3. 验收检查必须包含"文件是否已写入"这一项
4. **写入后必须验证文件存在**（`ls` 确认文件已落盘）

## 评审与修正记录规范

写入正式交付文档的评审记录和修正记录必须简略，避免喧宾夺主。

- **评审记录章节 ≤ 200 字**：文档中「评审记录」章节总篇幅不超过 200 字，避免喧宾夺主
- **聚焦结论**：仅保留各评审 agent 名称、通过结论（通过/有条件通过）、遗留问题数及简要分类（如"2 MEDIUM：容量估算、风险缓冲"）
- **详细评审意见不写入文档**：具体质疑点、分析过程留存于对话上下文，不写入正式交付文档正文
- **修正记录同样简略**：聚焦"改了什么"，控制在 200 字以内，不展开论证过程

## 通用术语

以下术语在本 skill 各阶段中统一使用，不重复定义。

### 问题分级

| 级别 | 含义 | 处理方式 |
|------|------|----------|
| **CRITICAL** | 阻塞性问题，会导致方向错误 | 必须修正，阻塞推进 |
| **HIGH** | 重要缺陷，显著影响质量 | 默认接受修正 |
| **MEDIUM** | 一般问题，有改善空间 | 评估后决定 |
| **LOW** | 轻微问题，可延后处理 | 可延期 |

### 评审结论三态

- **"通过"**：无未处理的 CRITICAL/HIGH 问题，或所有问题已修正/已评估
- **"有条件通过"**：存在已评估并记录处理方案的 HIGH 问题，无未处理的 CRITICAL 问题
- **"不通过"**：存在未处理的 CRITICAL 问题，或存在未评估的 HIGH 问题

### 修正工作量分级

| 级别 | 范围 | agent 配置 |
|------|------|-----------|
| 少量修正 | < 5 处，单点调整 | 1 个对应角色 agent |
| 中量修正 | 5-15 处，结构性调整 | 2 个 agent 分块并行修正 + 主会话整合 |
| 大量修正 | > 15 处或接近重写 | 回退到对应产出阶段重新执行多 agent 并行发散 |

### 标准评审修正循环骨架

1. **独立评审**：并行启动 ≥2 个 reviewer，按模板自检清单提出 CRITICAL/HIGH/MEDIUM/LOW 分级问题
2. **修正**：按修正工作量分级选择 agent 配置执行修正
3. **复核**：修正完成后重新启动独立评审
4. **循环上限**：最多 3 次
5. **终止条件**：所有 CRITICAL 和 HIGH 问题已修正 + 所有 reviewer 均给出"通过"或"有条件通过"
6. **降级规则**：3 次循环后仍未达标 → 降级置信度并标注"待后续补充"，或等待人工决策

## 主会话职责边界

主会话在多 agent 协作中只承担以下职责，**超出即越界**：

1. **调度**：决定哪个阶段派哪些 agent、什么时机并行
2. **搬运**：将 agent 产出写入文件（仅搬运，不编辑内容）
3. **整合**：当多个 agent 产出需要合并时，选择/拼接/去重（不改写核心结论）
4. **门禁**：检查出口标准、置信度阈值、agent 执行记录完整性

**主会话禁止行为**：
- 直接产出需要深度分析的文档内容
- 对 agent 产出进行实质性内容修改后写入（整合仅限选择、拼接、去重；格式统一不影响语义）
- 在没有 agent 执行记录的情况下声称某阶段已完成
- 用一个 agent "内部发散多个方案"替代多个 agent 独立产出

## 启动检测（以文件系统为真实来源）

**核心原则**：状态汇报必须以 `ls`/`wc`/`Read` 扫描文件系统的**实时结果**为准，禁止依赖任何独立维护的状态副本。

```bash
ls docs/requirements/ 2>/dev/null || echo "无现有需求文档"
```

**检测清单**：
1. 执行 `ls -la docs/requirements/`、`ls -la docs/requirements/reference/` 和 `ls -la docs/architecture/reference/` **三个命令**
2. 对声称"已完成"的文档，必须用 `ls` 验证文件存在、用 `wc -l` 验证非空
3. 汇报每个文件时必须使用**完整绝对路径**
4. 若两个目录下存在**同名或类似名文件**，必须在汇报中明确标注归属
5. **禁止行为**：
   - 只检测一个目录就声称"标杆研究已完成"
   - 将 `docs/architecture/reference/` 下的文件计入本 skill 的产出

**启动检测结果格式**（强制实时生成，禁止读取缓存）：
```
=== 启动检测 ===
文档检测结果（实时）：
- product-spec.md: [存在/不存在], X 行
- reference/product-*.md: N 份
- <feature-domain>.md: N 份
当前可判断阶段：[第 X 阶段]
当前置信度：[基于已产出文档质量判断]
```

**如果已有文档**：
> 检测到现有需求文档，根据文档完成度自动判定下一步：
> - product-spec.md 不存在 → 进入第 0 阶段（产品定位确认）
> - product-spec.md 存在但 reference/ 下无标杆研究 → 进入第 1 阶段（标杆研究）
> - reference/ 有文件但 Actor-Feature 未定稿 → 进入第 2 阶段（Actor-Feature 识别）
> - Actor-Feature 已定稿但 <feature-domain>.md 不完整 → 进入第 3 阶段（Scenario 挖掘）
> - 全部文档已定稿 → 进入第 4 阶段（维护模式），此时可选择具体改进方向

## 执行流程

### 第 0 阶段：产品定位确认（强制）

**准入条件**：无
**产出文件**：`docs/requirements/product-spec.md`（初始定位草稿，后续阶段持续丰富）

在启动标杆研究之前，必须先完成轻量级产品定位确认。如果产品边界不清，标杆研究可能选错对象，导致后续全部返工。

**核心原则**：
- 通过交互式提问逐层深入理解产品定位，禁止一次性抛出所有问题
- 追问策略和交互技巧详见 `product-manager.md`

**执行方式**：第 0 阶段为交互式产品定位确认，由 product-manager agent 执行。

确认后：**读取 `.claude/templates/req-product-spec.md` 模板**，将定位结论整理为 `product-spec.md` 初始草稿并落盘。

> **product-spec.md 的定位**：从第 0 阶段开始，它就是**持续迭代的活文档**。第 1 阶段标杆研究可能反过来修正产品定位，此时需**回溯修正** product-spec.md 中的定位章节。

> **豁免说明**：第 0 阶段适用豁免场景：纯交互式信息收集阶段。不执行完整多 agent 评审修正循环，但需通过准入检查清单 + 置信度评分确认信息充分性。

**第 0 阶段准入检查清单**（写入 `product-spec.md` 初稿前必须确认）：
- [ ] 核心痛点已收敛到一句话描述
- [ ] 所有 Actor 类型已明确列出（区分人类用户和外部系统）
- [ ] 部署拓扑和规模量级已确认
- [ ] 至少 2-3 个关键约束已明确（能用数字或等级描述）
- [ ] 至少 2-3 个"明确不做"的事项已列出
- [ ] 用户已确认定位摘要无误

**第 0 阶段置信度评分**（信息充分性评估）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 痛点收敛度 | 40% | 能用一句话描述核心痛点 |
| Actor 完整性 | 30% | 所有关键 Actor 已识别，无遗漏 |
| 约束明确度 | 30% | 至少 2-3 个可量化的关键约束 |

**进入第 1 阶段门槛：>= 0.75**
> 门槛说明：第 0 阶段为信息收集阶段，尚未产生正式设计决策，门槛适当降低至 0.75。后续阶段逐步提高（第 1 阶段 0.80，第 2/3 阶段 0.85）。
**低于门槛处理**：标记信息缺口清单，继续交互式提问补充，禁止进入第 1 阶段

**自动推进规则**：准入检查清单全部通过 + 置信度 >= 0.75 -> 自动进入第 1 阶段；否则继续交互式提问

### 第 1 阶段：标杆研究（强制，不可跳过）

**准入条件**：第 0 阶段产品定位确认已完成，且 `product-spec.md` 初始草稿已落盘
**产出文件**：`docs/requirements/reference/<product>.md`（每个标杆一个文件，至少 2-3 个）
**模板**：`.claude/templates/ref-requirements.md`

**执行方式**：
1. 主会话基于已有上下文识别候选标杆列表，向用户确认标杆选择
2. **每个标杆并行启动 2 个 researcher agent**（需求模式），各自从不同需求视角独立研究同一标杆
   - 产出目录：`docs/requirements/reference/`
   - 分析视角：**需求/用户/产品视角**，禁止技术分析
   - 模板约束：使用 `.claude/templates/ref-requirements.md`
   - **禁止**两个 researcher 使用同一 prompt 模板、同一分析框架
3. researcher 完成后，**主会话将内容写入文件**
4. **并行启动 `pm-reviewer` + `architect-reviewer` 执行独立评审**
   - `pm-reviewer` 重点评审需求视角正确性、功能边界清晰度、量化指标合理性
   - `architect-reviewer` 重点评审研究深度、方法论严谨性、结论与产品定位的匹配度
5. **主会话验收**：检查文档中是否出现技术实现细节，如有则**强制打回重写**
6. 不达标时反馈 researcher 补充分析，**评审修正循环**直到通过

**出口标准**：
- [ ] 每个标杆完成"功能特性组织方式、Scenario 策略、量化边界"的分析
- [ ] 无技术实现细节混入（纯需求/产品视角）
- [ ] `pm-reviewer` 和 `architect-reviewer` 均给出"通过"或"有条件通过"（定义见「通用术语」评审结论三态）
- [ ] 所有标杆文件已落盘并验证存在
- [ ] 文档状态标记为"第 1 阶段完成"
- [ ] **CRITICAL 问题**：标杆数量 < 2、或任一标杆未完成三维度分析、或存在技术实现细节混入 -> 必须修正，阻塞进入第 2 阶段
- [ ] **HIGH 问题**：分析深度不足、关键维度遗漏 -> 默认接受修正
- [ ] **MEDIUM/LOW 问题**：记录但不阻塞推进，纳入后续迭代跟踪

**评审修正循环**（按「通用术语」标准骨架执行）：
- **评审方**：`pm-reviewer` + `architect-reviewer`
- **修正 agent**：researcher；中量修正：2 个 researcher 分块并行修正 + 主会话整合
- **大量修正**：回退到重新选择标杆或重新执行多 researcher 并行研究

**第 1 阶段置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 研究深度 | 25% | 有 Feature 组织方式、Scenario 策略、量化边界的分析 |
| 视角正确性 | 25% | 无技术实现细节，纯需求视角 |
| 覆盖完整性 | 20% | 至少 2-3 个标杆，每个都有适用性评估 |
| 洞察质量 | 20% | 有明确的"对我方需求的启示" |
| 可复用性 | 10% | 结论能直接指导第 2 阶段 Actor-Feature 设计 |

**进入第 2 阶段门槛：>= 0.80**

**低于门槛处理规则**：
- 置信度 0.75-0.79 -> 自动再修正一次（执行评审修正循环）
- 置信度 < 0.75 -> 等待人工决策，禁止进入第 2 阶段

**自动推进规则**：
- 置信度 >= 0.80 且双评审均"通过"或"有条件通过" -> **确认后推进**（向用户汇报结果摘要，建议进入第 2 阶段）
- 置信度 < 0.80 或存在未修正 CRITICAL 问题 -> 等待人工决策或执行评审修正循环

### 第 2 阶段：Actor-Feature 识别与定稿（强制）

**准入条件**：第 1 阶段标杆研究完成并通过 product-manager 验收（>= 0.80）

#### Step 1：任务解构与 Actor 识别

> **Plan 阶段内联**：第 2 阶段为极高复杂度（核心 Feature / Actor 定义），必须先解构再发散。在并行启动 product-manager agent 前，派遣 **1 个 product-manager agent** 执行轻量级任务解构：
> 1. **派遣 product-manager agent 读取 `.claude/templates/req-product-spec.md`**，识别各章节的决策权重分布（Actor 定义、Feature 定义、Scenario 设计各占比）
> 2. 确认切分维度、分配视角约束、列出关键假设、识别评审视角缺口
> 
> 任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 product-manager agent 调度时 prompt 的组成部分。
>
> **任务解构完成判定**：模板已读取并识别决策权重分布、切分维度已确定并记录（如按用户场景视角拆分：终端用户/运维治理/集成扩展）、每个 product-manager agent 的视角约束已分配、关键假设清单已列出（如"假设 Actor 识别完整""假设标杆研究结论可直接映射"）、评审视角缺口已识别（如"需要从技术可行性角度补充评审"）、产出已由主会话以结构化文本形式在对话中输出。
>
> **Plan 阶段完成判定**：任务解构产出为方向性指引而非正式文档，不执行完整评审修正循环，但需满足上述任务解构完成判定方可进入 Actor 识别。

**产出文件**：`docs/requirements/product-spec.md#actors`

1. **product-manager** 基于产品定位和标杆研究洞察，识别所有 Actor
2. **并行启动 researcher** 补充竞品 Actor 映射表
3. 每个 Actor 明确：类型、职责、交互方式、关注点
4. **写入文件**

**Step 1 出口标准**：
- [ ] Actor 列表非空，至少识别出 1 个核心 Actor
- [ ] 每个 Actor 已明确：类型、职责、交互方式、关注点
- [ ] 隐性 Actor 检查已完成（安全审计/合规检查/运维值班/第三方集成/监控告警系统）
- [ ] 文档已按模板结构写入 `product-spec.md#actors`
- [ ] 文件已验证存在（`ls`/`wc` 确认）
- [ ] 文档状态标记为"第 2 阶段 Step 1 完成（Actor 已识别）"

**Step 1 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| Actor 完整性 | 40% | 无遗漏隐性 Actor，核心 Actor 已识别 |
| 职责清晰度 | 30% | 每个 Actor 的类型/职责/交互方式/关注点已明确 |
| 交互关系明确度 | 30% | Actor 间交互关系已初步梳理 |

**进入门槛：>= 0.80**

**低于门槛处理规则**：
- 置信度 0.75-0.79 -> 补充 Actor 识别后重新评分
- 置信度 < 0.75 -> 等待人工决策

**评审说明**：Step 1 产出（Actor 列表）为 Step 2 Feature 识别的输入，非独立可交付物。Actor 质量在 Step 3 独立评审中覆盖。若 Step 3 评审发现 Actor 相关问题 → 在 Step 4 修正阶段按修正工作量分级处理：
  - 少量修正（< 5 处）：1 个 product-manager agent 修正
  - 中量修正（5-15 处）：2 个 product-manager 分块并行修正 + 主会话整合
  - 大量修正（> 15 处或结构性重设计）：回退到 Step 1 重新执行 Actor 识别，并重新执行 Step 2（Feature 识别）

**自动推进规则**：
- 出口标准全部通过 + 置信度 >= 0.80 -> 自动进入 Step 2（Feature 识别）
- 置信度 < 0.80 -> 等待人工决策或补充识别

#### Step 2：Feature 识别（多 agent 并行）

**产出文件**：`docs/requirements/product-spec.md`（草稿状态）

1. **并行启动 3 个 product-manager agent**，各自从不同用户场景角度围绕 Actor 核心目标独立推导 Feature 列表
   - 禁止用一个 agent "内部发散多个方案"替代多个 agent 独立产出
   - 视角切分由任务解构的 product-manager agent 确定，主会话按切分结果调度
   - 每个 agent 独立产出，互不参考对方结论
   - **3 个 agent 的视角区分**（由任务解构的 product-manager agent 确定，必须按以下维度区分）：
     - `product-manager`（终端用户场景视角）：侧重终端用户直接交互的功能需求、用户体验、易用性
     - `product-manager`（运维治理场景视角）：侧重系统管理员、运维人员的管理需求、可观测性、故障处理
     - `product-manager`（集成扩展场景视角）：侧重第三方集成、API 设计、扩展性需求
2. 主会话整合三个 agent 的 Feature 列表，去重并客观记录冲突（仅记录差异，不做价值判断或合并决策，冲突解决交由评审修正循环处理）
3. 每个 Feature 记录：一句话价值摘要、涉及 Actor、归属特性域、优先级
4. 产出 Feature 间依赖关系矩阵
5. **并行启动 architect** 评估各 Feature 实现复杂度
6. Feature 价值论证必须是推导式，禁止"矩阵后直接给结论"
7. 多特性域时按域并行启动多个 product-manager agent（每域遵循高复杂度配置：>=2 个 agent）
8. **读取 `.claude/templates/req-product-spec.md` 模板**，写入 `product-spec.md`

**Step 2 出口标准**：
- [ ] Feature 列表非空，覆盖所有已识别 Actor 的核心目标
- [ ] 每个 Feature 已记录：一句话价值摘要、涉及 Actor、归属特性域、优先级
- [ ] Feature 间依赖关系矩阵已产出
- [ ] 价值论证为推导式，非直接给结论
- [ ] 文档已按模板结构写入 `product-spec.md`
- [ ] 文件已验证存在（`ls`/`wc` 确认）
- [ ] 文档状态标记为"第 2 阶段 Step 2 完成（Feature 已识别，待评审）"
- [ ] 已明确下一步进入 Step 3 独立评审

**评审说明**：Step 2 产出（Feature 列表）在 Step 3 独立评审中直接评审（pm-reviewer + architect-reviewer 同步评审 Feature 覆盖度、价值论证、依赖关系）。若 Step 3 评审发现 Feature 相关问题：
  - 少量修正（< 5 处）：1 个 product-manager agent 修正
  - 中量修正（5-15 处）：2 个 product-manager 分块并行修正 + 主会话整合
  - 大量修正（> 15 处或结构性重设计）：回退到 Step 2 重新执行多 agent 并行发散

**Step 2 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| Feature 覆盖度 | 30% | 覆盖所有已识别 Actor 的核心目标，无遗漏关键路径 |
| 价值论证质量 | 30% | 每个 Feature 有"做什么+为什么"的推导式论证 |
| 依赖关系清晰度 | 20% | Feature 间依赖矩阵完整，无循环依赖 |
| 与标杆对齐度 | 20% | Feature 设计体现标杆研究洞察 |

**进入门槛：>= 0.85**（Actor-Feature 定义为需求核心，涉及产品边界，门槛 >= 0.85）

**低于门槛处理规则**：
- 置信度 0.80-0.84 -> 自动再修正一次（补充价值论证或覆盖缺失 Feature）
- 置信度 < 0.80 -> 等待人工决策

**自动推进规则**：
- 出口标准全部通过 + 置信度 >= 0.85 -> 自动进入 Step 3（独立评审）
- 置信度 0.80-0.84 -> 自动再修正一次
- 置信度 < 0.80 -> 等待人工决策

#### Step 3：独立评审（评审修正循环起点）

**准入条件**：`product-spec.md` 草稿已写入文件

1. **并行启动 `pm-reviewer` 和 `architect-reviewer`** 执行**独立评审**
   - `pm-reviewer` 覆盖 3 个 product-manager agent 的用户场景视角差异：逐一检查每个 agent 视角产出的 Feature 是否被遗漏、冲突是否被客观记录、价值论证是否完整
   - `architect-reviewer` 从跨子系统边界和实现可行性视角审视 Feature 列表，识别 3 个 product-manager agent 均未覆盖的技术约束
2. 质疑点数量要求：按特性域复杂度（简单>=3、中等>=5、复杂>=7）

**Step 3 出口标准**：
- [ ] pm-reviewer 和 architect-reviewer 均完成独立评审
- [ ] 质疑点数量达标（简单>=3、中等>=5、复杂>=7）
- [ ] 所有质疑点已按 CRITICAL/HIGH/MEDIUM/LOW 分级标注
- [ ] 评审意见已写入文件或评审记录章节
- [ ] 评审通过标准：pm-reviewer 和 architect-reviewer 均给出"通过"或"有条件通过"，无未处理 CRITICAL 问题
- [ ] 评审意见已写入 `product-spec.md#评审记录` 或独立评审记录文件
- [ ] **评审记录章节 ≤ 200 字**（仅保留评审 agent 名称、通过结论、遗留问题数及简要分类）
- [ ] 评审修正循环上限：最多 3 次

**Step 3 置信度评分**（评审质量评估）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 评审深度 | 40% | 质疑点数量达标（简单>=3、中等>=5、复杂>=7），分级准确 |
| 视角独立性 | 30% | 双评审视角差异化，无重复质疑 |
| 问题分级准确性 | 30% | CRITICAL/HIGH/MEDIUM/LOW 分级合理，无遗漏关键问题 |

**进入门槛：>= 0.80**

**低于门槛处理规则**：
- 置信度 < 0.80 -> 补充评审深度或调整视角后重新评审

**自动推进**：双评审均"通过"或"有条件通过" + 置信度 >= 0.80 -> 自动进入 Step 4；任一评审"不通过"或置信度 < 0.80 -> 等待人工决策或**进入评审修正循环**

> **评审修正循环规则**：详见 Step 4。循环上限：最多 3 次。

#### Step 4：评审修正定稿

评审完成后**不等待用户确认**，由 product-manager 自动决策并执行修正。

**评审修正循环**（按「通用术语」标准骨架执行）：
- **独立评审**：并行启动 `pm-reviewer` + `architect-reviewer` 执行复核
- **修正 agent**：product-manager；少量修正（<5 处）：1 个 product-manager agent 修正；中量修正（5-15 处）：2 个 product-manager 分块并行修正 + 主会话整合
- **大量修正**：回退到 Step 2 重新执行多 agent 并行发散
- **自动决策规则**：CRITICAL 必须修正 → HIGH 默认修正 → MEDIUM 评估后决定 → LOW 可延期

**Step 4 置信度评分**（修正质量评估）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| CRITICAL 修正完整性 | 40% | 所有 CRITICAL 问题已修正 |
| HIGH 问题处理质量 | 30% | 所有 HIGH 问题已修正或有明确处理方案 |
| 评审回应充分性 | 20% | 所有质疑点有明确回应 |
| 文档一致性 | 10% | 修正后与 product-spec.md 其他章节无冲突 |

**进入门槛：>= 0.85**

**低于门槛处理规则**：
- 置信度 0.75-0.84 → 自动再修正一次
- 置信度 < 0.75 → **等待人工决策**

**自动推进规则**：
- 所有 CRITICAL 已修正 + 所有 HIGH 已修正或有明确处理方案 + 双 reviewer "通过"或"有条件通过" + 置信度 >= 0.85 → **确认后推进**（向用户汇报结果摘要，等待用户确认后进入第 3 阶段）
- 置信度 0.75-0.84 → 自动再修正一次
- 置信度 < 0.75 → **等待人工决策**

**第 2 阶段置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| Actor 完整性 | 25% | 无遗漏隐性 Actor |
| Feature 价值论证 | 25% | 每个 Feature 有"做什么+为什么" |
| 依赖关系清晰度 | 20% | Feature 间依赖矩阵完整 |
| 双评审质量 | 20% | 质疑都有明确回应 |
| 与标杆对齐度 | 10% | Feature 设计体现标杆研究洞察 |

**出口标准**：
- [ ] 所有 CRITICAL 问题已修正（阻塞性问题必须修正，否则不得推进）
- [ ] 所有 HIGH 问题已评估：接受修正 / 接受延期（需记录理由）/ 拒绝（需说明原因）
- [ ] MEDIUM/LOW 问题记录但不阻塞推进，纳入后续迭代跟踪
- [ ] pm-reviewer 和 architect-reviewer 均给出"通过"或"有条件通过"（定义见「通用术语」评审结论三态）
- [ ] 各特性域置信度 >= 0.85
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 2 阶段完成（Actor-Feature 已定稿）"

### 第 3 阶段：Scenario 挖掘与特性域定稿（强制）

**准入条件**：第 2 阶段 Actor-Feature 已定稿（>= 0.85）

#### Step 1：任务解构与 Scenario 挖掘（分域并行）

> **Plan 阶段内联**：第 3 阶段为高复杂度（Scenario / 用例设计，默认涉及跨子系统边界 + 性能/一致性关键路径），必须先解构再发散。在并行启动 product-manager agent 前，派遣 **1 个 architect agent** 执行轻量级任务解构：确认切分维度、分配视角约束、列出关键假设、识别评审视角缺口。任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 product-manager agent 调度时 prompt 的组成部分。
>
> **任务解构完成判定**：切分维度已确定（如按分析类型拆分：正常路径/故障异常路径）、每个 product-manager agent 的视角约束已分配、关键假设清单已列出（如"假设 Feature 依赖关系已稳定""假设 Actor 边界无变更"）、评审视角缺口已识别、产出已由主会话以结构化文本形式在对话中输出。
>
> **Plan 阶段完成判定**：任务解构产出为方向性指引而非正式文档，不执行完整评审修正循环，但需满足上述任务解构完成判定方可进入 Scenario 挖掘。

**产出文件**：各 `docs/requirements/<feature-domain>.md` 初稿

1. 主会话读取 `product-spec.md`（Actor-Feature 终稿）作为统一输入源
2. 按 domain 拆分，无依赖的 domain 并行处理
3. **每个 domain 的 agent 配置**（Scenario 挖掘 = **高复杂度**，默认按分析类型切分，配置 >=3 个 product-manager agent）
4. **并行启动 >=3 个 `product-manager` agent**（按分析类型切分）：
   - `product-manager`（正常路径视角）：侧重正常业务流程、用户预期行为、标准操作场景
   - `product-manager`（故障/异常路径视角）：侧重故障处理、异常恢复、边界条件
   - `product-manager`（性能/一致性关键路径视角）：侧重性能瓶颈、一致性保证、关键路径优化
   - 无依赖的 domain 可并行处理

> **降级条件**：仅当 domain 为极简单场景（如纯 UI 展示、单 Actor 无交互）时，允许降级为 2 个 agent，需在 Plan 阶段明确说明降级理由（如"该 domain 无跨子系统交互，无性能/一致性关键路径，降级为正常路径 + 故障路径双视角"）。
5. 每个 agent 基于定稿的 Actor-Feature 挖掘 Scenario
6. **所有内容结构严格遵循 `.claude/templates/req-feature.md` 模板**
7. 量化非功能需求
8. 所有 domain 初稿完成后，统一进入 Step 2

**Step 1 出口标准**（每个 domain 文档）：
- [ ] 文档已按 `req-feature.md` 模板结构写入
- [ ] 每个 Scenario 有明确的触发条件、执行步骤、预期结果
- [ ] 每个 Scenario 的验收标准可量化、可独立验证
- [ ] 非功能需求有量化指标（具体数字 + 验收方法）
- [ ] 文档已写入 `docs/requirements/<feature-domain>.md`
- [ ] 文件已验证存在（`ls`/`wc` 确认）
- [ ] 文档状态标记为"第 3 阶段 Step 1 完成（Scenario 已挖掘，待评审）"
- [ ] 已明确下一步进入 Step 2 独立评审

**评审说明**：Step 1 产出（Scenario 初稿）在 Step 2 独立评审中直接评审（pm-reviewer + architect-reviewer 同步评审 Scenario 质量）。若 Step 2 评审发现 Scenario 相关问题 → 在 Step 3 修正阶段按修正工作量分级处理。

**Step 1 置信度评分**（按 domain）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 模板符合度 | 25% | 按 `req-feature.md` 模板结构组织，必填章节无遗漏 |
| Scenario 完整性 | 25% | 正常路径充分 + 至少 4 种故障模式 |
| 验收标准可验证性 | 25% | 每个 Scenario 有可量化验收标准 |
| 非功能需求量化 | 25% | 有具体数字和验收方法 |

**进入门槛：>= 0.80**

**低于门槛处理规则**：
- 置信度 0.75-0.79 -> 自动再修正一次（补充缺失 Scenario 或量化指标）
- 置信度 < 0.75 -> 等待人工决策

**自动推进规则**：
- 所有 domain 出口标准通过 + 各 domain 置信度 >= 0.80 -> 自动进入 Step 2（独立评审）
- 任一 domain 置信度 < 0.80 -> 等待人工决策

#### Step 2：独立评审（评审修正循环起点）

**准入条件**：特性域文档初稿已写入文件

1. **并行启动 `pm-reviewer` 和 `architect-reviewer`** 执行**独立评审**
2. 质疑点数量要求与第 2 阶段 Step 3 相同

**Step 2 出口标准**：
- [ ] pm-reviewer 和 architect-reviewer 均完成独立评审
- [ ] 质疑点数量达标（简单>=3、中等>=5、复杂>=7）
- [ ] 所有质疑点已按 CRITICAL/HIGH/MEDIUM/LOW 分级标注
- [ ] 评审意见已写入评审记录章节
- [ ] **评审记录章节 ≤ 200 字**（仅保留评审 agent 名称、通过结论、遗留问题数及简要分类）
- [ ] 评审结论符合「通用术语」评审结论三态

**Step 2 置信度评分**（评审质量评估）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 评审深度 | 40% | 质疑点数量达标（简单>=3、中等>=5、复杂>=7），分级准确 |
| 视角独立性 | 30% | 双评审视角差异化，无重复质疑 |
| 问题分级准确性 | 30% | CRITICAL/HIGH/MEDIUM/LOW 分级合理，无遗漏关键问题 |

**进入门槛：>= 0.80**

**低于门槛处理规则**：
- 置信度 < 0.80 -> 补充评审深度或调整视角后重新评审

**自动推进**：双评审均"通过"或"有条件通过" + 置信度 >= 0.80 -> 自动进入 Step 3；任一评审"不通过"或置信度 < 0.80 -> 等待人工决策或直接进入修正

> **评审修正循环规则**：详见 Step 3（与第 2 阶段 Step 4 一致的修正阶段 agent 配置规则）。循环上限：最多 3 次。

#### Step 3：评审修正定稿

按 domain 分别评分。评审修正循环按「通用术语」标准骨架执行：
- **修正 agent**：product-manager（配置规则同第 2 阶段 Step 4）
- **复核**：修正后重新启动独立评审（pm-reviewer + architect-reviewer）
- **特殊终止条件**：所有 CRITICAL 已修正 + 所有 HIGH 已修正或有明确处理方案 + 双 reviewer "通过"或"有条件通过" + 各特性域置信度 >= 0.85

**自动推进规则**：
- 各特性域置信度 >= 0.85 且双评审均"通过"或"有条件通过" -> **确认后推进**（向用户汇报结果摘要，等待用户确认后进入第 4 阶段维护模式）
- 置信度 0.75-0.84 -> 自动再修正一次
- 置信度 < 0.75 -> 等待人工决策

**第 3 阶段置信度评分**（按 domain）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 场景覆盖度 | 25% | 正常路径充分 + 至少 4 种故障模式 |
| 验收标准可验证性 | 25% | 每个 Scenario 有可量化验收标准 |
| 非功能需求量化 | 20% | 有具体数字和验收方法 |
| 双评审质量 | 20% | 所有质疑点被回应 |
| 命名空间纪律 | 10% | 无子系统内部模块名，纯用户视角 |

**出口标准**：
- [ ] 所有 CRITICAL 问题已修正（阻塞性问题必须修正，否则不得推进）
- [ ] 所有 HIGH 问题已评估：接受修正 / 接受延期（需记录理由）/ 拒绝（需说明原因）
- [ ] MEDIUM/LOW 问题记录但不阻塞推进，纳入后续迭代跟踪
- [ ] pm-reviewer 和 architect-reviewer 均给出"通过"或"有条件通过"（定义见「通用术语」评审结论三态）
- [ ] 各特性域置信度 >= 0.85
- [ ] 验收标准可量化、可独立验证
- [ ] 非功能需求有量化指标
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 3 阶段完成（Scenario 已定稿）"

### 第 4 阶段：需求维护与扩展（维护模式）

**核心定位**：前 3 阶段完成后，需求文档已进入"可用状态"。第 4 阶段是**维护模式**——按需调用 `/df:define` 来修正、补充或调整需求定义。

**快速通道**（小修不走完整阶段）：

> **豁免说明**：快速通道属于维护模式单点修正，适用豁免场景。不走完整出口标准和评审修正循环，但修正后仍需至少 1 个 reviewer 快速复核。

流程：
- 文档一致性修正 / 单 Feature 补充 / 单篇标杆补充 → 修正 → **1 个 pm-reviewer 快速复核**

**快速通道出口标准**：
- [ ] 修正范围仅限于声明的修改点（单文件单章节或单篇标杆）
- [ ] 修正后与关联文档无冲突
- [ ] 至少 1 个 pm-reviewer 快速复核通过（无 CRITICAL/HIGH 问题；若发现 CRITICAL → 回退到对应完整阶段）
- [ ] 文档已写入并验证存在

**快速通道置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 修正准确性 | 40% | 修改内容准确无误，无逻辑错误 |
| 影响范围可控性 | 30% | 变更限于单文件单点，无涟漪效应 |
| 关联文档一致性 | 30% | 修改后与相关文档无矛盾 |

**进入门槛：>= 0.80**

**快速通道评审修正循环**：
- 快速复核发现问题 → 修正 → 再次快速复核
- **循环上限**：最多 1 次；1 次后仍不通过 → 回退到对应完整阶段
- **终止条件**：快速复核通过 → 自动落盘；发现 CRITICAL 问题 → 回退到对应完整阶段

**回退机制**（大变更回退到对应子流程重新执行）：
- 新增 Feature -> 回退到第 2 阶段 Step 2 -> Step 3 -> Step 4 -> 第 3 阶段
- 推翻已有 Scenario -> 回退到第 3 阶段 Step 1 起
- 新增特性域 -> 回退到第 3 阶段 Step 1 起

## 输出格式

所有需求交付件的内容结构**以模板为唯一规范来源**，写文档前**必须先读对应模板**。模板映射关系见上述各阶段的"模板"字段。

## 输出位置

### 本 Skill 产出（仅限 `docs/requirements/`）

- `docs/requirements/product-spec.md` — 全局需求总纲
- `docs/requirements/<feature-domain>.md` — 按特性域组织的需求规格
- `docs/requirements/reference/<product>.md` — 标杆需求分析

### 本 Skill 不产出

| 目录/文件 | 归属命令 |
|-----------|---------|
| `docs/architecture/` | `/df:design` |
| `docs/iteration-plan/` | `/df:plan` |
| `docs/test-strategy.md` | `/df:test-design` |

## 红旗信号

- 未做产品定位确认就进入标杆研究
- 标杆研究文档 < 2 个
- Feature 只有名称没有"为什么"论证
- 验收标准无法量化
- 单次对话内完成"研究->Feature->Scenario->文档"
- Agent 产出内容未写入项目文件
- 需求文档未经独立评审直接定稿
- 修正阶段未按工作量分级配置 agent（如中量修正仍用 1 个 agent）
- Actor-Feature 未定稿就进入 Scenario 挖掘
- 出口标准 checklist 未全部勾选就推进到下一阶段
- 评审修正循环超过 3 次仍不达标但未降级或等待人工决策
- 快速通道修正后未经 reviewer 复核直接落盘
- 目录归属混淆

## 与用户协作模式

**三种协作节奏统一使用**：
- **自动推进**：阶段内产出 -> 独立评审 -> 评审修正循环自动执行，循环结束后汇报结果
- **确认后推进**：阶段/Step 完成后汇报，等待用户确认再进入下一阶段/Step（第 0 阶段产品定位确认全程需要用户参与）
- **等待人工决策**：信息缺失、方向选择、置信度 < 0.75 时停止，等待用户决策

**正常流程**：汇报状态 -> 提出执行计划 -> 自动执行 -> 汇报结果摘要

**三种必须用户介入的情况**：信息缺失 | 方向选择 | 置信度 < 0.75（**等待人工决策**）

## 输出检查清单

- [ ] 第 0 阶段产品定位确认已完成，`product-spec.md` 初始草稿已落盘
- [ ] 第 1 阶段所有标杆研究文档已产出（>= 0.80），评审修正循环已完成
- [ ] 第 2 阶段 Actor 定义清晰，Feature 有价值论证（>= 0.85），所有 CRITICAL 问题已修正
- [ ] 第 3 阶段每个 Feature 的 Scenario 覆盖正常路径 + 故障模式，所有 CRITICAL 问题已修正
- [ ] 验收标准可量化、可独立验证
- [ ] 非功能需求有量化指标
- [ ] 各阶段独立评审已完成，评审 agent 均给出"通过"或"有条件通过"
- [ ] 各阶段评审修正循环已完成（<= 3 次循环）
- [ ] 各特性域置信度 >= 0.85
- [ ] 文档已写入项目文件并验证存在
