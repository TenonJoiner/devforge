---
name: product/design
description: 架构探索与决策 skill。采用多 agent 深度思考模式，强调标杆研究先行、方案对比、长时间迭代。禁止快速产出，强制螺旋式完善
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent]
---

# product/design — 架构探索与决策

## 概述

架构探索是**长期、深度、迭代**的过程，不是单次对话能完成的。本 skill 通过持续多个阶段的思考-研究-对比-完善循环，产出高质量的架构设计。

**核心原则**：
1. **标杆先行**：不研究业界方案，不允许提出自研方案
   > **与 `/df:define` 的关系**：本 skill 的标杆研究聚焦**技术架构视角**（原理层→设计层→优化层、子系统分解、数据流、一致性协议），与 define 的**需求视角**（用户场景、功能边界、验收标准）互补。两者选择的标杆不必一致。
2. **深度对比**：至少 3 个候选方案，每个方案必须有明确的取舍分析
3. **长时间迭代**：架构文档需要 5-20 个阶段才能定稿，禁止一次对话定架构
4. **方案 > 代码**：聚焦设计决策和 rationale，不讨论具体代码实现
5. **模板即法源**：所有正式交付文档的内容结构、必填章节、自检清单以 `.claude/templates/*.md` 为唯一标准

## Agent 调度总览

本 skill 调度 researcher、architect、architect-reviewer 三个预定义 agent。

## 复杂度判定

本 skill 各阶段的内容类型与基准复杂度判定如下：

| 阶段 | 内容类型 | 基准复杂度 | 判定依据 |
|------|---------|-----------|---------|
| 第 0 阶段：启动检测 + 前置调研 | 交互式信息收集 | 低 | 引导式问答，确定设计目标和约束 |
| 第 1 阶段：标杆研究 | 调研报告 | 中等 | 技术视角的信息整理与对比分析 |
| 第 2 阶段 Step 1：整体架构发散 | 系统级架构设计 | **极高** | 定义子系统边界、交互契约、全局数据流 |
| 第 2 阶段 Step 3：维度调研 | 调研报告 | 中等 | 单一技术域内信息整理与对比（researcher 执行） |
| 第 2 阶段 Step 4：维度方案发散 | 子系统级设计 | **高** | 单一技术域内多方案对比与选型（architect 执行） |
| 第 3 阶段 Step 1：ADR 收敛 | 架构决策（ADR） | **极高** | 影响多个子系统、一旦确定难以回退 |
| 第 3 阶段 Step 2：系统总纲定稿 | 系统级架构设计 | **极高** | 定义全局边界和数据流 |
| 第 3 阶段 Step 3：子系统架构定稿 | 子系统级设计 | **高** | 单一子系统内多技术域交叉 |
| 第 4 阶段：维护模式 | 单点修正 / 补充 | 低 | 结构固定、内容基于已有信息组合 |

**决策属性上调**（辅助判定）：
- 若涉及「跨子系统边界」或「一致性关键路径」，复杂度在基准上 +1 级
- 若涉及「不可逆决策」（一旦确定难以回退），复杂度在基准上 +1 级
- 若「无现成方案」（需原创设计），复杂度在基准上 +1 级
- 若涉及「安全 / 合规敏感」，复杂度在基准上 +1 级

**篇幅异常检测**（辅助复核）：
- 高/极高复杂度文档篇幅 <100 行 → 红旗，需复核分析深度是否不足
- 低复杂度文档篇幅 >400 行 → 红旗，需复核是否存在冗余或信息密度过低
- 篇幅与同类文档历史均值偏差 >50% → 提示复核，确认是合理差异还是质量问题

**复杂度与 agent 配置对照**：

| 阶段 | 复杂度 | 并行产出 agent | 独立评审 agent |
|------|--------|---------------|---------------|
| 第 1 阶段 | 中等 | 每个标杆 2 个 researcher | 2 个 architect-reviewer |
| 第 2 阶段 Step 1 | 极高 | ≥5 个 architect | ≥3 个 architect-reviewer |
| 第 2 阶段 Step 3 | 中等 | 每个维度 2 个 researcher | 委托至 Step 4 评审 |
| 第 2 阶段 Step 4 | 高 | 每个维度 ≥3 个 architect | 每个维度 ≥2 个 architect-reviewer |
| 第 3 阶段 Step 1 | 极高 | ≥5 个 architect | ≥3 个 architect-reviewer |
| 第 3 阶段 Step 2 | 极高 | ≥5 个 architect | ≥3 个 architect-reviewer |
| 第 3 阶段 Step 3 | 高 | ≥3 个 architect | ≥2 个 architect-reviewer |
| 第 4 阶段 | 低 | 1 个对应角色 agent | 按需 |

## 文档写入铁律

**每一阶段迭代的产出必须写入项目文件，禁止仅在对话中输出。**

**执行规则**：
1. Agent 产出内容后，**主会话必须立即将内容写入对应文件**
2. 写入完成后才能向用户汇报"本阶段完成"
3. 验收检查必须包含"文件是否已写入"这一项
4. **正式文档写入前，必须确认对应模板已被读取**

## 评审与修正记录规范

写入正式交付文档的评审记录和修正记录必须简略，避免喧宾夺主。

- **评审记录章节 ≤ 200 字**：文档中「评审记录」章节总篇幅不超过 200 字，避免喧宾夺主
- **聚焦结论**：仅保留各评审 agent 名称、通过结论（通过/有条件通过）、遗留问题数及简要分类（如"2 MEDIUM：容量估算、风险缓冲"）
- **详细评审意见不写入文档**：具体质疑点、分析过程留存于对话上下文，不写入正式交付文档正文
- **修正记录同样简略**：聚焦"改了什么"，控制在 200 字以内，不展开论证过程

## 通用术语

以下术语在本 skill 各阶段中统一使用，不重复定义。

### 问题分级

| 级别 | 含义 | 处理方式 |
|------|------|----------|
| **CRITICAL** | 阻塞性问题，会导致方向错误 | 必须修正，阻塞推进 |
| **HIGH** | 重要缺陷，显著影响质量 | 默认接受修正 |
| **MEDIUM** | 一般问题，有改善空间 | 评估后决定 |
| **LOW** | 轻微问题，可延后处理 | 可延期 |

### 评审结论三态

- **"通过"**：无未处理的 CRITICAL/HIGH 问题，或所有问题已修正/已评估
- **"有条件通过"**：存在已评估并记录处理方案的 HIGH 问题，无未处理的 CRITICAL 问题
- **"不通过"**：存在未处理的 CRITICAL 问题，或存在未评估的 HIGH 问题

### 修正工作量分级

| 级别 | 范围 | agent 配置 |
|------|------|-----------|
| 少量修正 | < 5 处，单点调整 | 1 个对应角色 agent |
| 中量修正 | 5-15 处，结构性调整 | 2 个 agent 分块并行修正 + 主会话整合 |
| 大量修正 | > 15 处或接近重写 | 回退到对应产出阶段重新执行多 agent 并行发散 |

### 标准评审修正循环骨架

1. **独立评审**：并行启动 ≥2 个 reviewer，视角差异化（如 reviewer-A 评技术完整性，reviewer-B 评可演进性/风险），按模板自检清单提出 CRITICAL/HIGH/MEDIUM/LOW 分级问题
2. **修正**：按修正工作量分级选择 agent 配置执行修正
3. **复核**：修正完成后重新启动独立评审
4. **循环上限**：最多 3 次
5. **终止条件**：所有 CRITICAL 和 HIGH 问题已修正 + 所有 reviewer 均给出"通过"或"有条件通过"
6. **降级规则**：3 次循环后仍未达标 → 降级置信度并标注"待后续补充"，或等待人工决策

## Template-first 内容规范

所有正式交付文档的内容结构、章节深度、自检清单，**不在本 skill 中重复定义**，统一由以下模板规定：

| 文档类型 | 模板路径 | 说明 |
|----------|----------|------|
| 标杆分析 | `.claude/templates/ref-architecture.md` | reference/*.md |
| ADR | `.claude/templates/arch-adr.md` | adr.md |
| 系统架构总纲 | `.claude/templates/arch-system.md` | design.md |
| 子系统架构 | `.claude/templates/arch-subsystem.md` | `<subsystem>/design.md` |

文档产出前必须先读模板，缺失任何 `mandatory-sections` 视为不达标。

### 阶段-章节映射表（design.md 填充范围控制）

`design.md`（基于 `arch-system.md` 模板）的章节填充必须按阶段严格控制，禁止跨阶段越界填充。每次写入前必须执行「阶段越界检查清单」。

| arch-system.md 章节 | 第 0 阶段 | 第 1 阶段 | 第 2 阶段 | 第 3 阶段 Step 2（总纲定稿） |
|---------------------|----------|----------|----------|---------------------------|
| 设计目标（全部子章节） | ✅ 填充 | ⏸ 占位 | ⏸ 占位 | ✅ 可回溯修正 |
| 设计原则 | ❌ 禁止 | ❌ 禁止 | ✅ 填充 | ✅ 可回溯修正 |
| 子系统划分 | ❌ 禁止 | ❌ 禁止 | ✅ 初步（名称+一句话职责） | ✅ 完整填充 |
| 全局设计约束/一致性模型 | ❌ 禁止 | ❌ 禁止 | ✅ 填充 | ✅ 可回溯修正 |
| 全局设计约束/故障假设 | ❌ 禁止 | ❌ 禁止 | ⏸ 占位 | ✅ 填充 |
| 全局设计约束/性能基线 | ❌ 禁止 | ❌ 禁止 | ⏸ 占位 | ✅ 填充 |
| 全局设计约束/安全边界（可选） | ❌ 禁止 | ❌ 禁止 | ❌ 禁止 | ✅ 填充 |
| 全局设计约束/可运维性约束（可选） | ❌ 禁止 | ❌ 禁止 | ❌ 禁止 | ✅ 填充 |
| 数据流总览 | ❌ 禁止 | ❌ 禁止 | ✅ 初步（主干数据流） | ✅ 完整填充（含错误传播路径） |
| 关键接口索引 | ❌ 禁止 | ❌ 禁止 | ⏸ 占位 | ✅ 填充 |
| 技术演进路线图 | ❌ 禁止 | ❌ 禁止 | ❌ 禁止 | ✅ 填充 |
| 风险与缓解策略 | ❌ 禁止 | ❌ 禁止 | ✅ 初步（技术选型风险） | ✅ 完整填充 |

> **图例**：✅ = 允许填充；❌ = 禁止填充（保持占位状态）；⏸ = 保持占位，可记录待填充要点但不在正式章节中展开

**越界检查规则**：
- 第 0 阶段写入 `design.md` 时，仅允许填充「设计目标」章节，其余章节必须保持占位/空白状态
- 第 2 阶段写入时，仅允许填充标记为 ✅ 的章节，禁止提前填充标记为 ❌ 或 ⏸ 的章节
- 第 3 阶段 Step 2（总纲定稿）是对 `design.md` 的最终定稿，所有章节允许填充或回溯修正
- **CRITICAL 问题判定**：更新内容超出阶段允许范围 → 必须修正，阻塞推进

## 执行流程

### 启动检测（skill 入口前置动作，独立执行）

**目录归属铁律**：`docs/architecture/reference/`（本 skill）与 `docs/requirements/reference/`（`/df:define`）是两个**完全独立**的目录，禁止仅凭文件名判断文件归属。

**检测清单**：
1. 执行 `ls -la docs/architecture/reference/` 和 `ls -la docs/requirements/reference/` **两个命令**
2. 汇报每个文件时必须使用**完整绝对路径**
3. 若两个目录下存在**同名或类似名文件**，必须在汇报中明确标注归属
4. **禁止**：只检测一个目录就声称"标杆研究已完成"；将 `docs/requirements/reference/` 下的文件计入本 skill 的产出

**状态汇报格式**（强制）：
```
=== 状态汇报 ===
architecture/reference/（本 skill 产出）：
  - product-A.md（X KB）
requirements/reference/（/df:define 产出，仅参考）：
  - product-C.md（X KB）
```

启动检测完成后，进入第 0 阶段。

### 第 0 阶段：前置调研与定位确认（交互式五步引导，不可跳过）

**执行方式**：派遣 architect agent 执行交互式前置调研，按 `arch-system.md` 模板产出 `design.md` 初稿。

**核心原则**：不调研清楚背景和约束就开始标杆研究，等同于盲人摸象。必须与用户通过**多次交互式提问**逐层深入，禁止一次性抛出所有问题。

**追问策略和交互技巧详见 `architect.md`**（与 `/df:define` 第 0 阶段保持一致的逐步展开式提问）。

**准入条件**：无
**产出文件**：`docs/architecture/design.md`（初稿/WIP 状态，按 `arch-system.md` 模板写入）

**阶段越界检查清单**（按「阶段-章节映射表」第 0 阶段列执行，每次写入前必须确认）：
- [ ] 本次填充的章节是否在映射表第 0 阶段允许范围内（仅「设计目标」章节）？
- [ ] 映射表中标记为 ❌ 的章节是否仍保持占位状态？

> **轻量级复核说明**：第 0 阶段产出 `design.md` 初稿（WIP 状态），其质量把关通过以下两条路径完成：
> 1. **主会话自检**：写入前执行「阶段越界检查清单」，确认章节填充范围正确
> 2. **第 1 阶段同步复核**：第 1 阶段的 reviewer 在独立评审标杆研究时，同步检查 `design.md` 初稿的章节填充范围是否正确、禁止章节是否仍保持占位状态
>
> 若发现越界填充 → 回退修正（由 architect agent 修正，主会话搬运）。

**第 0 阶段准入检查清单**（写入 `design.md` 初稿前必须确认）：
- [ ] 设计目标已收敛到一句话描述
- [ ] 所有 Actor 类型已明确列出
- [ ] 数据量级 / 访问模式 / 流量特征中至少 2 个维度已明确
- [ ] 部署拓扑和至少 2-3 个关键约束已确认
- [ ] 至少 2-3 个"明确不做"的事项已列出
- [ ] 用户已确认定位摘要无误

**第 0 阶段置信度评分**（信息充分性评估）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 系统定位清晰度 | 30% | 能用一句话描述系统场景、限制、目标 |
| Actor 完整性 | 20% | 所有关键 Actor 已识别，人类/系统区分明确 |
| 规模约束明确性 | 25% | 数据量级/访问模式/流量特征至少 2 个维度已量化 |
| 边界清晰度 | 15% | 至少 2-3 个"明确不做"事项已列出 |
| 用户确认度 | 10% | 用户已确认定位摘要无误 |

**进入第 1 阶段门槛：≥ 0.75**（信息收集阶段，门槛略低于正式阶段。豁免依据：第 0 阶段产出 design.md 为 WIP 状态初稿，非正式交付物，正式评审委托至第 1 阶段同步复核）
**低于门槛处理**：标记信息缺口清单，继续交互式提问补充，禁止进入第 1 阶段

**自动推进规则**：准入检查清单全部通过 + 置信度 ≥ 0.75 → 自动进入第 1 阶段；否则继续交互式提问

#### 终止条件

- 用户明确表示"只是为了练习架构设计方法"→ 使用默认假设，进入简化流程
- 用户已有详细的需求文档（如 `docs/requirements/*.md` 已存在）→ 直接读取现有文档，只补充缺失信息
- 用户表示"之前已经讨论过"→ 请求用户提供之前的调研结论摘要

---

### 第 1 阶段：标杆研究（强制，不可跳过）

**准入条件**：第 0 阶段前置调研已完成，标杆列表已与用户确认
**产出文件**：`docs/architecture/reference/<product>.md`（每个标杆一个文件，至少 2-3 个）
**模板**：`.claude/templates/ref-architecture.md`

**执行方式**：
1. 主会话读取 `ref-architecture.md` 模板，确认深度要求
2. 并行派遣 **2 个 researcher**，每个标杆从不同技术视角独立研究（researcher-A 侧重原理层与协议分析，researcher-B 侧重工程实现与部署运维视角，通过关注点区分），禁止使用同一 prompt 模板、同一分析框架
3. researcher 完成后，**主会话将内容写入** `docs/architecture/reference/<product>.md`
4. **并行启动 2 个 architect-reviewer 执行独立评审**
   - reviewer-A：评审研究深度与视角正确性（原理层→设计层→优化层三层剖析完整性、技术实现细节混入检查）
   - reviewer-B：评审参考来源可靠性与洞察质量（对我方架构的启示、trade-off 分析、可复用性）
   - 依据模板末尾自检清单逐项确认，提出 CRITICAL/HIGH/MEDIUM/LOW 分级问题
5. 主会话汇总两个 reviewer 的评审结论
6. 不达标时按修正工作量选择 agent 配置，进入**评审修正循环**

**评审修正循环**（按「通用术语」标准骨架执行）：
- **修正 agent**：researcher
- **分块并行**（中量修正）：researcher-A（原理层/协议分析）、researcher-B（工程实现/部署运维）
- **大量修正**：回退到重新派遣 researcher 执行标杆研究

**出口标准**：
- [ ] 每个标杆完成"原理层→设计层→优化层"三层剖析
- [ ] 每个标杆的调研深度通过 **2 个 architect-reviewer** 按模板自检清单独立验收
- [ ] 文档中无技术实现细节混入（纯架构技术视角）
- [ ] **2 个 architect-reviewer 均给出"通过"或"有条件通过"**（定义见「通用术语」评审结论三态）
- [ ] 所有文件已落盘并验证存在（`ls -la` 确认）
- [ ] 文档状态标记为"第 1 阶段完成"
- [ ] **CRITICAL 问题**：标杆数量 < 2、或任一标杆未完成三层剖析、或存在需求视角混入 → 必须修正，阻塞进入第 2 阶段
- [ ] **HIGH 问题**：分析深度不足、关键维度遗漏 → 默认接受修正
- [ ] **MEDIUM/LOW 问题**：记录但不阻塞推进，纳入后续迭代跟踪

**第 1 阶段置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 研究深度 | 25% | 完成原理层→设计层→优化层三层剖析 |
| 视角正确性 | 25% | 无需求视角混入，纯架构技术视角 |
| 覆盖完整性 | 20% | 至少 2-3 个标杆，每个都有适用性评估 |
| 洞察质量 | 20% | 有明确的"对我方架构的启示"和 trade-off 分析 |
| 可复用性 | 10% | 结论能直接指导第 2 阶段方案发散 |

**进入第 2 阶段门槛：≥ 0.80**

**低于门槛处理规则**：
- 置信度 < 0.70：等待人工决策，禁止进入第 2 阶段
- 置信度 0.70-0.80：明确反馈 researcher 补充分析，执行评审修正循环
- 置信度 ≥ 0.80：自动进入第 2 阶段

**自动推进规则**：
- 置信度 ≥ 0.80 且 **2 个 architect-reviewer 均给出"通过"或"有条件通过"** → **确认后推进**（向用户汇报结果摘要，建议进入第 2 阶段）
- 置信度 0.70-0.80 或存在未修正 HIGH 问题 → 执行评审修正循环，循环通过后自动推进
- 置信度 < 0.70 或存在未修正 CRITICAL 问题 → **等待人工决策**

### 第 2 阶段：方案发散（强制，分层展开）

**准入条件**：第 1 阶段标杆研究已完成，2 个 architect-reviewer 均给出"通过"或"有条件通过"，且置信度 ≥ 0.80

方案发散必须**从整体到关键技术逐层展开**，并同步迭代 `design.md`：

#### Step 1：任务解构与整体架构发散（多 agent 并行，复杂度：极高）

> **Plan 阶段内联**：第 2 阶段涉及系统级架构设计（基准复杂度 = **极高**），必须先解构再发散。在并行启动 architect agent 前，派遣 **1 个 architect agent** 执行轻量级任务解构：
> 1. **读取 `.claude/templates/arch-system.md`**，识别各章节的决策权重分布（整体架构、关键技术选型、数据流设计各占比）
> 2. 基于标杆研究洞察和 `design.md` 初稿，确认切分维度、分配视角约束、列出关键假设、识别评审视角缺口
> 
> 任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 architect agent 调度时 prompt 的组成部分。任务解构完成后立即进入整体架构发散。

- **并行启动 3 个 architect agent**，各自从不同维度出发，独立发散系统整体架构风格的候选方案
  - `architect`（数据流与通信范式视角）
  - `architect`（状态管理与一致性范式视角）
  - `architect`（性能约束倒推视角）
- 每个 agent 产出 ≥2 个候选方案，含明确的取舍分析
- 主会话整合三个 agent 的产出，选择最优结构或拼接不同方案的优点（不改写核心结论），形成统一的 decision-overall.md
- 同步更新 `design.md`：基于标杆研究新发现，回溯修正「设计目标」章节

**Step 1 出口标准**（写入 decision-overall.md 前必须确认）：
- [ ] 每个 architect agent 产出 ≥2 个候选方案
- [ ] 每个候选方案有明确的取舍分析
- [ ] 至少覆盖数据流与通信、状态管理与一致性、性能约束三个视角
- [ ] 主会话整合后无矛盾或已标记待解决冲突
- [ ] 2 个 architect-reviewer 均给出"通过"或"有条件通过"
- [ ] 所有 CRITICAL 问题已修正
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 2 阶段 Step 1 完成（整体架构发散）"

**Step 1 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 候选方案充分性 | 30% | 每个 agent 产出 ≥2 个候选方案，总计 ≥6 个 |
| 视角覆盖完整性 | 30% | 覆盖数据流与通信、状态管理与一致性、性能约束三个视角 |
| 取舍分析深度 | 25% | 每个候选方案有明确的 trade-off 分析和量化推导 |
| 整合质量 | 15% | 主会话整合后无矛盾或冲突已标记待解决 |

**进入门槛：≥ 0.80**

**低于门槛处理规则**：
- 置信度 0.70-0.79 → 执行评审修正循环
- 置信度 < 0.70 → 等待人工决策，禁止进入 Step 2

**自动推进规则**：
- 出口标准全部通过 + 2 个 reviewer "通过"或"有条件通过" + 置信度 ≥ 0.80 → 自动进入 Step 2
- 置信度 0.70-0.79 → 执行评审修正循环，循环通过后自动推进
- 置信度 < 0.70 或存在未修正 CRITICAL 问题 → 等待人工决策

**Step 1 评审修正循环**（decision-overall.md 产出后执行，按「通用术语」标准骨架）：
- **reviewer 配置**：并行启动 2 个 architect-reviewer
  - reviewer-A（架构完整性与技术假设）
  - reviewer-B（可演进性与风险识别）
- **修正 agent**：architect；中量修正分块：architect-A（数据流/状态管理）、architect-B（性能约束/可演进性）
- **大量修正**：回退到重新执行 Step 1 多 architect 并行发散
- **降级标注**：有条件通过（允许进入 Step 2，后续补充）

> **自我认证禁止规则（强制）**:
> - 主会话**只负责调度、验收、写入文件**，**不负责产出方案内容**
> - 主会话**禁止扮演 researcher 或 architect**直接产出决策文档内容
> - 若 researcher/architect agent 未执行，则该维度**视为未完成**，不得进入 Step 2

> **Step 依赖关系说明**：
> - Step 1（任务解构 + 整体架构发散）内先解构再发散，解构完成后立即进入发散
> - Step 2（维度确认）必须在 Step 1 完成后执行
> - Step 3（调研）与 Step 4（发散）按维度串行执行：每个维度先 Step 3 再 Step 4
> - Step 5（同步迭代 design.md）与 Step 3-4（调研+发散）无依赖关系，**可以并行**
> - Step 6（执行回溯检查）必须在 Step 3-5 全部完成后执行
> - Step 7（一致性检查）必须在 Step 6 完成后执行

**Step 2：维度确认**

- **派遣 1 个 architect agent** 读取 `decision-overall.md`，识别并分类关键技术维度，说明每个维度为什么需要独立探索
- 主会话仅搬运 architect 产出，在 `decision-overall.md`「关键技术维度识别」章节末尾追加确认标记
- 说明每个维度为什么需要独立探索
- 确认后在 `decision-overall.md`「关键技术维度识别」章节末尾追加确认标记

**Step 2 出口标准**：
- [ ] 关键技术维度已列出，每个维度有独立探索理由
- [ ] 维度与整体架构方向有关联性说明
- [ ] 确认标记已追加到 `decision-overall.md`
- [ ] 无维度遗漏（所有 Step 1 识别的关键技术方向均已覆盖）

**Step 2 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 维度完整性 | 40% | 所有关键技术方向已识别为独立维度，无遗漏 |
| 关联清晰度 | 35% | 每个维度与整体架构的关联性已说明 |
| 探索必要性 | 25% | 每个维度的独立探索理由充分 |

**进入门槛：≥ 0.80**

**评审说明**：Step 2 产出为在 `decision-overall.md` 已有章节末尾追加确认标记，非独立可交付物，其质量在 Step 7 一致性检查中自然覆盖。若 Step 7 发现维度划分问题 → 按 Step 7 修正配置处理。

**Step 3：并行 researcher 调研**
- 每个维度独立派遣 **2 个 researcher agent** 做深度调研，**必须从不同的技术视角出发**（researcher 侧重原理层与协议分析、researcher 侧重工程实现与部署运维视角，通过关注点区分），禁止多个 researcher 使用同一 prompt 模板、同一分析框架
- researcher 完成后，主会话将调研内容写入 `docs/architecture/decisions/decision-<维度>.md`（"标杆参考"章节）
- **此步骤禁止跳过——每个维度必须有 researcher agent 执行记录，主会话不得自行研究并写入**

**Step 3 出口标准**（每个维度）：
- [ ] researcher agent 执行记录完整（原理层视角 researcher + 工程实现视角 researcher）
- [ ] 调研内容覆盖原理层分析（协议/算法机制）
- [ ] 调研内容覆盖工程实现视角（部署/运维/性能数据）
- [ ] 无主观臆断，关键结论有引用来源
- [ ] 调研已写入 `decision-<维度>.md`"标杆参考"章节
- [ ] 文件已验证存在（`ls`/`wc` 确认）

**Step 3 置信度评分**（按维度）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 原理层覆盖度 | 40% | 协议/算法机制分析完整，有技术引用来源 |
| 工程实现视角 | 40% | 部署/运维/性能数据分析完整 |
| 引用可靠性 | 20% | 关键结论有明确引用，无主观臆断 |

**进入门槛：≥ 0.80**

**低于门槛处理规则**：
- 置信度 < 0.80 → 补充调研后重新评分，禁止进入 Step 4

**评审说明**：Step 3 产出（调研内容）为 Step 4 候选方案设计的输入，非独立可交付物。其质量在 Step 4 评审修正循环中覆盖（reviewer 同步评审 researcher 调研深度：原理层覆盖度、工程实现视角完整性、引用可靠性）。若 Step 4 评审发现调研内容不足以支撑方案设计（如无引用来源、原理层分析不足、工程视角严重缺失）→ 回退到 Step 3 补充调研后重新执行 Step 4。
  - 调研内容存在事实性错误
- **回退后 agent 配置**：重新派遣 researcher agent 补充调研，主会话搬运写入

**自动推进规则**：
- 所有维度出口标准通过 + 各维度置信度 ≥ 0.80 → 自动进入 Step 4
- 任一维度置信度 < 0.80 → 等待人工决策

**Step 4：并行 architect 发散**

> **Plan 阶段内联**：第 2 阶段 Step 4 为高复杂度（单一技术域内多方案对比与选型），必须先解构再发散。在并行启动 architect agent 前，主会话基于 Step 2 确认的维度列表和 Step 3 researcher 调研结果，执行轻量级任务解构：确认每个维度的切分粒度、分配 architect 视角约束（技术实现 vs 运维部署）、列出关键技术假设、识别评审视角缺口。任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 architect agent 调度时 prompt 的组成部分。任务解构完成后立即进入维度方案发散。

- 每个维度独立派遣 **2 个 architect agent**，基于 researcher 调研结果独立设计候选方案
  - `architect`（技术实现与性能优化视角）
  - `architect`（运维部署与可演进性视角）
- 每个 architect 产出 ≥2 个候选方案，主会话整合后确保该维度总计 ≥3 个候选方案
- 主会话将方案内容写入 `decision-<维度>.md`（"候选方案"及之后章节）
- **禁止单个 architect agent 串行产出多个维度文档**

**Step 4 出口标准**（每个维度写入前必须确认）：
- [ ] 该维度有 ≥3 个候选方案（2 个 architect 整合产出）
- [ ] 每个候选方案有明确的取舍分析
- [ ] 候选方案基于 Step 3 researcher 调研结果（非凭空想象）
- [ ] 有量化对比矩阵（非纯定性描述）
- [ ] 已识别待验证假设
- [ ] 已定义风险矩阵
- [ ] 2 个 architect-reviewer 均给出"通过"或"有条件通过"
- [ ] 所有 CRITICAL 问题已修正
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 2 阶段 Step 4 完成（维度方案已发散）"

**Step 4 置信度评分**（按维度）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 候选方案充分性 | 30% | 该维度 ≥3 个候选方案，覆盖核心技术选型空间 |
| 量化对比矩阵质量 | 30% | 有明确的取舍分析和量化推导，非纯定性描述 |
| 待验证假设完整性 | 20% | 所有关键技术假设已显式列出 |
| 风险矩阵覆盖度 | 20% | 每个候选方案有明确的风险识别和缓解策略 |

**进入门槛：≥ 0.80**

**低于门槛处理规则**：
- 置信度 0.70-0.79 → 执行评审修正循环
- 置信度 < 0.70 → 等待人工决策

**自动推进规则**：
- 所有维度出口标准通过 + 各维度 2 个 reviewer "通过"或"有条件通过" + 各维度置信度 ≥ 0.80 → 自动进入 Step 5
- 任一维度置信度 0.70-0.79 → 执行该维度评审修正循环，循环通过后自动推进
- 任一维度置信度 < 0.70 → 等待人工决策

**Step 4 评审修正循环**（每个维度 decision-<维度>.md 产出后执行，按「通用术语」标准骨架）：
- **reviewer 配置**：并行启动 2 个 architect-reviewer
  - reviewer-A（技术完整性与方案对比）
  - reviewer-B（与整体架构一致性/可演进性）
  - **调研内容质量独立评审**（Step 3 委托评审的回退覆盖）：独立评审 researcher 调研深度是否充分支撑候选方案设计，包括原理层覆盖度、工程实现视角完整性、引用可靠性；若调研内容不足以支撑方案 → 回退到 Step 3 补充调研
- **修正 agent**：architect
- **大量修正**：回退到重新执行 Step 3-4 的 researcher 调研和 architect 发散
- **降级标注**：有条件通过（允许进入 Step 5，后续补充）

**Step 5：同步迭代 design.md**
- **派遣 architect agent** 按阶段-章节映射表的第 2 阶段允许范围更新 `design.md`
- 主会话仅搬运 agent 产出，不自行撰写内容

**Step 5 出口标准**：
- [ ] 更新范围仅限于阶段-章节映射表第 2 阶段允许范围内
- [ ] 与 decision-overall.md 及各维度决策文档无冲突
- [ ] 主会话仅搬运 agent 产出，未自行撰写内容
- [ ] 已执行阶段越界检查
- [ ] **CRITICAL**：更新内容超出阶段允许范围 → 必须修正，阻塞进入 Step 6
- [ ] **HIGH**：与决策文档存在冲突 → 默认接受修正
- [ ] 文档已验证存在（`ls`/`wc` 确认）

**Step 5 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 更新范围正确性 | 40% | 更新内容在阶段-章节映射表第 2 阶段允许范围内 |
| 与决策文档一致性 | 40% | 与 decision-overall.md 及各维度决策文档无冲突 |
| 阶段越界合规性 | 20% | 未填充禁止章节，越界检查已执行 |

**进入门槛：≥ 0.80**

**低于门槛处理规则**：
- 置信度 0.70-0.79 → 重新检查更新范围并修正
- 置信度 < 0.70 → 等待人工决策

**评审说明**：Step 5 产出（design.md 更新）为 Step 7 一致性检查的对象，非独立可交付物。其质量在 Step 7 评审修正循环中覆盖（reviewer-B 重点评审 design.md 更新质量与阶段-章节映射合规性）。若 Step 7 发现 design.md 更新问题，按修正工作量分级处理：
  - 少量修正（范围错误、章节冲突等 < 5 处）：派遣 architect agent 修正后重新进入 Step 7
  - 中量修正（结构性问题 5-15 处）：2 个 architect agent 分块并行修正 + 主会话整合后重新进入 Step 7
  - 大量修正（> 15 处或核心章节错误）：回退到 Step 5 重新派遣 architect agent 更新 design.md

**Step 6：执行回溯检查（强制）**

**Step 6 出口标准**：
- [ ] 每个维度有 researcher agent 执行记录
- [ ] 每个维度有 architect agent 执行记录
- [ ] 主会话写入的内容严格属于"搬运 agent 产出"
- [ ] 执行声明已输出（记录各 agent 执行 ID）
- [ ] **判定标准**：3 项全部通过 → 进入 Step 7；任一缺失 → 强制回退到对应 Step

**Step 6 置信度评分**（执行记录完整性）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| researcher 执行记录 | 35% | 每个维度有明确的 researcher agent 执行记录 |
| architect 执行记录 | 35% | 每个维度有明确的 architect agent 执行记录 |
| 主会话合规性 | 30% | 主会话仅搬运，未自行撰写/评审，执行声明已输出 |

**进入门槛：≥ 0.90**（回溯检查为质量门禁，门槛从严）

**低于门槛处理规则**：
- 任一维度 < 0.90 → 强制回退到对应 Step 补充执行

**评审修正循环说明**：Step 6 为质量门禁步骤，不适用评审修正循环。回溯检查失败 → 直接强制回退到对应 Step。

**主会话写入前的强制声明**（每次写入前必须输出）：
> "执行记录：[已完成] researcher: [维度A] by [agent-id] [已完成] architect: [维度A] by [agent-id] [已完成] 写入：decision-A.md（主会话仅搬运）"

**Step 7：阶段级一致性检查与补充评审（强制）**

> **定位说明**：Step 1 和 Step 4 已分别对 decision-overall.md 和各维度 decision 文档执行了独立评审修正循环。Step 7 的核心职责是**跨文档一致性检查**——确保所有决策文档之间无冲突、design.md 更新正确、阶段级出口标准达标。

**Step 7 出口标准**：
- [ ] decision-overall.md 与各维度 decision 文档之间无冲突
- [ ] 各维度 decision 文档之间无矛盾（同一技术点在不同维度的表述一致）
- [ ] design.md 更新与所有决策文档一致
- [ ] design.md 更新范围符合阶段-章节映射表第 2 阶段允许范围，未越界填充禁止章节
- [ ] design.md 更新由 architect agent 产出、主会话仅搬运，非主会话自行撰写
- [ ] 所有文档按模板要求完成 7 项硬性指标（候选方案数、参考来源、量化对比矩阵、待验证假设、风险矩阵、初步倾向理由、置信度评估）
- [ ] 2 个 architect-reviewer 均给出"通过"或"有条件通过"
- [ ] 文档状态已标记为"第 2 阶段完成"

**Step 7 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 跨文档一致性 | 30% | decision-overall 与各维度文档无冲突，维度间无矛盾 |
| design.md 更新质量 | 25% | 更新范围正确，与阶段-章节映射表一致 |
| 7 项硬性指标完成度 | 25% | 候选方案数、参考来源、量化对比矩阵、待验证假设、风险矩阵、初步倾向、置信度评估均达标 |
| 评审回应质量 | 20% | 所有 CRITICAL/HIGH 问题有明确回应 |

**进入门槛：≥ 0.80**

**低于门槛处理规则**：
- 置信度 0.70-0.79 → 执行评审修正循环
- 置信度 < 0.70 → 等待人工决策，禁止进入第 3 阶段

**评审修正循环**（按「通用术语」标准骨架）：
- **前置一致性检查**：主会话在"整合"职责范围内检查跨文档一致性（整合多个 agent 产出时自然需要验证一致性，不额外派遣 agent）
- **reviewer 视角**：reviewer-A（跨维度一致性/隐藏耦合）、reviewer-B（design.md 更新质量/阶段-章节映射合规性）
- **修正 agent**：architect；中量修正分块：architect-A（技术架构内容）、architect-B（可演进性/风险/一致性）
- **大量修正**：回退到 Step 2-6 重新执行 researcher 调研和 architect 发散
- **特殊终止条件**：核心指标缺失 → 禁止进入第 3 阶段，回退 Step 2-6

**第 2 阶段置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 候选方案充分性 | 25% | 每个维度 ≥3 个候选方案，整体架构 ≥2 个 |
| 参考来源可靠性 | 20% | 标杆研究洞察已正确映射到方案设计 |
| 量化对比矩阵质量 | 20% | 有明确的取舍分析和量化推导 |
| 待验证假设完整性 | 15% | 所有关键技术假设已显式列出 |
| 风险矩阵覆盖度 | 15% | 每个方案有明确的风险识别和缓解策略 |
| 评审回应质量 | 5% | 所有 CRITICAL/HIGH 问题有明确回应 |

**进入第 3 阶段门槛：≥ 0.80**

**第 2 阶段出口标准**（阶段级汇总，Step 7 完成后检查）：
- [ ] `decision-overall.md` 已通过 Step 1 评审修正循环（2 个 architect-reviewer"通过"或"有条件通过"）
- [ ] 所有维度 `decision-<维度>.md` 已通过 Step 4 评审修正循环（各 2 个 architect-reviewer"通过"或"有条件通过"）
- [ ] Step 7 一致性检查通过（跨文档无冲突、design.md 更新正确）
- [ ] 所有 CRITICAL 问题已修正，无未处理的 HIGH 问题
- [ ] `design.md` 已按阶段-章节映射表更新（第 2 阶段允许范围内）
- [ ] 所有决策文档已写入并验证存在
- [ ] 文档状态标记为"第 2 阶段完成"

**自动推进规则**：
- 阶段级出口标准全部通过 + 置信度 ≥ 0.80 → **确认后推进**（向用户汇报结果摘要，等待用户确认后进入第 3 阶段）
- 置信度 0.70-0.79 → 执行评审修正循环，循环通过后确认后推进
- 存在未修正 CRITICAL 问题或置信度 < 0.70 → 等待人工决策

### 第 3 阶段：评估收敛与文档定稿（强制，逐维度收敛）

**准入条件**：所有决策文档通过第 2 阶段 Step 1/4 独立评审及 Step 7 一致性检查

**收敛顺序**：先 ADR 决策 → 再系统总纲 → 最后子系统文档

**自动化原则**：阶段内各 Step **自动推进**（产出→独立评审→评审修正循环全自动串联）；阶段之间/交付件闭环后 **确认后推进**（每阶段/交付件完成后汇报，等待用户确认后再进入下一阶段）

#### Step 1：ADR 收敛（全自动，评审修正循环）

> **Plan 阶段内联**：第 3 阶段 Step 1 为极高复杂度（涉及不可逆决策和一致性关键路径），必须先解构再发散。在并行启动 architect agent 前，派遣 **1 个 architect agent** 执行轻量级任务解构：
> 1. **读取 `.claude/templates/arch-adr.md`**，识别各章节的决策权重分布（问题陈述、方案对比、决策依据各占比）
> 2. 基于第 2 阶段决策文档，确认 ADR 切分维度（按技术域/子系统拆分）、分配 3 个 architect 视角约束（技术可行性/性能、可维护性/可观测性/演进路径、安全边界/合规/运维成本）、列出关键假设、识别评审视角缺口
> 
> 任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 architect agent 调度时 prompt 的组成部分。任务解构完成后立即进入 ADR 收敛。

> **主会话角色铁律**：主会话只负责调度 agent 和搬运产出写入文件，禁止直接撰写或评审 ADR 内容。

**出口标准**：
- [ ] 所有 CRITICAL 问题已修正（阻塞性问题必须修正，否则不得推进）
- [ ] 所有 HIGH 问题已评估：接受修正 / 接受延期（需记录理由）/ 拒绝（需说明原因）
- [ ] MEDIUM/LOW 问题记录但不阻塞推进，纳入后续迭代跟踪
- [ ] 2 个 architect-reviewer 均给出"通过"或"有条件通过"
- [ ] 每个 ADR 候选方案数达标（单一技术 ADR ≥3 个，复杂演进式 ADR ≥2 个候选 + 1 个被排除）
- [ ] 量化对比矩阵完整，有明确取舍分析和量化推导
- [ ] 待验证假设已显式列出
- [ ] 风险矩阵覆盖所有候选方案
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 3 阶段 Step 1 完成（ADR 已定稿）"

**评审修正循环**（按「通用术语」标准骨架，产出阶段 ≥3 agent）：
- **多 architect 并行初评**：3 个 `architect`（技术可行性/性能视角、可维护性/可观测性/演进路径视角、安全边界/合规/运维成本视角）→ 主会话整合为统一 ADR 初稿
- **reviewer 视角**：reviewer-A（技术完整性与方案对比）、reviewer-B（一致性协议选型合理性与工程落地可行性）
- **修正 agent**：architect；中量修正分块：architect-A（技术可行性/性能/量化对比）、architect-B（可维护性/安全边界/运维成本）
- **大量修正**：回退到 Step 1 重新执行多 architect 并行初评
- **质疑回应规则**：接受 / 反驳（需说明理由）/ 讨论（标注待澄清，不阻塞）

**第 3 阶段 Step 1 置信度评分**（ADR 涉及不可逆决策和一致性关键路径）：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 方案充分性 | 25% | 候选方案数量达标（单一技术 ≥3，复杂演进式 ≥2+1），覆盖空间完整 |
| 量化推导质量 | 25% | 对比矩阵有量化指标和明确取舍分析，非纯定性描述 |
| 风险识别完整性 | 20% | 每个候选方案有明确风险及缓解策略 |
| 假设验证充分性 | 15% | 关键技术假设已显式列出，有验证路径 |
| 决策可执行性 | 15% | 团队技术栈与能力匹配，落地路径清晰 |

**门槛：≥ 0.85**（ADR 涉及不可逆决策和一致性关键路径，门槛 ≥ 0.85）

**低于门槛处理规则**：
- 置信度 < 0.70：等待人工决策，禁止产出总纲和子系统
- 置信度 0.70-0.80：仅更新决策文档，可产出总纲（标注"有条件"），禁止产出子系统
- 置信度 0.80-0.85：可产出总纲，禁止产出子系统，需在后续迭代中补充验证
- 置信度 > 0.85：可产出总纲 + 对应子系统

**自动推进规则**：
- 循环终止条件达成（所有 CRITICAL 已修正 + 双 reviewer "通过"/"有条件通过"）→ 自动进入 Step 2
- 置信度 < 0.70 或任一 reviewer "不通过" → 等待人工决策
- 3 次评审修正循环后仍未达标 → 降级置信度并标注"待后续补充"，或等待人工决策

#### Step 2：系统架构总纲定稿（全自动，评审修正循环）

> **Plan 阶段内联**：第 3 阶段 Step 2 为极高复杂度（系统级架构设计，涉及不可逆决策），必须先解构再发散。在并行启动 architect agent 前，派遣 **1 个 architect agent** 执行轻量级任务解构：
> 1. **读取 `.claude/templates/arch-system.md`**，识别各章节的决策权重分布（数据流/通信、状态管理/一致性、集成/扩展性各占比）
> 2. 基于 ADR 终稿和 `design.md` 初稿，确认总纲结构切分维度、分配 3 个 architect 视角约束、列出关键假设（如 ADR 约束已冻结）、识别评审视角缺口（如子系统边界清晰度/接口契约完整性）
> 
> 任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 architect agent 调度时 prompt 的组成部分。任务解构完成后立即进入总纲定稿。

**输入**：ADR 终稿 + `design.md` 初稿（第 0 阶段产出，经第 1-2 阶段迭代）

> **主会话角色铁律**：同 Step 1，禁止直接撰写或评审总纲内容。

**评审修正循环**（按「通用术语」标准骨架，产出阶段 ≥3 agent）：
- **多 architect 并行发散**：3 个 `architect`（数据流/通信视角、状态管理/一致性视角、集成/扩展性视角）→ 发散 ≥2 个总纲结构备选 → 主会话整合
- **reviewer 视角**：reviewer-A（子系统边界/接口契约/ADR 约束映射）、reviewer-B（数据流完整性/总纲-ADR 一致性/低置信度 ADR 处理）
- **修正 agent**：architect；中量修正分块：architect-A（子系统边界/接口契约/ADR 映射）、architect-B（数据流完整性/错误传播/低置信度 ADR 处理）
- **大量修正**：回退到 Step 1 重新发散
- **特殊终止条件**：置信度 < 0.85 或任一 reviewer "不通过" → 等待人工决策

**出口标准**：
- [ ] 所有 CRITICAL 问题已修正（阻塞性问题必须修正，否则不得推进）
- [ ] 所有 HIGH 问题已评估：接受修正 / 接受延期（需记录理由）/ 拒绝（需说明原因）
- [ ] MEDIUM/LOW 问题记录但不阻塞推进，纳入后续迭代跟踪
- [ ] 2 个 architect-reviewer 均给出"通过"或"有条件通过"
- [ ] 子系统边界清晰度达标（接口契约完整、数据流无断点）
- [ ] ADR 约束已正确映射到子系统边界
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 3 阶段 Step 2 完成（总纲已定稿）"

**第 3 阶段 Step 2 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 子系统边界清晰度 | 25% | 各子系统职责明确，无重叠或遗漏 |
| 接口契约一致性 | 25% | 跨子系统接口定义完整，与 ADR 一致 |
| 数据流完整性 | 20% | 正常路径 + 错误传播路径均已定义 |
| ADR-总纲一致性 | 20% | 所有 ADR 决策正确映射到总纲结构 |
| 可演进性 | 10% | 预留扩展点，关键接口可向后兼容 |

**门槛：≥ 0.85**（系统级架构设计，涉及不可逆决策）

**关键纪律**：`design.md` 未产出前，禁止产出任何 `<subsystem>/design.md`

#### Step 3：子系统架构定稿（每个子系统独立执行评审修正循环，全自动）

> **Plan 阶段内联**：第 3 阶段 Step 3 为高复杂度（单一子系统内多技术域交叉），必须先解构再发散。在并行启动 architect agent 前，主会话执行轻量级任务解构：
> 1. **读取 `.claude/templates/arch-subsystem.md`**，识别各章节的决策权重分布（核心功能路径、故障处理、接口契约各占比）
> 2. 基于总纲接口契约和 ADR 技术约束，确认该子系统的切分维度、分配 2 个 architect 视角约束、列出关键假设（如总纲接口已冻结）、识别评审视角缺口（如边界条件/可运维性）
> 
> 任务解构产出以结构化文本形式在对话中输出，不写入文件，作为后续 architect agent 调度时 prompt 的组成部分。任务解构完成后立即进入子系统架构定稿。

**输入**：ADR 终稿 + 系统架构总纲（`design.md`）

**双重约束**：ADR 技术约束 + 总纲接口契约。若发现冲突，优先暂停子系统设计，修正总纲或 ADR 后再继续。

**重要约束**：
- 禁止主会话一次性批量产出多个子系统文档
- 每个子系统必须独立经过"发散→质疑→修正"评审修正循环
- 只有对应维度 ADR 达到高置信度(>0.80)时，才启动该子系统
- **无依赖关系的子系统，并行启动 2 个 architect agent 同时设计**（复杂度"高"，最小并行产出 agent 数 ≥2）
  - `architect`（核心功能路径视角）
  - `architect`（故障处理视角）

**评审修正循环**（每个子系统独立执行，按「通用术语」标准骨架）：
- **多 architect 并行发散**：2 个 `architect`（核心功能路径/数据流视角、故障处理/边界条件/运维接口视角）→ 各自产出 ≥2 个候选方案 → 主会话整合
- **reviewer 视角**：reviewer-A（功能完整性/接口契约）、reviewer-B（边界条件/可运维性）
- **修正 agent**：architect；中量修正分块：architect-A（正向设计内容）、architect-B（故障处理/边界条件/与总纲 ADR 一致性）
- **大量修正**：回退到 Step 1 重新执行多 architect 并行发散
- **特殊终止条件**：与总纲/ADR 冲突 → 优先暂停该子系统，修正总纲或 ADR 后再继续

**子系统间推进规则**：无依赖关系的子系统并行启动评审修正循环；每个子系统循环终止后汇报结果；全部子系统定稿后进入第 4 阶段

**出口标准**：
- [ ] 所有 CRITICAL 问题已修正（阻塞性问题必须修正，否则不得推进）
- [ ] 所有 HIGH 问题已评估：接受修正 / 接受延期（需记录理由）/ 拒绝（需说明原因）
- [ ] MEDIUM/LOW 问题记录但不阻塞推进，纳入后续迭代跟踪
- [ ] 2 个 architect-reviewer 均给出"通过"或"有条件通过"
- [ ] 核心功能路径数据流完整定义
- [ ] 故障处理策略与边界条件覆盖
- [ ] 与总纲/ADR 无冲突
- [ ] 文档已写入并验证存在
- [ ] 文档状态标记为"第 3 阶段 Step 3 完成（子系统已定稿）"

**第 3 阶段 Step 3 置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| 功能路径完整性 | 25% | 核心数据流、状态转换、接口契约完整 |
| 故障处理覆盖度 | 25% | 故障模式、降级策略、恢复流程已定义 |
| 边界条件充分性 | 20% | 输入边界、资源边界、并发边界已覆盖 |
| 与总纲/ADR 一致性 | 20% | 不偏离总纲接口契约和 ADR 技术决策 |
| 可运维性 | 10% | 运维接口、监控点、日志规范已定义 |

**门槛：≥ 0.80**

**执行回溯检查**（每个子系统完成评审修正循环后强制执行）：
- [ ] architect 执行记录（发散 + 修正定稿）
- [ ] architect-reviewer 执行记录（独立质疑）
- [ ] 主会话是否仅搬运 agent 产出，未自行撰写/评审
- 缺失记录 → 强制回退到对应环节

**第 3 阶段 Step 3 执行回溯检查置信度评分**：

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| architect 执行记录完整性 | 35% | 发散和修正定稿均有明确的 agent 执行记录 |
| architect-reviewer 执行记录完整性 | 35% | 独立质疑有明确的 agent 执行记录 |
| 主会话合规性 | 30% | 主会话仅搬运，未自行撰写/评审，执行声明已输出 |

**进入门槛：≥ 0.90**（回溯检查为质量门禁，门槛从严）
**低于门槛处理规则**：任一维度 < 0.90 → 强制回退到对应环节补充执行

**主会话写入前的强制声明**：
> "执行记录：[已完成] architect(发散): [子系统名] by [agent-id] [已完成] reviewer(质疑): [子系统名] by [agent-id] [已完成] architect(修正): [子系统名] by [agent-id] [已完成] 写入：<subsystem>/design.md（主会话仅搬运）"

### 第 4 阶段：架构维护与扩展（维护模式）

**核心定位**：前 3 阶段完成后，架构文档已进入"可用状态"。第 4 阶段是**维护模式**——按需调用 `/df:design` 来修正、补充或调整。

**触发原因**：实现验证后发现设计缺陷 / 需求或范围变更 / 补充新标杆研究 / 整合实现中发现的不一致

**下一步判定表**（按优先级顺序检查）：

| 当前状态 | 强制下一步 | 原因 |
|---------|---------|------|
| `adr.md` 不存在 | 第 3 阶段 Step 1：ADR 收敛 | ADR 是总纲和子系统的前置 |
| `design.md` 未定稿 | 第 3 阶段 Step 2：系统总纲定稿 | 总纲是子系统的前置 |
| 子系统文档未完成评审修正循环 | 第 3 阶段 Step 3：补全子系统 | 必须经过完整评审修正循环 |
| 所有文件已定稿 | 等待用户具体指令 | 此时才进入自由指令模式 |

**禁止在第 4 阶段生成多选项列表**。非定稿状态下下一步是唯一的。

**快速通道**（小修不走完整阶段）：文档一致性修正 / 假设验证状态更新 / 单篇标杆补充

> **豁免说明**：快速通道属于维护模式单点修正，适用豁免场景。不走完整出口标准和评审修正循环，但修正后仍需至少 1 个 reviewer 快速复核。

**快速通道出口标准**：
- [ ] 修正范围仅限于声明的修改点（单文件单章节或单篇标杆）
- [ ] 修正后与关联文档无冲突
- [ ] 至少 1 个 architect-reviewer 快速复核通过（复核重点：一致性、无新冲突、无 CRITICAL/HIGH 问题；若发现 CRITICAL → 回退到对应完整阶段）
- [ ] 文档已写入并验证存在

**快速通道评审修正循环**：
- 快速复核发现问题 → 修正 → 再次快速复核
- **循环上限**：最多 2 次（快速通道仅适用于少量修正；中量及以上修正须回退到对应完整阶段）
- **终止条件**：快速复核通过（无 CRITICAL/HIGH 问题）→ 快速通道完成；发现 CRITICAL 问题或 2 次循环后仍未达标 → 回退到对应完整阶段

**回退机制**（大变更回到对应子流程）：
- 新增关键技术维度 → 回退到第 2 阶段
- 推翻已有 ADR → 回退到第 3 阶段 Step 1
- 新增子系统 → 回退到第 3 阶段 Step 2-3

## 红旗信号（自查清单）

以下情况出现时，**立即停止，回到研究阶段**：

- 🚩 标杆研究文档 < 2 个
- 🚩 候选方案只有 1 个
- 🚩 方案描述中代码细节 > 设计 rationale
- 🚩 单次对话内完成"研究→方案→文档"
- 🚩 没有明确的 trade-off 分析
- 🚩 Agent 产出内容未写入项目文件
- 🚩 向用户汇报"本阶段完成"但文件未落盘
- 🚩 主会话一次性批量产出多个子系统架构文档
- 🚩 子系统架构文档未经 architect-reviewer 质疑直接定稿
- 🚩 `design.md` 总纲未产出前就产子系统文档
- 🚩 主会话直接撰写 ADR/总纲/子系统设计文档内容（应由 architect agent 产出）
- 🚩 主会话直接评审文档（应由 architect-reviewer agent 产出）
- 🚩 主会话写入文档前未声明 agent 执行记录
- 🚩 正式文档产出前未读取对应模板
- 🚩 目录归属混淆：将 `docs/requirements/reference/` 下的文件误报为本 skill 产出
- 🚩 生成多选项列表：违反"下一步是唯一的"原则

## 与用户协作模式

**三种协作节奏统一使用**：
1. **自动推进**：阶段内产出→独立评审→评审修正循环自动执行，循环结束后汇报结果
2. **确认后推进**：每阶段/交付件完成后汇报，等待用户确认后再进入下一阶段
3. **等待人工决策**：置信度 < 0.70、信息缺失或方向选择时停止，等待用户决策

**第 0 阶段前置调研**：交互式提问理解系统背景（唯一全程需要用户参与的环节）
