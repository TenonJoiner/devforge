# 数据查询 需求规格评审

> 本评审文件由 R1/R2 多轮评审累积写入。架构视角评审基于 `.claude/templates/req-feature.md` 模板与 SKILL 自检清单。

---

## R2 评审（视角：architect-reviewer，2026-05-08）

### 评审基线

- 模板：`.claude/templates/req-feature.md`（mandatory-sections 已完整覆盖）
- 重点：架构耦合、跨 Feature 一致性、NFR 互相兼容性、故障模式系统化、依赖闭环、演进路径
- 跨域引用核验：`边云协同.md`、`边缘自治.md`、`安全合规.md`

### 架构问题清单

#### H-A-01【HIGH】Scenario 中"专用查询计划优化路径"泄漏内部子系统设计

- **维度**：架构耦合识别
- **位置**：Feature「告警评估通道」价值论证 第 242 行 第 3 项："专用查询计划优化路径：跳过通用查询优化器的成本估算阶段，直接选择索引扫描+最近分区裁剪的最短路径"
- **理由**：产品需求文档不应规定"跳过成本估算阶段""选择索引扫描+最近分区裁剪"等执行计划生成机制——这是查询引擎子系统的内部架构决策，应在 ADR/子系统 design 中讨论。需求层只需声明"告警通道在通用查询优化器满载时仍能保证 P99 < 500ms"。该描述会反向锁死实现方案，剥夺架构师在子系统设计阶段评估其他方案（如 plan cache、专用 short-path executor、混合策略）的空间。
- **修复建议**：将第 3 项重写为效果型描述："专用快路径：在通用查询规划阶段拥塞时仍保证规划阶段耗时 < 50ms（具体实现机制由子系统架构决定）"。

#### H-A-02【HIGH】"独立 SLA 承诺"与"承诺 P99 < 500ms（不受通用查询负载影响）"存在不可证伪表述

- **维度**：NFR 合理性 / 可验收性
- **位置**：Feature「告警评估通道」第 240-244 行 + 第 259 行 Scenario #4："系统写入负载达峰值 80% 且分析查询通道满载时，告警查询 P99 < 500ms"
- **理由**：(1)"不受通用查询负载影响"是绝对化表述，无法落地——任何共享存储/网络 IO 的系统都会有耦合，即使资源完全独立的进程也会因磁盘带宽、PageCache 受影响。(2) 80% 写入 + 分析通道满载只是单一负载点，未定义"超出 80%"或"分析通道极端拥塞"时的 SLA 退化曲线。架构上需要清晰的退化策略（拒绝/降级/排队），但当前承诺暗示"无论何种负载都 < 500ms"。
- **修复建议**：补充负载-延迟分段承诺表，例如"写入 ≤ 80% 峰值 + 分析满载：P99 < 500ms；写入 > 80% 时进入降级窗口，承诺退化为 P99 < 1s 并触发告警"。

#### H-A-03【HIGH】SQL/PromQL/PG Wire 三协议语义对齐缺失

- **维度**：跨 Feature 一致性
- **位置**：Feature 1（SQL）、Feature 2（PromQL）、Feature 10（PG Wire）
- **理由**：三种协议都允许查询同一份时序数据，但文档未约束以下语义对齐：(1) 时间范围语义：SQL 默认本地时区 vs PromQL UTC vs PG Wire timestamp with time zone，是否要求三者对同一查询返回一致结果？(2) 空值处理：SQL 的 NULL vs PromQL 的"无数据点"vs PG Wire NULL，是否要求语义等价？(3) 聚合差异：PromQL 的 `rate()` 与 SQL 的窗口差分在乱序数据下结果可能不同。架构上必须在需求层先声明"三协议结果在等价查询下的允许偏差"，否则子系统设计阶段会陷入"对齐到何种程度"的反复返工。
- **修复建议**：在 Feature 10 后增设跨协议语义对齐 Feature 或在「本域非功能需求」补一行"协议语义一致性"——明确三协议对同一逻辑查询的结果偏差容忍度（如时间戳一致到秒、聚合数值偏差 < 0.1%、NULL 语义等价）。

#### H-A-04【HIGH】资源隔离与缓存的交互未定义

- **维度**：跨 Feature 一致性
- **位置**：Feature 3「资源隔离」 + Feature 4「查询缓存」
- **理由**：缓存命中是否绕过资源配额？两种合理设计相互冲突：(1) 若绕过：低优先级查询可通过缓存命中"白嫖"配额，破坏隔离；(2) 若不绕过：缓存意义削弱，命中查询仍占用配额计算的"虚拟时间"。当前需求未表态。Feature 3 #1 "告警查询 P99 延迟劣化 < 20%"是否假设缓存被禁用以保证隔离纯净？这是架构上必须先明确的需求层决策。
- **修复建议**：在 Feature 3 或 Feature 4 价值论证补一段"缓存命中的资源计费策略"——明确选择哪种语义，并在 Scenario 中加一例验证（例如"低优先级缓存命中查询不消耗资源组配额，但每秒最多 N 次"）。

#### H-A-05【HIGH】快照与告警通道的实时性语义冲突

- **维度**：跨 Feature 一致性
- **位置**：Feature 9「一致性快照」 + Feature 8「告警评估通道」 第 258 行 Scenario #3："查询刚写入 2s 内的数据点，返回包含最近 2s 内写入的数据点"
- **理由**：告警通道要求"刚写入 2s 内可见"，意味着查询路径上不能有快照隔离层；而快照 Feature 提供"严格时间点一致"的快照视图。如果二者共用同一查询执行栈，则需要在路由层区分"snapshot read"与"realtime read"，但当前需求未声明这是同一路径还是分离路径。架构上影响巨大：(a) 同一路径需要双语义执行计划，复杂度高；(b) 分离路径需要两套读路径子系统。
- **修复建议**：在「跨域依赖说明」后增"读路径语义分层"小节，明确告警通道、PG Wire、SQL 默认是否走快照视图，是否支持显式 `AS OF SNAPSHOT 'xxx'` 语法。

#### H-A-06【HIGH】流式订阅与批量导出的资源竞争场景缺失

- **维度**：故障模式完整性
- **位置**：Feature 5「流式订阅」 + Feature 6「批量导出」
- **理由**：两者都是长时间持续占用资源的查询模式（流式订阅长连接持续推送，批量导出长时间扫描），故障模式矩阵中"资源耗尽"列各自只引用了内部 Scenario，未覆盖"流式订阅 1000 个长连接 + 批量导出 4 路并行"的叠加场景。架构上需要明确流式推送的内存配额是否受批量导出影响、批量导出是否会拖慢流式推送的 P99 延迟。
- **修复建议**：在两者的 Scenario 中增补一例叠加场景，或在「本域验收标准」加"持续写入 10 万点/秒 + 流式订阅 1000 连接 + 批量导出并发 4 路时，告警 P99 < 500ms 且推送 P99 < 1s"。

#### H-A-07【HIGH】NFR 互相兼容性未做交叉校验

- **维度**：NFR 合理性
- **位置**：第 388-401 行 NFR 表 + 各 Feature 成功指标
- **理由**：以下指标在同一集群是否能同时达成未论证：
  - 告警评估 P99 < 500ms（满载场景） + 批量导出 500MB/s 单节点 + 流式订阅写入到推送 P99 < 1s + 缓存新鲜度 < 5s + 快照创建 < 100ms + 并发查询 ≥ 100。
  - 例：批量导出 500MB/s 在 SSD 顺序读已逼近极限，此时 PageCache 被冲刷，告警通道还能保证 < 500ms 吗？流式推送的写入感知延迟还能保证 < 1s 吗？
  - 缺少"在所有 NFR 同时达成时的最小硬件配置"或"哪些 NFR 不能同时达成（互斥）"的声明。
- **修复建议**：在「本域非功能需求」表后追加"NFR 交叉兼容性"段，列出确认可以同时达成的指标组合，以及互斥场景下的优先级（如告警 P99 优先于批量吞吐）。

#### H-A-08【HIGH】跨时间线 JOIN 的下推语义未声明

- **维度**：架构耦合 / 演进路径
- **位置**：Feature 5（实为 Feature「跨时间线关联查询」）价值论证 第 155 行："计算下推到存储层"
- **理由**：(1) "下推到存储层"已经是子系统职责划分，需求层不应规定下推策略；(2) 下推语义影响存储层接口契约（必须支持 join hint、列裁剪、谓词下推），需要在子系统设计阶段评估，需求层只需说"3 条时间线 7 天关联 P95 < 2s"。当前表述会导致存储域设计被反向锁死。
- **修复建议**：删除"计算下推到存储层"，替换为"在不传输全量原始数据的前提下完成关联计算"——保留性能效果，去除架构机制。

#### M-A-09【MEDIUM】告警评估通道的"两个评估周期内必须完成"未定义周期长度

- **维度**：可验收性
- **位置**：第 261 行 Scenario #6："单条规则最大评估延迟不超过 2 个评估周期（30s）"
- **理由**："2 个评估周期 = 30s"暗示评估周期是 15s，但 Feature 价值论证（第 235 行）说"每 5-15 秒执行一轮"。周期长度是配置项还是固定值？若可配置，"2 个周期"的承诺在 5s 周期下是 10s，在 15s 周期下是 30s，验收标准随之浮动，需要明确。
- **修复建议**：声明评估周期为 Actor 提供的请求参数，最大延迟统一表达为"≤ 2 × 评估周期且 ≤ 30s"。

#### M-A-10【MEDIUM】PG Wire 不支持的 SQL 语法清单不闭合

- **维度**：演进路径
- **位置**：Feature 10 第 319 行 Scenario #4："执行不支持的 SQL 语法（JOIN、子查询、DDL）"
- **理由**：当前列举"JOIN、子查询、DDL"为不支持，但 Feature 5 明确支持跨时间线 JOIN。两者矛盾：是 PG Wire 子集刻意不暴露 JOIN 还是 JOIN 仅限私有 API？演进路径上若未来需要在 PG Wire 暴露 JOIN，旧客户端语义如何处理？
- **修复建议**：在 Feature 10 增补"PG Wire 当前 SQL 子集与 Feature 1 SQL 引擎全集的差异说明"，并在待澄清问题中加入"PG Wire 是否计划达到 SQL 引擎全集"。

#### M-A-11【MEDIUM】缓存节点宕机的 60s 命中率回升 SLA 缺乏依据

- **维度**：可验收性
- **位置**：第 145 行 Scenario #7："缓存节点恢复后 60s 内命中率回升至 50%+"
- **理由**：60s 与 50% 的具体阈值缺乏建模——回升速度取决于查询模式（仪表盘刷新周期 15-30s）和缓存预热策略。在低 QPS 场景下 60s 内可能根本没有 50% 的查询发生过。
- **修复建议**：将"60s 内"改为"在每秒 ≥ N 次查询持续负载下，缓存节点恢复后 60s 内命中率回升至 50%+"。

#### M-A-12【MEDIUM】"协议升级"故障模式被矩阵备注 [3] 默认免除

- **维度**：演进路径
- **位置**：第 351 行备注 [3]："查询协议版本兼容性由各自协议 Feature 的兼容性保证（PromQL/SQL 语义版本化），不单独设计升级 Scenario"
- **理由**：备注称"PromQL/SQL 语义版本化"在文档中没有任何 Feature/Scenario 体现，没有 SQL/PromQL 的版本协商或语义版本号声明，备注 [3] 实际上是空头支票。只有 PG Wire 有升级 Scenario（#10、#11），其他两协议升级时如何保证兼容性是真空。
- **修复建议**：补充"SQL/PromQL 函数集语义版本"NFR，或承认这是已知缺口（加入待澄清问题列表）。

#### L-A-13【LOW】跨时间线 JOIN 的"查询计划缓存被淘汰"是子系统术语

- **维度**：架构耦合
- **位置**：第 173 行 Scenario #9："关联查询执行期间查询计划缓存被淘汰，系统重新生成计划后继续执行"
- **理由**:"查询计划缓存"是查询引擎子系统的内部机制，需求层不应直接引用。该 Scenario 的实际意图是验证"重规划不影响正在执行的查询"，应该用效果性描述。
- **修复建议**：改写为"长时间执行的关联查询在中途遇到内部状态刷新（如配置变更、热数据迁移）时不中断"。

### 架构问题统计

| 级别 | 数量 | 分值 | 小计 |
|------|------|------|------|
| CRITICAL | 0 | 10 | 0 |
| HIGH | 8 (H-A-01..08) | 3 | 24 |
| MEDIUM | 4 (M-A-09..12) | 1 | 4 |
| LOW | 1 (L-A-13) | 0.1 | 0.1 |
| **合计** | **13** | — | **28.1** |

### 隐性假设识别（≥2）

1. **隐含假设：缓存路径与资源隔离正交**——文档默认两者独立设计，未讨论缓存命中是否计入资源配额。该假设被证伪（任一选择都需要在 Feature 间协调）。
2. **隐含假设：所有 NFR 在单一硬件配置下可同时达成**——批量导出 500MB/s + 告警 P99 500ms + 流式 P99 1s + 缓存新鲜度 5s 之间没有冲突分析。
3. **隐含假设：三协议（SQL/PromQL/PG Wire）共用同一查询执行栈**——价值论证暗示通过统一引擎实现，但快照语义、告警通道差异化执行计划暗示需要分层路径，假设未被显式验证。

### 遗漏 Trade-off 补充

1. **缓存新鲜度 vs 命中率 vs 资源隔离**：缩短缓存失效窗口（< 5s）会降低命中率（80%→？）；当前文档单方面承诺两者都达标，未做 trade-off 量化。
2. **告警通道独立性 vs 集群资源利用率**：专用通道+独立 SLA 承诺需要预留资源容量，集群整体利用率必然下降。需求层未声明能接受多大的预留浪费。

### 置信度评估调整

R1 通过后 architect 隐含的"已可冻结"置信度建议从 0.80 降至 **0.70**——理由：跨 Feature 协同问题（H-A-03 至 H-A-07）尚未在需求层处理，进入子系统设计阶段会触发反复返工。

### 通过门槛核算

- 缺陷密度 = 28.1 / 10 Feature = **2.81 分/Feature**（门槛 1.5）
- CRITICAL = 0（门槛 0）
- **结论：FAIL**（缺陷密度超阈值，需进入下一轮修正）

### 修正方向（按优先级）

1. **必修**（H-A-01、H-A-08、L-A-13）：剔除内部子系统术语（执行计划、计划缓存、计算下推）。
2. **必修**（H-A-03、H-A-04、H-A-05）：补充三协议语义对齐、缓存与资源隔离交互、读路径语义分层三项跨 Feature 协调声明。
3. **必修**（H-A-07）：增加 NFR 交叉兼容性段，声明指标互斥关系。
4. **建议**（H-A-02、H-A-06）：分段化 SLA 承诺、补叠加场景。
5. **可推迟**（M-A-09..12、L-A-13）：可在下一版细化。

| 轮次 | 日期 | 缺陷密度 | CRITICAL | HIGH | MEDIUM | LOW | 结论 |
|------|------|---------|----------|------|--------|-----|------|
| R2（architect-reviewer） | 2026-05-08 | 2.81/Feature | 0 | 8 | 4 | 1 | ❌ FAIL |

---

## R3 评审（视角：architect-reviewer，2026-05-09）

### 评审基线

- **模板**：`.claude/templates/req-feature.md`（mandatory-sections 已完整覆盖）
- **重点维度**：架构耦合、跨 Feature 一致性、NFR 互相兼容性、故障模式系统化、依赖闭环、演进路径
- **跨域引用核验**：`边云协同.md`（存在，PASS）、`边缘自治.md`（存在，PASS）、`安全合规.md`（存在，PASS）
- **复核任务**：验证 R2 提出的 13 个问题（H-A-01..08、M-A-09..12、L-A-13）的修正质量

---

### R2 修正验证结果

| 编号 | 级别 | 修正状态 | 验证结论 |
|------|------|---------|---------|
| H-A-01 | HIGH | 已修复 | 告警通道"专用查询计划优化路径"已改为效果型描述"专用快路径：规划阶段拥塞时仍保证规划耗时 < 50ms，查询执行选择最短数据访问路径（具体实现机制由子系统架构决定）"。内部子系统术语已剔除，实现空间已释放。**通过**。 |
| H-A-02 | HIGH | 已修复 | SLA 已分段化：写入 <= 80% 峰值时 P99 < 500ms，> 80% 时退化为 P99 < 1s 并触发降级告警。成功指标同步更新。"不受通用查询负载影响"的绝对化表述已消除。**通过**。 |
| H-A-03 | HIGH | 已修复 | SQL（第 59 行）、PromQL（第 89 行）、PG Wire（隐含通过 Feature 1 引用）三协议均增加了"协议语义一致性声明"，明确时间戳一致到秒级、聚合数值偏差 < 0.1%、NULL 与"无数据点"语义等价。PromQL 额外声明 rate() 与 SQL 窗口差分在乱序数据下偏差 < 0.1%。**通过**。 |
| H-A-04 | HIGH | 已修复 | Feature 3 价值论证第 113 行补充了缓存命中资源计费策略："缓存命中不消耗资源组 CPU/内存配额，但每个资源组对缓存命中查询设置每秒上限（防止缓存被滥用为无限容量通道），超限后回退到实时计算并正常计费"。新增 Scenario #7（第 129 行）验证超限回退行为。**通过**。 |
| H-A-05 | HIGH | 已修复 | 跨域依赖说明后新增"读路径语义分层"小节（第 36-41 行），明确各 Feature 默认读路径：告警通道/PG Wire/SQL/PromQL 默认走实时读路径、支持显式 `AS OF SNAPSHOT` 切换；批量导出必须基于快照；流式订阅不经过快照层。快照与告警通道的实时性语义冲突已消解。**通过**。 |
| H-A-06 | HIGH | 已修复 | 流式订阅新增 Scenario #7（第 213 行）："流式订阅与批量导出并发执行：1000 个流式订阅长连接 + 4 路批量导出并行扫描"。批量导出新增 Scenario #7（第 241 行）同场景交叉验证。叠加场景已覆盖。**通过**。 |
| H-A-07 | HIGH | 已修复 | NFR 表后新增"NFR 交叉兼容性声明"小节（第 420-434 行），列出 4 组确认可同时达成的指标组合及条件，声明 2 个互斥场景与降级策略，并给出优先级排序（告警 P99 > 批量导出吞吐 > 流式推送延迟 > 缓存命中率）。**通过**。 |
| H-A-08 | HIGH | 已修复 | 跨时间线 JOIN 价值论证第 167 行删除"计算下推到存储层"，改为"在不传输全量原始数据的前提下完成关联计算"。效果保留，架构机制去除。**通过**。 |
| M-A-09 | MEDIUM | 已修复 | 告警 Scenario #6（第 275 行）明确"评估周期为 Actor 配置参数，范围 5-15s"，最大延迟统一为"不超过 2 个评估周期且 <= 30s"。验收标准不再浮动。**通过**。 |
| M-A-10 | MEDIUM | 已修复 | PG Wire Scenario #4（第 333 行）移除 JOIN 示例（仅保留子查询、DDL），新增 Scenario #6（第 335 行）验证 ORM JOIN 通过 PG Wire 路由到 SQL 引擎执行。待澄清问题 #4（第 458 行）重写为"PG Wire 当前 SQL 子集与 SQL 引擎全集的差异说明"。演进路径已澄清。**通过**。 |
| M-A-11 | MEDIUM | 已修复 | 缓存 Scenario #7（第 157 行）增加负载前提"在每秒 >= 10 次查询持续负载下，缓存节点恢复后 60s 内命中率回升至 50%+"，排除了低 QPS 场景下的不可证伪问题。**通过**。 |
| M-A-12 | MEDIUM | 已修复 | 故障模式矩阵备注 [3]（第 366 行）更新为承认"SQL/PromQL 函数集语义版本化机制待定义"，并新增待澄清问题 #5（第 459 行）"SQL/PromQL 函数集语义版本化"。空头支票已转为明确待办。**通过**。 |
| L-A-13 | LOW | 已修复 | 跨时间线 JOIN Scenario #9（第 185 行）"查询计划缓存被淘汰"改为"内部状态刷新（如配置变更、热数据迁移）时不中断"。子系统术语已剔除。**通过**。 |

**R2 修正质量总评**：13/13 全部修复，修复质量良好。无"部分修复"或"引入新问题"项。

---

### R3 新发现问题

#### H-R3-01【HIGH】NFR 交叉兼容性声明中的"标准硬件配置"缺乏存储 I/O 规格

- **维度**：NFR 互相兼容性
- **位置**：第 422 行"标准硬件配置（单节点 32 核 / 128GB 内存 / NVMe SSD）"
- **理由**：NVMe SSD 的 I/O 性能差异巨大——消费级 PCIe 3.0 x4（~3GB/s 顺序读）与企业级 PCIe 4.0 x4（~7GB/s）相差 2 倍以上。批量导出 500MB/s + 告警 P99 500ms 的组合是否能在消费级 NVMe 上达成？存储 I/O 规格是 NFR 交叉兼容性的关键变量，当前缺失。
- **修复建议**：补充 NVMe 的最低 IOPS/吞吐规格，如"NVMe SSD，顺序读 >= 3GB/s，随机读 IOPS >= 300K"。

#### H-R3-02【HIGH】PG Wire Scenario #6 的 JOIN 路由语义与 Feature 10 价值论证中"查询子集"定位矛盾

- **维度**：跨 Feature 一致性
- **位置**：Feature 10 价值论证第 319 行"实现 PostgreSQL wire protocol 查询子集"+ Scenario #6 第 335 行"PG Wire 将 JOIN 路由到 SQL 引擎执行"
- **理由**：价值论证称 PG Wire 是"查询子集"，但 Scenario #6 却支持 JOIN（通过路由到 SQL 引擎）。这引发两个问题：(1) 如果 JOIN 可以路由到 SQL 引擎，那子查询、DDL 是否也可以路由？为什么只路由 JOIN？(2) 路由到 SQL 引擎后，结果通过 PG Wire 协议返回，那 PG Wire 的"子集"边界在哪里——是协议消息类型的子集，还是 SQL 语法的子集？当前定义模糊，会导致实现阶段边界争议。
- **修复建议**：在 Feature 10 价值论证中明确 PG Wire 的"子集"定义维度——是"协议消息类型子集"（只实现必要消息类型）而非"SQL 语法子集"，SQL 语法能力通过路由到 SQL 引擎获得。或反之，若确实是 SQL 语法子集，则需解释为什么 JOIN 被例外允许路由。

#### M-R3-03【MEDIUM】读路径语义分层中 PromQL 的"实时读路径"与 rate() 的窗口语义存在隐含冲突

- **维度**：跨 Feature 一致性
- **位置**：读路径语义分层第 39 行"PromQL 查询：默认走实时读路径；范围查询的瞬时向量评估基于查询发起时刻的实时数据状态"
- **理由**：rate() 函数需要至少两个数据点计算差分，如果查询发起时刻的"实时数据状态"中某时间线只有 1 个数据点（刚写入），rate() 会返回空。这与"实时读路径"的直觉（看到最新数据）冲突——用户可能期望 rate() 能基于最新点给出某种近似值。需求层未声明 rate() 在数据不足时的行为，而这是 PromQL 兼容性的关键语义。
- **修复建议**：在 PromQL Feature 的协议语义一致性声明中补充 rate() 等窗口函数在数据点不足时的行为（如"数据点少于 2 个时 rate() 返回空 vector，与 Prometheus 参考实现一致"）。

#### M-R3-04【MEDIUM】NFR 交叉兼容性声明的互斥场景缺少缓存维度

- **维度**：NFR 互相兼容性
- **位置**：第 431-433 行互斥场景
- **理由**：两个互斥场景覆盖了"批量导出+流式+告警"和"写入峰值+分析+批量导出"，但未覆盖缓存相关的资源竞争。例如：缓存预热期（节点恢复后 60s 内命中率从 0 回升到 50%）大量查询回退到实时计算，此时告警 P99 是否还能保证 < 500ms？缓存 LRU 淘汰触发大量实时重算时，对批量导出的吞吐影响如何？
- **修复建议**：在互斥场景中增加一条"缓存大规模失效/冷启动期间：告警 P99 退化为 < 1s，批量导出吞吐不受影响（因批量导出不走缓存）"。

#### M-R3-05【MEDIUM】告警通道的"补评估"机制与流式订阅的"断线补推"在语义上未区分

- **维度**：演进路径 / 跨 Feature 一致性
- **位置**：告警通道 Scenario #8（第 278 行）"恢复后系统自动补评估被跳过的周期"+ 流式订阅 Scenario #4（第 210 行）"从断点补推"
- **理由**：两个 Feature 都有"补"机制，但补的语义不同：流式订阅补的是"数据点"（精确到每条记录），告警通道补的是"评估周期"（聚合后的窗口值）。如果告警通道补评估时依赖的聚合数据已被流式订阅消费者处理过，两者在审计/溯源场景下如何对齐？需求层未声明这种跨 Feature 的"补"语义关系。
- **修复建议**：在"读路径语义分层"或跨域依赖说明中增加一段"补机制语义区分"：流式订阅补原始数据点，告警通道补聚合评估结果，两者独立不互斥，审计日志中需标注补触发来源。

#### L-R3-06【LOW】自检清单第 446 行"实际覆盖 7 个维度"的计数与 NFR 表不一致

- **维度**：文档一致性
- **位置**：自检清单第 446 行
- **理由**：NFR 表实际列出 7 个维度（性能、可用性、一致性、安全性、可观测性、扩展性、兼容性），自检清单称"实际覆盖 7 个维度"，但 NFR 表中有 13 行需求项，"可用性"维度只有 1 行（查询服务可用性 >= 99.9%）。计数正确但"维度"与"需求项"的表述可能让读者困惑——7 个维度对应 13 个需求项，自检清单只说"7 个维度"未说"13 项需求"，虽不算错误但信息不完整。
- **修复建议**：将"实际覆盖 7 个维度"改为"实际覆盖 7 个维度、13 项需求项"，与 NFR 表对齐。

#### L-R3-07【LOW】待澄清问题 #3 的"计划澄清时间"为"第 2 轮迭代前"，但其他 HIGH 优先级问题为"第 1 轮迭代前"

- **维度**：文档一致性
- **位置**：待澄清问题表第 457 行
- **理由**：问题 #3 优先级为 MEDIUM，计划澄清时间为"第 2 轮迭代前"，与问题 #1、#2（HIGH，第 1 轮迭代前）形成合理梯度。但问题 #4（MEDIUM，第 1 轮迭代前）和问题 #5（MEDIUM，第 2 轮迭代前）同为 MEDIUM 却有不同计划时间，虽可解释（#4 阻塞 PG Wire 开发，#5 可延后），但文档未说明差异原因。此问题极轻微，不影响通过。
- **修复建议**：可在待澄清问题表中增加一列"延后原因"，或在问题描述中简要说明。非必须。

---

### 隐性假设识别（R3）

1. **隐含假设：PG Wire 的 JOIN 路由是通用模式**——Scenario #6 展示 JOIN 可路由到 SQL 引擎，但未声明子查询、DDL 等其他"不支持的语法"是否也可路由。若不可路由，需明确路由的判定条件。
2. **隐含假设：缓存冷启动期间仅影响仪表盘查询，不影响告警通道**——文档未声明告警查询是否走缓存（读路径语义分层中告警通道未提及缓存），若告警查询也走缓存则冷启动期 SLA 承诺不成立。

### 遗漏 Trade-off 补充（R3）

1. **PG Wire "子集"定位 vs 生态完整性**：定位为"子集"意味着部分 SQL 语法返回错误，但 ORM 框架（如 SQLAlchemy）在初始化时可能执行大量元数据查询（如 `information_schema` 查询），若这些查询不被支持则 ORM 无法正常工作。当前 Scenario 未覆盖 ORM 初始化阶段的兼容性。

### 置信度评估调整

R2 修正后，architect 隐含的"已可冻结"置信度从 R2 的 0.70 回升至 **0.85**——理由：R2 提出的 8 个 HIGH 问题全部高质量修复，跨 Feature 协同问题（三协议对齐、缓存隔离、读路径分层、NFR 兼容性）已在需求层明确。剩余 R3 新发现问题以 MEDIUM/LOW 为主，不阻塞子系统设计启动。

### 通过门槛核算

- R3 新发现问题：HIGH × 2 + MEDIUM × 3 + LOW × 2 = 6 + 3 + 0.2 = **9.2 分**
- 缺陷密度 = 9.2 / 10 Feature = **0.92 分/Feature**（门槛 1.5）
- CRITICAL = 0（门槛 0）
- **结论：PASS**（缺陷密度低于阈值，可进入冻结候选）

### 修正建议（按优先级）

1. **建议**（H-R3-01）：补充 NVMe I/O 规格到标准硬件配置。
2. **建议**（H-R3-02）：明确 PG Wire "子集"的定义维度（协议消息 vs SQL 语法）。
3. **可选**（M-R3-03..05）：补充 PromQL rate() 边界行为、缓存冷启动互斥场景、补机制语义区分。
4. **可选**（L-R3-06..07）：文档一致性微调。

---

| 轮次 | 日期 | 缺陷密度 | CRITICAL | HIGH | MEDIUM | LOW | 结论 |
|------|------|---------|----------|------|--------|-----|------|
| R2（architect-reviewer） | 2026-05-08 | 2.81/Feature | 0 | 8 | 4 | 1 | ❌ FAIL |
| R3（architect-reviewer） | 2026-05-09 | 0.92/Feature | 0 | 2 | 3 | 2 | ✅ PASS |

---

## R3 评审（视角：product-reviewer，2026-05-09）

### 评审基线

- **模板**：`.claude/templates/req-feature.md`
- **自检清单**（逐条检查）：
  1. Feature 价值论证完整（痛点→不足→价值→成功指标）
  2. 每个 Feature 的 Scenario 清单 ≥ 5 个（正常路径 + 故障/边界）
  3. 故障模式覆盖矩阵中每个 Feature 至少 5 个 ✅
  4. 每个验收标准已量化且标注可测试性等级
  5. 本域 Feature 在 product-spec.md 中的依赖关系已确认
  6. 非功能需求至少覆盖 3 个维度，每条有量化目标值
  7. 已知待澄清问题已记录并分配跟进计划
- **重点维度**：Scenario 完整性、验收标准可验收性、Feature 价值论证、Actor 角色视角覆盖、跨 Feature 依赖合理性

---

### R2 修正验证结果

| R2 编号 | 级别 | 验证状态 | 验证说明 |
|---------|------|---------|---------|
| H-A-01 | HIGH | ✅ 已修复 | "专用快路径"改为效果型描述，明确"具体实现机制由子系统架构决定" |
| H-A-02 | HIGH | ✅ 已修复 | 分段 SLA 清晰：≤80% 峰值 P99<500ms，>80% 退化为 P99<1s 并触发降级告警 |
| H-A-03 | HIGH | ✅ 已修复 | Feature 1/2 各增"协议语义一致性声明"，Feature 10 依赖 Feature 1 声明（可接受） |
| H-A-04 | HIGH | ✅ 已修复 | Feature 3 补充缓存命中计费策略，新增 Scenario #7 验证 |
| H-A-05 | HIGH | ✅ 已修复 | 新增"读路径语义分层"小节，各 Feature 默认读路径和快照支持清晰 |
| H-A-06 | HIGH | ✅ 已修复 | Feature 5/6 各增叠加场景 Scenario #7，验收标准量化 |
| H-A-07 | HIGH | ✅ 已修复 | 新增"NFR 交叉兼容性声明"，含 4 组可达成组合、2 组互斥降级策略、优先级排序 |
| H-A-08 | HIGH | ✅ 已修复 | "计算下推到存储层"改为"在不传输全量原始数据的前提下完成关联计算" |
| M-A-09 | MEDIUM | ✅ 已修复 | 评估周期明确为 Actor 配置参数（5-15s），最大延迟"≤2×周期且≤30s" |
| M-A-10 | MEDIUM | ✅ 已修复 | Scenario #4 移除 JOIN，新增 #6 验证 ORM JOIN 路由；待澄清问题 #4 重写 |
| M-A-11 | MEDIUM | ✅ 已修复 | 增加负载前提"每秒≥10 次查询持续负载下" |
| M-A-12 | MEDIUM | ✅ 已修复 | 备注 [3] 更新，承认 SQL/PromQL 语义版本化待定义，新增待澄清问题 #5 |
| L-A-13 | LOW | ✅ 已修复 | "查询计划缓存被淘汰"改为"内部状态刷新（如配置变更、热数据迁移）时不中断" |

**R2 修正质量总评**：13/13 全部修复，无"部分修复"或"引入新问题"项。修正质量高，原问题根因均已消除。

---

### R3 新发现问题清单（product-reviewer 视角）

#### P-R3-01【MEDIUM】Feature 8 成功指标"达标率 >= 99.9%"未定义统计周期
- **维度**：验收标准可验收性
- **位置**：Feature 8 成功指标（第 264 行）
- **理由**："端到端评估延迟 < 30s，达标率 >= 99.9%"缺少统计周期定义。99.9% 在 1 小时和 24 小时内的含义完全不同，直接影响验收可操作性。产品级需求必须明确统计窗口，否则无法写入 SLA。
- **修复建议**：补充统计周期，如"以 24 小时为统计周期，端到端评估延迟 < 30s 的达标率 >= 99.9%"

#### P-R3-02【MEDIUM】Feature 10 价值论证未充分覆盖「应用开发者」Actor 视角
- **维度**：Feature 价值论证 / Actor 视角覆盖
- **位置**：Feature 10 价值论证（第 313-319 行）
- **理由**：Feature 10 涉及数据分析师和应用开发者两个主 Actor，但"本 Feature 价值"段仅突出数据分析师视角（psql、DBeaver、Grafana 零门槛接入）。应用开发者通过 ORM 复用现有数据库驱动的价值未在价值论证中明确阐述，仅在"现有方案不足"中一句话带过。从 product-reviewer 视角，这会导致产品上市时应用开发者 GTM 材料缺少需求层支撑。
- **修复建议**：在"本 Feature 价值"中补充应用开发者视角，如"应用开发者可零适配复用 SQLAlchemy、Hibernate 等现有 ORM 和数据库驱动，无需重写数据访问层"

#### P-R3-03【LOW】「安全审计员」Actor 在本域无直接 Scenario 覆盖
- **维度**：Actor 角色视角覆盖
- **位置**：Actors 列表（第 29 行）+ 各 Feature Scenario 清单
- **理由**：安全审计员被列为协作 Actor，但本域 10 个 Feature 的 71 个 Scenario 中没有任何一个从安全审计员视角出发验证查询审计功能。本域已声明"查询审计"NFR 和"查询权限控制"验收标准，但缺少端到端验证场景。产品级需求应确保每个声明的 Actor 至少有一个直接验证场景。
- **修复建议**：在 Feature 1 或 Feature 10 中增加一个安全审计员视角的 Scenario，如"安全审计员查询过去 24 小时所有敏感时间线的访问记录，系统在 5s 内返回完整审计日志"

#### P-R3-04【MEDIUM】Feature 6 与 Feature 8 的"灰色地带"未在 Scenario 中预留边界验证
- **维度**：跨 Feature 依赖合理性
- **位置**：待澄清问题 #3（第 457 行）
- **理由**：待澄清问题 #3 明确记录了近实时告警场景（"数据到达后 5s 内触发告警评估"）的归属模糊，但 Feature 6 和 Feature 8 的 Scenario 中均未预留此类边界案例。product-reviewer 视角下，待澄清问题一旦 resolved，需要快速补充验证场景，当前文档未做预留，可能导致澄清后遗漏验收。
- **修复建议**：在 Feature 6 或 Feature 8 中增加一个标注为"边界/待澄清"的 Scenario，如"告警规则引擎要求数据写入后 5s 内触发评估，系统通过流式订阅推送或告警通道轮询满足要求（归属待澄清问题 #3 确定后细化）"

#### P-R3-05【LOW】Feature 1 故障模式矩阵"数据损坏"列引用 #4 不匹配
- **维度**：故障模式矩阵一致性
- **位置**：故障模式矩阵 Feature 1 行（第 352 行）
- **理由**：Feature 1 的 #4 是"提交语法错误的 SQL 查询"，属于查询语法故障处理，与"数据损坏"（存储层数据损坏后的查询行为）语义不匹配。Feature 1 没有直接覆盖数据损坏的 Scenario。product-reviewer 视角下，矩阵的准确性直接影响测试覆盖率的统计。
- **修复建议**：将 Feature 1"数据损坏"列改为"—"，备注"数据损坏由存储域保障，查询层在损坏数据上返回校验和错误"；或补充一个数据损坏 Scenario。

#### P-R3-06【MEDIUM】NFR 交叉兼容性声明与 Feature 6 Scenario #7 的叠加条件存在矛盾
- **维度**：NFR 合理性 / 跨 Feature 一致性
- **位置**：NFR 交叉兼容性声明（第 432 行）+ Feature 6 Scenario #7（第 213 行）
- **理由**：Feature 6 #7 的验收标准包含"告警查询 P99 < 500ms"（非降级条件），但 NFR 互斥场景声明"批量导出 + 流式订阅 + 告警查询"组合下"告警 P99 退化为 < 1s"。两者矛盾：Feature 6 #7 暗示该叠加场景下告警可保持 < 500ms，而 NFR 声明必须降级。未明确 Feature 6 #7 是否假设了超出"标准硬件配置"的资源条件，导致产品级承诺与客户验收标准可能不一致。
- **修复建议**：在 NFR 互斥场景中注明 Feature 6 #7 的叠加场景是否属于"标准硬件配置"下的可达成组合，或明确该 Scenario 需要增强硬件配置。

#### P-R3-07【LOW】待澄清问题 #3 优先级与计划澄清时间不匹配
- **维度**：已知待澄清问题
- **位置**：待澄清问题 #3（第 457 行）
- **理由**：流式订阅与告警通道的边界问题（MEDIUM 优先级）计划第 2 轮迭代前澄清，但如果第 1 轮迭代即涉及 Feature 6 或 Feature 8 的实现，架构决策需要提前。待澄清问题 #1/#2 同为 HIGH 优先级且计划第 1 轮迭代前澄清，#3 的 MEDIUM 优先级可能低估其影响。product-reviewer 视角下，边界问题越早澄清越能避免返工。
- **修复建议**：将优先级提升为 HIGH，或计划澄清时间改为"第 1 轮迭代前"

#### P-R3-08【LOW】Feature 8 Scenario #6 验收标准一句话包含多个独立断言
- **维度**：Scenario 清晰度 / 可验收性
- **位置**：Feature 8 Scenario #6（第 275-276 行）
- **理由**：该 Scenario 验收标准一句话包含三个可独立验证的断言：(a) 排队规则下一周期优先调度，(b) 单条规则最大延迟 ≤2 周期且 ≤30s，(c) 排队深度超 3 倍时拒绝并返回错误码。product-reviewer 视角下，一个 Scenario 应验证一个行为，多断言会导致验收时部分通过/部分失败的判定困难。
- **修复建议**：拆分为 2-3 个独立 Scenario，或至少将验收标准分行描述。

#### P-R3-09【LOW】文档元数据"负责人"字段为空
- **维度**：文档完整性
- **位置**：文档元数据（第 13 行）
- **理由**：模板要求填写负责人，当前为空。product-reviewer 视角下，需求文档进入冻结前必须明确负责人，以便变更追溯。
- **修复建议**：填写负责人姓名或角色。

---

### R3 评审数字摘要（product-reviewer）

| 级别 | 数量 | 分值 | 小计 |
|------|------|------|------|
| CRITICAL | 0 | 10 | 0 |
| HIGH | 0 | 3 | 0 |
| MEDIUM | 4 (P-R3-01, P-R3-02, P-R3-04, P-R3-06) | 1 | 4 |
| LOW | 5 (P-R3-03, P-R3-05, P-R3-07, P-R3-08, P-R3-09) | 0.1 | 0.5 |
| **合计** | **9** | — | **4.5** |

- **缺陷密度**：4.5 / 10 Feature = **0.45 分/Feature**（门槛 1.5）✅ PASS
- **CRITICAL**：0（门槛 0）✅ PASS
- **R2 修正验证**：13/13 全部修复，无引入新问题

### 结论

**R3 评审通过**。R2 提出的 13 个问题已全部高质量修复，文档在架构耦合消除、跨 Feature 一致性、NFR 兼容性、故障模式覆盖等维度均有显著改进。R3 新发现的 9 个问题均为 MEDIUM/LOW 级别，不影响通过门槛。建议在需求冻结前处理 4 个 MEDIUM 级问题（P-R3-01 统计周期、P-R3-02 Actor 视角、P-R3-04 灰色地带预留、P-R3-06 NFR 矛盾）。

| 轮次 | 日期 | 缺陷密度 | CRITICAL | HIGH | MEDIUM | LOW | 结论 |
|------|------|---------|----------|------|--------|-----|------|
| R2（architect-reviewer） | 2026-05-08 | 2.81/Feature | 0 | 8 | 4 | 1 | ❌ FAIL |
| R3（architect-reviewer） | 2026-05-09 | 0.92/Feature | 0 | 2 | 3 | 2 | ✅ PASS |
| R3（product-reviewer） | 2026-05-09 | 0.45/Feature | 0 | 0 | 4 | 5 | ✅ PASS |

