# 数据生命周期管理 需求规格 - 评审记录

> 由 product-reviewer 维护。每轮评审追加一节，不覆盖历史记录。

---

## R2 评审（product-reviewer）

- **评审日期**：2026-05-08
- **评审视角**：产品评审专家（用户/商业视角，独立于 product 起草）
- **评审范围**：`docs/requirements/数据生命周期管理.md` 全部 2 个 Feature + 故障矩阵 + 验收标准 + 非功能需求
- **基线**：`.claude/templates/req-feature.md` 模板自检清单 + R1 修正项关闭情况

---

### 修正项关闭检查（与 R1→R2 修正声明对照）

| R1 遗留问题 | R2 声明修正 | 复核结论 |
|------------|------------|----------|
| 合规边界缺失 | 已补"合规边界（反向冲突说明）"+ TTL #5 合规拒绝 Scenario | 关闭 |
| 故障矩阵错配 | 已重映射并补全网络分区/数据损坏/升级回滚列 | 关闭 |
| 降采样误差阈值无依据 | 已按指标类型分阈值（max/min 误差=0，avg 相对误差<=1e-9） | 关闭 |
| 隐性 Actor 遗漏 | 已补安全审计员、运维 SRE、监控告警系统 | 关闭 |
| TTL/降采样依赖未定义 | 已补"与 TTL 的依赖优先级"说明 + 降采样 #8 编排冲突 Scenario | 关闭 |

R1 五项修正声明全部落地。

---

### R2 新发现问题清单

#### HIGH-1（维度：Actor 遗漏 — 数据分析师）
- **位置**：涉及 Actors 列表（第 25-33 行）、降采样 Feature 全部 Scenario
- **问题**：`数据分析师`作为 product-spec.md 中定义的高权力高利益 Actor，在本域完全缺失。降采样直接影响数据分析师的查询结果——原始数据 vs 聚合数据的可视化差异、聚合精度对趋势判断的影响、查询时自动切换数据源的透明度等，均未从数据分析师视角定义验收标准。当前文档仅关注"系统正确执行"，忽视了"用户正确感知"。
- **理由**：数据分析师是核心利益相关方。降采样后，分析师查询历史时间范围时，系统返回的是聚合数据而非原始数据，这一行为变化必须被分析师理解和接受。若缺少分析师视角的 Scenario，可能导致上线后用户投诉"历史数据精度变低了但系统没有提示"。
- **修正建议**：补充至少 1 个数据分析师视角的 Scenario，例如"数据分析师查询已降采样的时间范围时，查询结果自动返回聚合数据并标注精度级别与原始数据可用性状态"，验收标准量化：聚合数据查询延迟 P95 <= 原始数据查询延迟的 120%，精度标注准确率 = 100%。

#### HIGH-2（维度：Feature 价值 — 降采样 ROI 未量化）
- **位置**：降采样 Feature 价值论证（第 71-84 行）
- **问题**：降采样的核心价值主张是"降低存储成本"（痛点中"存储成本每月增长 30%+"），但成功指标中未包含任何与"成本节省"相关的可衡量指标。三个成功指标（及时性、吞吐影响、兼容性）均与成本无关。
- **理由**：Feature 上线后无法验证 ROI 是产品管理的大忌。同时，"30%+"的增长率缺乏行业基准支撑——VictoriaMetrics 的压缩比可达 10-20x，若本系统已采用列式压缩，实际增长率可能远低于 30%，该数字可能是主观臆断。
- **修正建议**：在成功指标中补充存储成本相关指标，例如"降采样后历史数据（>30 天）存储量相对于保留原始数据减少 >= 80%"或"单 TB 月度存储成本下降 >= 70%"，并明确测量口径（含压缩后的实际磁盘占用）。

#### MEDIUM-1（维度：场景覆盖 — TTL 清理暂停/恢复运维场景缺失）
- **位置**：TTL Scenario 清单（第 53-66 行）
- **问题**：缺少运维场景下暂停/恢复 TTL 清理的 Scenario。实际运维中，SRE 经常需要在合规审计、重大故障排查、数据恢复验证等时段暂停清理。
- **理由**：若文档不定义暂停/恢复行为，实现阶段可能遗漏该能力，导致审计期间数据被意外清理引发合规风险，或故障排查期间清理干扰诊断。暂停期间的过期数据积压如何在恢复后处理，也需要定义。
- **修正建议**：补充 Scenario"运维 SRE 在审计期间暂停 TTL 清理，审计结束后恢复执行"，验收标准：暂停期间零清理触发；恢复后 48h 内完成积压过期数据清理；暂停/恢复操作 100% 记入审计日志。

#### MEDIUM-2（维度：验收标准量化 — "空间回收率 >= 95%"测量口径模糊）
- **位置**：TTL Feature 成功指标（第 49 行）、本域验收标准（第 120 行）、Scenario #1（第 57 行）
- **问题**："空间回收率 >= 95%"的测量口径未定义，存在多种解读：(a) 逻辑删除记录数 / 应删除记录数，(b) 实际释放磁盘空间 / 过期数据占用空间，(c) 考虑压缩后的有效空间释放。
- **理由**：不同存储引擎（LSM-tree vs B-tree）的物理删除延迟不同。若采用 LSM-tree，compaction 是异步的，"24h 内清理完成"与"空间回收率 95%"在物理层面存在时间窗口冲突，验收时必然产生争议。
- **修正建议**：明确空间回收率定义口径，例如"逻辑删除的数据记录数 / 应删除的记录数 >= 95%，物理空间释放由存储层 compaction 异步完成，在 compaction 完成后 7 天内达到 90%"；或拆分为两个独立指标：逻辑清理率 + 物理回收率。

#### MEDIUM-3（维度：场景覆盖 — 降采样聚合数据 TTL 未定义）
- **位置**：降采样 Feature 全部 Scenario
- **问题**：降采样产生的聚合数据（1min 精度、1h 精度）本身的保留策略（TTL）完全未定义。
- **理由**：这是数据生命周期管理的核心问题。若聚合数据与原始数据同 TTL（如均 30 天），则降采样的长期价值大打折扣（用户无法查询 30 天前的趋势）。若聚合数据独立 TTL，则需要定义默认策略和配置方式。当前文档的缺失会导致实现阶段的产品决策真空。
- **修正建议**：补充聚合数据 TTL 策略定义，例如"聚合数据支持独立 TTL 配置，默认聚合数据保留周期 = 原始数据 TTL × 10（最低 1 年）"；并增加 Scenario"数据工程师为 1h 精度聚合数据配置 2 年独立保留策略"。

#### MEDIUM-4（维度：依赖关系 — 边缘自治域 TTL 依赖未识别）
- **位置**：自检清单（第 153 行）声称"依赖关系已确认"
- **问题**：product-spec.md 中"边缘自治"Feature（P0）明确包含"离线期间独立完成 TTL 管理"，但本文档未识别与边缘自治域的任何依赖关系。
- **理由**：边缘节点离线时的 TTL 清理行为（是否执行？积压如何处理？恢复后如何与云端策略同步？）与本文档定义的云端 TTL 策略存在直接依赖。若两边独立定义，可能导致边云 TTL 策略不一致（如云端 30 天、边缘 7 天），数据同步时产生冲突。
- **修正建议**：在已知待澄清问题中增加"边缘节点离线期间 TTL 清理策略与云端策略的同步机制"，或在文档中明确声明"本文档仅覆盖云端集群 TTL/降采样，边缘自治域 TTL 管理由边缘自治域文档定义，边云策略一致性由边云协同域保障"。

#### LOW-1（维度：格式 — R1 评审记录缺陷密度数值异常）
- **位置**：评审记录表（第 163 行）
- **问题**：R1 缺陷密度写为"15.15/Feature"，但按 1 CRITICAL(10) + 4 HIGH(3×4=12) = 22 分，密度应为 11.0/Feature，数字对不上。
- **理由**：数值不一致会导致评审历史可信度受损，后续轮次无法正确追踪改进趋势。
- **修正建议**：修正为准确值"11.0/Feature"，或注明计算方式。

---

### Actor 遗漏检查表

| 检查项 | 是否覆盖 | 说明 |
|--------|---------|------|
| 安全审计员 | 已覆盖 | TTL #5 合规拒绝、NFR 安全性维度均涉及 |
| 合规检查（数据保留合规） | 已覆盖 | 合规边界说明、成功指标"合规违规清理事件=0" |
| 运维值班（SRE） | 已覆盖 | TTL #6/#7/#8、降采样 #8/#10 均涉及告警 SRE |
| 第三方集成方 | 部分 | 监控告警系统已覆盖，但边缘自治域未识别依赖 |
| 监控/告警系统 | 已覆盖 | 明确列为协作 Actor，NFR 可观测性维度覆盖 |
| 数据分析师 | ❌ 遗漏 | 降采样直接影响查询结果，但无任何分析师视角 Scenario，参见 HIGH-1 |

---

### 需求蔓延检查

| 维度 | 评估 |
|------|------|
| Feature 总数（2 个）合理性 | 合理。TTL 清理 + 降采样聚合是数据生命周期管理域的最小完备集 |
| 可延期项 | 无明确可延期项标识。降采样 P1 优先级合理，但 ROI 量化缺失影响优先级论证 |
| 优先级数据支撑 | TTL=P0、降采样=P1 与 product-spec 一致；但降采样的"存储成本节省"成功指标缺失，ROI 论证不足 |

---

### 评分统计

| 级别 | 数量 | 分值 | 小计 |
|------|------|------|------|
| CRITICAL | 0 | 10 | 0 |
| HIGH | 2 | 3 | 6 |
| MEDIUM | 4 | 1 | 4 |
| LOW | 1 | 0.1 | 0.1 |
| **总分** | **7** | — | **10.1** |
| **密度** | — | — | **5.05/Feature** |

### 通过判定

| 门槛项 | 要求 | 实际 | 判定 |
|--------|------|------|------|
| 总分 | <= 3.0 | 10.1 | ❌ |
| 密度 | <= 1.5/Feature | 5.05/Feature | ❌ |
| CRITICAL | = 0 | 0 | ✅ |

**结论：FAIL**

**关键阻塞项**：
1. HIGH-1 数据分析师 Actor 完全遗漏——降采样是查询体验的重大变更，缺少用户视角验收标准
2. HIGH-2 降采样 ROI 未量化——核心商业价值无法验证

**修正方向**：补充数据分析师视角 Scenario + 降采样存储成本节省量化指标 + 聚合数据 TTL 策略定义。

---

## R2 修正关闭记录（product）

- **修正日期**：2026-05-08
- **修正范围**：`docs/requirements/数据生命周期管理.md` 全部 2 个 Feature + 故障矩阵 + 验收标准 + 非功能需求
- **基线**：R2 评审发现的 7 个问题（2 HIGH + 4 MEDIUM + 1 LOW）

### 修正项关闭检查

| 问题编号 | 问题描述 | 修正措施 | 状态 |
|----------|----------|----------|------|
| HIGH-1 | 数据分析师 Actor 完全遗漏 | 在 Actors 列表新增数据分析师（主 Actor）；降采样 Feature 新增 Scenario #10（分析师查询已降采样历史数据，精度标注准确率 = 100%）、Scenario #11（分析师查询混合时间范围，合并结果无丢失无重复） | 关闭 |
| HIGH-2 | 降采样 ROI 未量化 | 成功指标新增："降采样后历史数据（>30 天）存储量相对于保留原始数据减少 >= 80%"（含压缩后实际磁盘占用）；新增"聚合数据查询 P95 延迟 <= 同时间范围原始数据查询 P95 延迟的 50%" | 关闭 |
| MEDIUM-1 | 运维暂停/恢复 TTL 清理场景缺失 | TTL Scenario 清单新增 #10：运维 SRE 暂停/恢复 TTL 清理，验收标准：暂停期间零清理、恢复后 48h 内完成积压、操作 100% 审计 | 关闭 |
| MEDIUM-2 | 空间回收率测量口径模糊 | 将"空间回收率 >= 95%"拆分为逻辑清理率（>= 95%，24h 内）和物理回收率（>= 90%，compaction 完成后 7 天内），明确测量口径 | 关闭 |
| MEDIUM-3 | 聚合数据 TTL 未定义 | 降采样 Feature 价值论证新增"聚合数据 TTL 策略"段落：支持独立 TTL，默认 = 原始数据 TTL × 10（最低 1 年）；新增 Scenario #12（1h 聚合数据配置 2 年独立保留策略） | 关闭 |
| MEDIUM-4 | 边缘自治域 TTL 依赖未识别 | 已知待澄清问题新增 #4：边缘节点离线期间 TTL 策略同步机制；NFR 兼容性维度新增边缘自治域 TTL 策略同步项；自检清单依赖关系确认范围扩展至边缘自治域、边云协同域 | 关闭 |
| LOW-1 | R1 缺陷密度数值异常 | 修正 R1 密度为 11.0/Feature，注明计算方式（1 CRITICAL × 10 + 4 HIGH × 3 = 22 分 / 2 Feature = 11.0） | 关闭 |

### 新增内容统计

- **新增 Actor**：1 个（数据分析师）
- **新增 Scenario**：4 个（TTL #10 运维暂停、降采样 #10/#11 分析师查询、降采样 #12 聚合数据独立 TTL）
- **新增成功指标**：2 个（存储成本节省 >= 80%、查询加速 <= 50%）
- **新增验收标准**：4 项（清理空间回收、降采样存储成本节省、聚合数据查询性能、聚合数据 TTL 独立配置）
- **新增 NFR**：3 条（聚合数据查询加速、大规模删除性能、混合查询数据完整性、边缘自治域 TTL 策略同步）

### 修正后预估

- 修正前：7 issues，密度 5.05/Feature
- 修正后预估：0 HIGH（2 个已关闭），MEDIUM/LOW 已全部关闭
- **预估修正后密度**：<= 0.5/Feature（待 R3 复核确认）

---

## R3 复核（product-reviewer）

- **评审日期**：2026-05-08
- **评审视角**：产品评审专家（用户/商业视角，独立于 product 起草）
- **评审范围**：`docs/requirements/数据生命周期管理.md` 全部 2 个 Feature + 故障矩阵 + 验收标准 + 非功能需求
- **基线**：R2 修正声明（7 项问题关闭）+ `.claude/templates/req-feature.md` 模板自检清单

---

### R2 修正项关闭检查

| R2 问题 | 修正声明 | R3 复核结论 |
|---------|---------|------------|
| HIGH-1 数据分析师 Actor 遗漏 | Actors 列表新增数据分析师（主 Actor）；降采样新增 Scenario #10/#11（分析师查询） | **关闭**。数据分析师已列为主 Actor，Scenario #10（聚合数据查询+精度标注）、#11（混合时间范围查询）均从分析师视角定义，验收标准量化且可独立验证 |
| HIGH-2 降采样 ROI 未量化 | 成功指标新增"存储量相对于保留原始数据减少 >= 80%"（含压缩后实际磁盘占用）；新增"聚合数据查询 P95 延迟 <= 原始数据 50%" | **关闭**。存储成本节省指标明确量化，测量口径（含压缩后实际磁盘占用、同时间范围同 measurement 对比）清晰可验证；查询加速指标作为 ROI 补充验证 |
| MEDIUM-1 运维暂停/恢复 TTL 场景缺失 | TTL 新增 Scenario #10：运维 SRE 暂停/恢复 TTL 清理 | **关闭**。验收标准完整：暂停期间零清理、恢复后 48h 内完成积压、操作 100% 审计 |
| MEDIUM-2 空间回收率口径模糊 | 拆分为逻辑清理率（>= 95%，24h 内）和物理回收率（>= 90%，compaction 完成后 7 天内） | **关闭**。双口径定义清晰，与存储层 compaction 异步机制解耦，避免验收争议 |
| MEDIUM-3 聚合数据 TTL 未定义 | 价值论证新增"聚合数据 TTL 策略"段落；新增 Scenario #12（1h 聚合数据 2 年独立保留） | **关闭**。默认策略（原始 TTL × 10，最低 1 年）、独立配置能力、合规约束继承均定义完整 |
| MEDIUM-4 边缘自治域 TTL 依赖未识别 | 已知待澄清问题新增 #4；NFR 兼容性维度新增边缘自治域 TTL 策略同步项；自检清单依赖范围扩展 | **关闭**。依赖边界声明清晰（边缘自治域独立管理、边云协同域保障一致性），不侵入其他域职责 |
| LOW-1 R1 缺陷密度数值异常 | 修正为 11.0/Feature，注明计算方式 | **关闭**。数值与计算方式一致 |

R2 七项修正声明全部落地，无残留。

---

### R3 新发现问题清单

#### MEDIUM-R3-1（维度：验收标准 — 本域验收标准表存在重复项）
- **位置**：本域验收标准表（第 128-142 行）
- **问题**："TTL 清理及时性"与"清理及时性"两行内容高度重复，前者缺少逻辑清理率量化，后者完整。前者为冗余行，应删除。
- **理由**：重复验收标准会导致特性级 OpenSpec 阶段引用歧义，增加下游实现和测试的维护成本。
- **修复建议**：删除"TTL 清理及时性"行，保留"清理及时性"行（含逻辑清理率 >= 95% 量化标准）。

#### LOW-R3-1（维度：格式 — 负责人字段为空）
- **位置**：文档元数据（第 13 行）
- **问题**："负责人"字段为空，未填写。
- **理由**：模板要求元数据至少填写冻结标识和更新日期，负责人为推荐项。虽非强制，但空字段影响文档可追溯性。
- **修复建议**：填写负责人名称或标注"待分配"。

---

### Actor 遗漏检查表

| 检查项 | 是否覆盖 | 说明 |
|--------|---------|------|
| 安全审计员 | 已覆盖 | TTL #5 合规拒绝、NFR 安全性维度均涉及 |
| 合规检查（数据保留合规） | 已覆盖 | 合规边界说明、成功指标"合规违规清理事件=0" |
| 运维值班（SRE） | 已覆盖 | TTL #6/#7/#10、降采样 #8 均涉及告警/操作 SRE |
| 第三方集成方 | 已覆盖 | 监控告警系统已覆盖；边缘自治域依赖已识别 |
| 监控/告警系统 | 已覆盖 | 明确列为协作 Actor，NFR 可观测性维度覆盖 |
| 数据分析师 | 已覆盖 | 列为主 Actor，Scenario #10/#11 从分析师视角定义 |

---

### 需求蔓延检查

| 维度 | 评估 |
|------|------|
| Feature 总数（2 个）合理性 | 合理。TTL 清理 + 降采样聚合是数据生命周期管理域的最小完备集 |
| 可延期项 | 无。2 个 Feature 均为核心能力，P0/P1 优先级与 product-spec 一致 |
| 优先级数据支撑 | TTL=P0（合规刚需+成本增长）、降采样=P1（成本优化+查询加速），ROI 已量化（存储节省 >= 80%、查询加速 <= 50%） |

---

### 评分统计

| 级别 | 数量 | 分值 | 小计 |
|------|------|------|------|
| CRITICAL | 0 | 10 | 0 |
| HIGH | 0 | 3 | 0 |
| MEDIUM | 1 | 1 | 1 |
| LOW | 1 | 0.1 | 0.1 |
| **总分** | **2** | — | **1.1** |
| **密度** | — | — | **0.55/Feature** |

### 通过判定

| 门槛项 | 要求 | 实际 | 判定 |
|--------|------|------|------|
| 总分 | <= 3.0 | 1.1 | ✅ |
| 密度 | <= 1.5/Feature | 0.55/Feature | ✅ |
| CRITICAL | = 0 | 0 | ✅ |

**结论：PASS**

**剩余问题说明**：
1. MEDIUM-R3-1 验收标准重复项 — 建议删除冗余行，不影响系统方向
2. LOW-R3-1 负责人字段为空 — 建议补充，不影响功能正确性

---

### 评审记录表更新

| 轮次 | 日期 | 缺陷密度 | CRITICAL | HIGH | 结论 |
|------|------|---------|----------|------|------|
| R1 | 2026-05-08 | 11.0/Feature | 1 | 4 | ❌ FAIL |
| R2 | 2026-05-08 | 5.05/Feature | 0 | 2 | ❌ FAIL |
| R2-修正 | 2026-05-08 | — | — | — | 修正完成：7 项问题全部关闭 |
| **R3** | **2026-05-08** | **0.55/Feature** | **0** | **0** | **✅ PASS** |

**评审状态**: ✅ PASS（缺陷密度 0.55/Feature，无 CRITICAL，循环 3/3）
