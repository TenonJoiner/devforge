---
template-for: docs/architecture/system-overview.md
mandatory-sections:
  - 系统简介
  - 架构分层与设计思想
  - 子系统全景
  - 数据流总览
  - 全局设计约束
  - 已知风险
optional-sections: []
checklist-at-end: true
---

# <系统名> 架构总览

## 系统简介

> **写什么**：这个系统是什么、解决什么核心问题、目标场景
> **不写什么**：不在此展开子系统细节
> **最低深度**：2-3 句概述 + 目标场景

<2-3 句话概述系统定位>

**目标场景**：
- <场景 1>
- <场景 2>

## 架构分层与设计思想

> **写什么**：系统整体分层架构、每层职责、贯穿各层的设计思想
> **不写什么**：不在此展开各层内部设计（属于 subsystem 文档职责）
> **最低深度**：分层图（ASCII Art）+ 每层一句话职责 + ≥2 条贯穿各层的设计思想

**分层结构**：

```
<ASCII Art 分层图，标注层间关系>
```

| 层 | 职责 | 所属子系统 |
|----|------|-----------|
| <层名> | <一句话职责> | <子系统列表> |

**贯穿各层的设计思想**：
1. **<思想名>**：<描述及在各层的体现>
2. **<思想名>**：<描述及在各层的体现>

## 子系统全景

> **写什么**：系统由哪些子系统组成、各自在分层中的位置
> **不写什么**：不在此展开子系统内部设计
> **最低深度**：表格覆盖所有子系统

| 子系统 | 所属层 | 职责 | 文档链接 |
|--------|--------|------|----------|
| <子系统名> | <层> | <一句话职责> | [overview.md](<subsystem>/overview.md) |

## 数据流总览

> **写什么**：请求跨层、跨子系统如何流转
> **不写什么**：不在此描述子系统内部数据流
> **最低深度**：≥2 条端到端流路径（ASCII/Mermaid），标注穿越的层和子系统

### <流名 1>

```
<ASCII Art 或 Mermaid，标注层和子系统>
```

**路径**：<请求入口> → <层 A: 子系统 X> → <层 B: 子系统 Y> → <输出>

### <流名 2>

```
<ASCII Art 或 Mermaid，标注层和子系统>
```

**路径**：<请求入口> → ... → <输出>

## 全局设计约束

> **写什么**：跨子系统的一致约束（一致性模型、故障假设、性能基线等）
> **不写什么**：不在此写子系统内部的局部约定
> **最低深度**：≥3 条全局约束，每条有从代码中观察到的证据

| 约束 | 描述 | 适用子系统 | 代码证据 |
|------|------|-----------|----------|
| <约束名> | <描述> | <子系统列表> | <代码中的体现或引用> |

## 已知风险

> **写什么**：系统级的技术债务和全局性风险
> **不写什么**：不在此列子系统级已知问题（属于各自 subsystem 文档）
> **最低深度**：无则写"未发现"

| 风险 | 可能性 | 影响 | 缓解方向 |
|------|--------|------|----------|
| <风险> | 高/中/低 | 高/中/低 | <缓解方向> |

若无可写"未发现"。

---

## 自检清单

- [ ] 系统简介 2-3 句 + 目标场景清晰
- [ ] 分层图（ASCII Art）完整，每层职责明确
- [ ] ≥2 条贯穿各层的设计思想
- [ ] 子系统全景表覆盖所有子系统
- [ ] ≥2 条端到端数据流路径，标注穿越的层和子系统
- [ ] ≥3 条全局约束，各有代码证据
- [ ] 已知风险无则写"未发现"
- [ ] 文档总篇幅 ≤300 行
- [ ] 不展开任何子系统的内部设计
