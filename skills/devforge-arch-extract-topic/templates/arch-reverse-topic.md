---
template-for: docs/architecture/<subsystem>/<topic>.md
mandatory-sections:
  - 设计背景与问题
  - 核心设计
  - 关键设计
  - 接口与交互
  - 已知问题
optional-sections: []
checklist-at-end: true
---

# <主题名>

> **写作原则**：本文档主要面向 coding agent，同时兼顾人的阅读体验。每个设计决策都应回答三个问题——**解决了什么问题、为什么这样解决、舍弃了什么**。章节间应有逻辑递进，必要时给出技术背景铺垫，让读者能独立理解设计脉络。代码与社区文档一致的直接写，不一致时以代码为准并注明差异。

## 设计背景与问题

> **写什么**：这个主题为何存在、面临什么技术矛盾、在子系统中的定位。给出必要的背景铺垫，让不熟悉这个模块的读者也能理解"为什么需要这个设计"
> **不写什么**：不泛泛描述"为什么需要 X"、不在此展开具体实现
> **最低深度**：明确陈述 1 个核心技术矛盾

<技术背景 + 1 个核心技术矛盾 + 在子系统中的定位>

## 核心设计

> **写什么**：整体设计思路、组件拓扑、一条核心流程的端到端描述
> **不写什么**：不在此展开各设计点的子问题分析（属于关键设计章节）
> **最低深度**：ASCII Art 组件拓扑图 + 1 条核心流程的时序/交互图 + 文字描述

**整体思路**：
<3-5 句概述整体设计思路和核心设计模式，点明这个设计的核心思想>

**组件拓扑**：

```
<ASCII Art 组件图，标注组件间关系>
```

<对组件图的文字描述：每个组件的职责、组件间的数据/控制依赖关系>

**核心流程**（以 <典型场景> 为例）：

```
<Mermaid 或 ASCII Art 时序图，标注参与者、调用方向、同步/异步、关键数据>
```

<对时序图的文字描述，补充图上不易表达的细节>

## 关键设计

> **写什么**：2-4 个关键设计点的深度分析，用多段文字逐层展开——先交代这个设计点解决什么子问题，再描述怎么做，做选择时一并说明为什么选这条路、取舍是什么。涉及核心数据结构时直接在此展示源码定义（带字段注释），不另设章节
> **不写什么**：不在此重复核心设计章节已描述的整体流程
> **最低深度**：每个设计点覆盖「子问题 + 方案描述 + 取舍理由」，取舍内嵌在方案描述中，不独立分段；涉及的核心结构有源码定义

### <设计点 1>

<一段文字点明这个设计解决什么子问题，为什么需要它>

<多段文字展开设计方案，做了什么选择、按什么逻辑组织、并发/容错/性能语义自然融入。做选择处顺带说明备选方案和取舍理由，不单独成段。涉及核心结构时直接展示源码定义：>

```c
// <这个结构在设计中的角色>
struct xxx {
    int     field1;   // <用途、语义、约束>
    void   *field2;   // <用途、语义、约束>
};
```

<若有刻意安排的布局细节（如对齐到 cache line、static_assert 强制大小等），在此说明设计意图，否则不写>

<若有必要，最后提及极限/异常场景下的行为，一两句带过；若无特别值得说的边界行为，不写>

### <设计点 2>

<子问题>

<方案描述与取舍>


## 接口与交互

> **写什么**：核心接口的使用方式、隐藏契约、以及接口背后的上下游交互关系
> **不写什么**：不罗列所有公开函数、不在此展开内部实现
> **最低深度**：≥2 个核心接口，每个含源码签名 + 使用场景 + 隐藏契约 + 上下游交互方式

### <接口名>

```c
<源码函数签名，含参数名和类型>
```

**使用场景**：<谁在何时调用、典型调用序列>

**隐藏契约**（只写"看签名/源码一眼看不出来"的约束——参数取值范围、常规错误码、通用语言约定等可直接推断的内容不写；保留反直觉的语义契约、设计选择、隐式上下文依赖）：
- 前置：<调用前必须满足>
- 后置：<调用后保证>
- 异常：<错误返回码及语义>
- <内存所有权、锁持有、可重入性等非显而易见的约束>

**交互**：调用方 <模块>（同步/异步，耦合 强/中/弱），被调用 <模块>

### <接口名>

```c
<源码函数签名>
```

**使用场景**：

**隐藏契约**：

**交互**：调用方 <模块>（耦合强度），被调用 <模块>

## 已知问题

> **写什么**：当前设计局限、与上游社区文档的版本差、值得关注的 TODO/FIXME。每条标注性质——架构约束（永久保留）、文档分叉（稳定记录，防读者被上游文档误导）、待核实（TODO，需后续确认后消解或删除）
> **不写什么**：不在此记录已解决或计划中已有明确排期的问题
> **最低深度**：无已知问题则写"未发现"；文档-代码差异记录"文档说 X，代码实际为 Y"

| 问题 | 影响 | 性质 | 备注 |
|------|------|------|------|
| <描述> | <影响范围> | 架构约束 / 文档分叉 / 待核实 | <补充信息> |

若无可写"未发现"。

---

## 自检清单

- [ ] 每个设计决策都回答了"为什么"，而非仅陈述"是什么"
- [ ] 核心技术矛盾已明确陈述，必要的背景铺垫让读者能独立理解
- [ ] 组件拓扑图 + 核心流程时序/交互图已包含
- [ ] 2-4 个关键设计点各自用多段 prose 逐层展开，取舍内嵌，涉及的核心结构有源码定义（字段注释）
- [ ] ≥2 个核心接口各附源码签名 + 使用场景 + 隐藏契约 + 交互关系
- [ ] 已知问题无则写"未发现"，有则记录"文档说 X，代码实际为 Y"
- [ ] 文档总篇幅 ≤500 行
- [ ] 安全/性能/错误语义融入关键设计描述中，未独立成章
- [ ] 章节间有逻辑递进，读者无需先读源码也能理解设计脉络
