---
name: devforge-arch-extract-topic
description: 逆向提取技术主题深度设计文档——跨目录追踪一个技术主题，深度分析其架构设计、数据结构、核心算法、并发模型，生成单篇 topic 文档
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent, AskUserQuestion]
---

# devforge-arch-extract-topic — 技术主题逆向提取

## 概述

从已有源码逆向提取一个技术主题的深度设计文档。技术主题是按技术关注点组织的横向概念（如"协程调度模型""本地存储引擎""Raft 共识"），跨目录存在，不按代码目录组织。

**核心原则**：
1. **源码 = 行为真相**：代码是唯一权威的事实来源
2. **社区文档 = 参考线索**：可选的社区文档提供概念框架和设计背景，但与代码矛盾时以代码为准
3. **分析密度优先**：深度分析阶段用多 agent 按维度切分并行，确保每个维度读够源码
4. **单次生成单篇文档**：每次执行只产出当前目标 topic 的一篇文档

**与 subsystem/system skill 的关系**：
- topic 是最深层的设计文档，聚焦单个技术主题的"怎么做"和"为什么这样做"
- subsystem 文档为 topic 提供架构上下文（组件拓扑、模块间关系）
- topic 文档不重复 subsystem 已有的上下文描述

## 工作目录约定

- **输入**：
  - 源码目录 `--src=<dir>`（默认：当前工作目录）—— 事实来源
  - 社区文档目录 `--ref-doc=<dir>`（默认：空，不启用社区文档）—— 参考来源
- **输出**：`--output-docs=<dir>`（默认：`docs`），实际路径为 `<output-docs>/architecture/<subsystem>/<topic>.md`
- **模板**：`skills/devforge-arch-extract-topic/templates/arch-reverse-topic.md`

**输出路径确定**：
- `<subsystem>` 由阶段 3 范围确认时与用户商定
- `<topic>` 文件名由主会话根据主题名生成（小写、连字符分隔，如 `coroutine-scheduler`）

## 启动检测

### 步骤 1：解析参数

- `--target=<主题名>`（必选）：要分析的技术主题，如"协程调度模型""本地存储引擎"
- `--src=<dir>`（可选，默认：当前工作目录）：源码目录路径
- `--ref-doc=<dir>`（可选，默认：空）：社区文档目录路径，不传则不启用社区文档
- `--output-docs=<dir>`（可选，默认：`docs`）：输出文档根目录，实际输出路径为 `<output-docs>/architecture/<subsystem>/<topic>.md`

若无 `--target` 参数，报错提示用法。

---

## 初次生成模式

### 第 1 阶段：主题定位

**职责**：根据主题名在代码库中跨目录定位相关文件、识别入口点

派遣 1 个 developer agent：

```
**任务模式**：代码库主题定位
**任务**：根据主题名 "<target>" 在 <src> 中定位所有相关源文件

**输入**：
- 主题名：<target>
- 代码库根目录：<src>

**分析内容**：
1. 扫描目录结构，匹配主题关键词（含同义词、英文对应词）
2. 识别核心入口文件（头文件、主模块文件）
3. 识别相关实现文件（跨目录追踪依赖）
4. 识别相关测试文件（了解使用方式）

**输出**（写入 `/tmp/arch-extract-topic-locate-<ts>.md`）：
- 文件清单：按目录分组的文件列表
- 入口点：3-5 个核心入口文件/函数
- 依赖关系：文件间的 include/import 关系概览
- 规模估算：总行数、文件数
- 边界建议：主题的合理范围建议（哪些文件属于本主题、哪些属于边界外）
```

**质量门禁**：
- 找到 ≥3 个相关文件（否则主题名可能不对，向用户确认）
- 入口点已识别

### 第 2 阶段：文档发现与分析

**前置条件**：`--ref-doc=<dir>` 已提供。未提供则跳过本阶段。

派遣 2 个 researcher agent 串行（Agent B 依赖 Agent A 的文档列表）：

**Agent A — 文档发现与筛选**：

```
**任务模式**：社区文档发现
**任务**：在 <ref-doc> 下搜索与主题 "<target>" 相关的文档

**输入**：
- 文档目录：<ref-doc>
- 主题名：<target>

**匹配逻辑**（两步并行，结果合并去重）：
1. 搜索目录索引文件（SUMMARY.md、INDEX.md、README.md、toc.md 等），从中按主题逐层导航定位相关文档
2. 按 target 关键词（及其同义词/英文对应词）全文搜索所有文档内容，作为补充
3. 合并去重，筛选最相关的 1-5 篇

**输出**（写入 `/tmp/arch-extract-topic-doclist-<ts>.md`）：
- 匹配到的文档列表（路径 + 标题 + 相关度评分）
```

**Agent B — 设计声明提取**：

```
**任务模式**：设计声明提取
**任务**：从匹配到的社区文档中提取设计层面的声明

**输入**：
- 文档列表：由 Agent A 产出
- 主题名：<target>

**提取维度**：
1. 设计动机/背景：这个主题为何存在、解决了什么技术矛盾、设计目标是什么
2. 架构声明：组件划分、模块关系
3. 接口声明：API 定义、契约约定
4. 数据模型声明：核心数据结构描述
5. 关键机制/流程声明：核心流程描述、触发条件、执行步骤
6. 并发/一致性声明：锁策略、一致性保证
7. 设计约束声明：明确的设计约定和限制

**输出**（写入 `/tmp/arch-extract-topic-claims-<ts>.md`）：
- 声明清单：按维度组织的设计声明列表
- 每条声明标注出处（文档路径 + 章节）
```

**质量门禁**（若启用了文档）：
- Agent A 找到 1-5 篇相关文档
- Agent B 提取 ≥3 条设计声明

### 第 3 阶段：范围确认

**职责**：向用户展示定位结果 + 匹配到的文档列表 + 文档声明清单，确认主题边界

主会话读取阶段 1 和阶段 2 的产出摘要（≤5 行），展示给用户：

1. **主题定位结果**：找到 N 个相关文件，跨 M 个目录
2. **入口点**：3-5 个核心入口
3. **文档匹配**（若启用）：匹配到 K 篇文档，提取 L 条设计声明
4. **边界建议**：建议的主题范围

使用 AskUserQuestion 确认：
- 主题边界是否合适 → 不合适时提供调整选项：
  - 缩小：从建议清单中剔除部分目录/文件
  - 扩大：追加用户指定的目录/文件
  - 重新定位：修改 target 关键词，回到阶段 1 重新搜索
- 输出路径：`<output-docs>/architecture/<subsystem>/<topic>.md`（确认 subsystem 和 topic 文件名）

确认后检查输出文件是否已存在：
- **不存在** → 进入初次生成模式（第 4-9 阶段）
- **已存在** → 进入补全模式（用模板自检清单扫描缺失项，跑阶段 4-10 补全）
  - 若非补全而是修订已有内容，不适用本 skill，主人直接用 `/df:design` 或手动编辑

### 第 4 阶段：代码分析

**职责**：深度分析源码，从四个维度并行切入

**上下文注入**（主会话准备，注入各 agent prompt）：
- 阶段 1 定位结果中的文件清单和入口点
- 若启用了 `--ref-doc`：阶段 2 的设计声明清单（`/tmp/arch-extract-topic-claims-<ts>.md`），agent 分析时对照声明逐条验证代码是否支持（"设计动机/背景"类声明无需验证，留作阶段 8 写作参考）
- 若 `<output-docs>/architecture/<subsystem>/` 下已有 subsystem/topic 文档：留作阶段 8 写作参考（避免重复已有上下文），不注入阶段 4

派遣 4 个 developer agent 并行：

**Agent A — 代码组织与控制流**：

```
**任务**：阅读源码，理清代码组织方式、从入口点出发的调用链、关键数据结构的传递路径

**输入**：
- 文件清单与入口点：阶段 1 产出（入口点是控制流追踪的起点）
- 主题名：<target>
- <若启用了 ref-doc> 设计声明清单（架构/关键机制部分）：/tmp/arch-extract-topic-claims-<ts>.md，逐条与代码对照

**分析要求**：
1. 代码组织：从 #include/import 关系和目录结构出发，画出文件之间的依赖关系图。列出每个目录/命名空间下有哪些关键文件、各文件之间的 include 关系
2. 调用链：从阶段 1 给出的每个入口函数出发，向下追踪内部调用（不超出阶段 1 的文件清单范围），列出调用路径上经过的函数和文件行号。分支路径只追到主题边界内的叶子函数
3. 数据传递：选择阶段 1 文件清单中出现频率最高的 2-3 个核心 struct/class，追踪它们在调用链上的传递方式（函数参数/返回值/全局变量/队列），标注每次传递的函数和行号

**输出**（写入 `/tmp/arch-extract-topic-arch-<ts>.md`）：
- 文件依赖关系图（ASCII Art，标注目录和关键文件路径）
- 每个入口函数的调用链（函数名 → 函数名 → ...，每步标注文件和行号）
- 2-3 个核心 struct 的传递路径（从创建到消费，标注每步的函数和行号）
- <若启用了 ref-doc> 对照声明清单的逐条验证：代码支持 / 代码不支持 / 无法验证（附具体行号）
```

**Agent B — 数据结构与算法**：

```
**任务**：提取阶段 1 入口点涉及的核心 struct/class 的完整定义、内存/磁盘布局、以及入口函数内的关键算法实现

**输入**：
- 文件清单与入口点：阶段 1 产出（优先分析入口函数直接操作的数据结构）
- 主题名：<target>
- <若启用了 ref-doc> 设计声明清单（数据模型部分）：/tmp/arch-extract-topic-claims-<ts>.md>

**分析要求**：
1. 核心 struct/class 选择：阶段 1 入口函数的参数类型和返回值类型优先；若入口函数涉及的 struct 不足 2 个，再扩展到入口函数调用的内部函数所操作的 struct
2. 字段提取：完整列出每个 struct 的所有字段（字段名、类型、源码行号），附源码中的注释原文。计算 sizeof 并列出对齐填充
3. 内存布局：列出嵌套 struct 的包含关系、union 的变体、柔性数组等影响内存布局的结构。若代码中有 static_assert(sizeof(...) == N)，一并列出
4. 磁盘/网络布局：找到序列化/反序列化函数，逐字段描述写入和读出的顺序、格式（如 varint、固定宽度、length-prefixed）、字节序处理
5. 关键算法：选择入口函数内部最复杂的 1-2 段业务逻辑（非参数校验、非错误处理），从源码中提取算法步骤，用伪代码描述，标注源函数和行号

**输出**（写入 `/tmp/arch-extract-topic-data-<ts>.md`）：
- 每个核心 struct 的字段表（字段名 | 类型 | 行号 | 注释原文）
- 每个 struct 的内存布局描述（sizeof、对齐、嵌套关系、static_assert）
- 序列化/反序列化的逐字段格式描述（附序列化函数和行号）
- 1-2 个关键算法的伪代码（标注源函数和行号）
- <若启用了 ref-doc> 对照声明清单中数据模型部分的逐条验证
```

**Agent C — 并发与资源**：

```
**任务**：识别项目并发框架，基于框架提取同步原语和执行单元的创建点，追踪关键对象的分配和释放路径

**输入**：
- 文件清单与入口点：阶段 1 产出（入口函数直接创建/操作的对象即为关键对象）
- 主题名：<target>
- <若启用了 ref-doc> 设计声明清单（并发/一致性部分）：/tmp/arch-extract-topic-claims-<ts>.md

**分析要求**：
1. 识别并发框架：从阶段 1 文件清单的 #include 中识别并发/同步库（pthread.h、abt.h、folly/executors、seastar、std::thread 等），确定项目的并发模型和对应的同步原语。若项目使用单线程无锁模型（如 Seastar reactor），后续步骤 2 改为描述实际的同步机制（future/promise、continuation、消息队列等）而非搜索传统锁
2. 同步原语：从步骤 1 识别的框架同步原语出发，列出每个同步原语的类型（框架对应的 mutex/rwlock/spinlock/atomic 或等价 API）、保护对象（它守卫哪个成员/哪段逻辑）、获取/释放调用点（含 RAII 守卫）、是否存在跨函数持有，全部标注行号。若步骤 1 判定为无锁模型，描述代码中实际使用的同步机制（如等待 future、提交回调、写入消息队列），标注行号
3. 执行单元：从步骤 1 识别的创建 API 出发，搜索执行单元的创建点（ULT/tasklet/goroutine/fiber/thread 的 spawn/create），列出每个创建点的入口函数和传入参数。描述入口函数内部的主循环或核心逻辑，标注行号。不推断执行单元的"角色"，只陈述入口函数做了什么
4. 内存管理：从阶段 1 文件清单的 #include 中识别内存管理方案——先搜索自定义 allocator/内存池/pmem 等封装的分配器头文件，再针对性搜索其分配/释放 API。若未找到封装，回退到 malloc/free/new/delete/智能指针。对阶段 1 入口函数涉及的关键对象，追踪其分配点、所有权转移点（指针传递、move）、释放点，标注行号
5. 生命周期：以阶段 1 入口点为起点，追踪入口函数创建的关键对象从 init/create → 运行中使用（被哪些函数访问/修改）→ deinit/destroy 的完整阶段，每阶段标注函数和行号

**输出**（写入 `/tmp/arch-extract-topic-concur-<ts>.md`）：
- 同步原语清单（变量名 | 框架/API | 保护对象 | 获取/释放行号 | 是否跨函数持有；无锁模型则描述同步机制 + 行号）
- 执行单元清单（创建点行号 | 框架/API | 入口函数 | 传入参数 | 主逻辑位置）
- 关键对象的内存分配/释放路径（对象 | 分配点行号 | 所有权转移点 | 释放点行号）
- 关键对象的生命周期阶段表（对象 | 创建 | 使用中 | 销毁，每阶段函数+行号）
- <若启用了 ref-doc> 对照声明清单中并发/一致性部分的逐条验证
```

**Agent D — 接口与异常**：

```
**任务**：以阶段 1 入口点为关键接口，提取每个接口的签名、前置条件、错误返回、边界检查，并从测试文件中提取调用模式

**输入**：
- 文件清单与入口点：阶段 1 产出（入口点列表即关键接口列表）
- 主题名：<target>
- <若启用了 ref-doc> 设计声明清单（接口/契约部分）：/tmp/arch-extract-topic-claims-<ts>.md

**分析要求**：
1. 函数签名：对阶段 1 的每个入口函数，从头文件中提取完整的函数签名（参数名+类型+是否 const/指针、返回值类型），标注声明行号
2. 前置条件：从函数体内的 assert、参数校验（NULL 检查、范围检查、状态检查）中提取调用方必须满足的条件，标注每个检查的行号
3. 错误返回：从函数体内的错误码定义（enum/#define）和 return 语句中提取所有可能的错误返回值及其触发条件；沿调用链向下追一层，看被调用函数的错误如何向上传播（直接返回/包装后返回/日志后吞掉），标注行号
4. 边界检查：从函数体内的边界判断（buffer 满/空、计数器溢出、超时、资源耗尽）中提取边界条件及其处理方式（返回错误/阻塞等待/abort），标注行号
5. 测试中的调用模式：从相关测试文件中提取每个入口函数的典型调用序列——测试先构造什么参数、调用后检查什么返回值、如何清理，用伪代码描述

**输出**（写入 `/tmp/arch-extract-topic-iface-<ts>.md`）：
- 每个入口函数的接口表（签名行号 | 前置条件 | 成功返回值 | 错误返回值及触发条件）
- 错误码表（错误码 | 含义 | 触发条件 | 传播方式，每项附行号）
- 边界条件表（边界 | 检测位置行号 | 处理方式）
- 每个入口函数的测试调用序列（伪代码，标注来源测试文件和行号）
- <若启用了 ref-doc> 对照声明清单中接口/契约部分的逐条验证
```

**质量门禁**：4 个 agent 全部返回，每个产出包含各自维度的分析内容；若启用了 `--ref-doc`，每个产出须包含对照声明清单的验证结果。

### 第 5 阶段：文档-代码核对

**前置条件**：阶段 2 产出了设计声明清单。未启用 `--ref-doc` 则跳过。

主会话先读取阶段 2 设计声明清单，按维度检查是否有条目，有内容的维度才派遣对应核对 agent：

- "接口声明"维度有内容 → 派遣 Agent A
- "架构声明""数据模型声明""并发/一致性声明""关键机制/流程声明"任一有内容 → 派遣 Agent B
- 某维度无内容 → 主会话直接记录"文档无 <维度名> 声明"，不派遣

**Agent A — 接口契约核对**（有派遣时）：

```
**任务**：将阶段 2 "接口声明"维度的每条声明，与阶段 4-D 的入口函数接口表逐条对比

**输入**：
- 设计声明清单（接口声明部分）：阶段 2 产出
- 接口分析：阶段 4-D 产出（含入口函数接口表、错误码表、边界条件表）

**对比方式**：
- 对每条声明，在阶段 4-D 中查找对应的入口函数，对比：函数名是否存在 → 参数类型是否匹配 → 返回值语义是否一致 → 错误码是否一致
- 若声明中的函数在阶段 4-D 中找不到 → "文档有代码无"
- 若阶段 4-D 中有入口函数但文档未提及 → 在输出末尾追加"代码有文档无"汇总行

**输出**（写入 `/tmp/arch-extract-topic-check-iface-<ts>.md`）：

| 声明（出处） | 对比结果 | 代码实际情况 |
|-------------|---------|-------------|
| <文档中声明的函数/接口>（<出处>） | 一致 / 文档有代码无 / 矛盾 | <一致时写"符合"；不一致时写代码实际签名/返回值/错误码> |

末尾追加"代码有文档无"汇总：列出阶段 4-D 中有但文档未提及的入口函数。
```

**Agent B — 设计约束核对**（有派遣时）：

```
**任务**：将阶段 2 架构/数据模型/并发/关键机制四个维度的每条声明，与阶段 4-A/B/C 的分析逐条对比

**输入**：
- 设计声明清单（架构/数据模型/并发/关键机制部分）：阶段 2 产出
- 阶段 4-A：代码组织与控制流（文件依赖、调用链、struct 传递路径）
- 阶段 4-B：数据结构与算法（字段表、内存/磁盘布局、算法伪代码）
- 阶段 4-C：并发与资源（同步原语清单、执行单元清单、分配路径、生命周期）

**对比方式**：
- 架构声明 → 对照 4-A 的文件依赖关系和调用链
- 数据模型声明 → 对照 4-B 的字段表和存储布局
- 并发声明 → 对照 4-C 的同步原语和执行单元清单
- 关键机制声明 → 对照 4-A 的调用链和 4-B 的算法伪代码
- 每条声明标注对比结果和代码实际行为

**输出**（写入 `/tmp/arch-extract-topic-check-design-<ts>.md`）：

| 声明（出处） | 维度 | 对比结果 | 代码实际情况 |
|-------------|------|---------|-------------|
| <文档中的声明>（<出处>） | 架构/数据模型/并发/关键机制 | 一致 / 文档有代码无 / 代码有文档无 / 矛盾 | <具体描述代码实际行为，附阶段 4 来源文件路径> |

末尾追加"代码有文档无"汇总：列出四个维度中阶段 4 有但文档未提及的重要发现。
```

**文档-代码核对规则**：
- **一致**：产出文档可引用社区文档的描述，标注"与上游文档一致"
- **文档有，代码无**：以代码为准；在"已知问题"中记录为可能的版本差
- **代码有，文档无**：正常提取到产出文档，不特别标注
- **矛盾**：以代码为准，记录"文档说 X，代码实际为 Y"

### 第 6 阶段：架构推断

**职责**：综合阶段 4 四个维度的代码事实和阶段 5 核对结果，归纳架构设计思路

派遣 1 个 architect agent：

```
**任务**：读取阶段 4 的代码分析产出和阶段 5 的核对结果，归纳主题的架构设计思路

**输入**：
- 阶段 4-A：代码组织与控制流（文件依赖关系图、调用链、struct 传递路径）
- 阶段 4-B：数据结构与算法（字段表、内存/磁盘布局、算法伪代码）
- 阶段 4-C：并发与资源（同步原语清单、执行单元清单、分配路径、生命周期）
- 阶段 4-D：接口与异常（入口函数接口表、错误码表、边界条件表）
- 阶段 5 核对结果（若有）：文档-代码差异表，辅助判断哪些代码行为是刻意设计 vs 版本差异

**推断要求**：
1. 组件拓扑：从 4-A 的文件依赖关系图和调用链中，归纳出逻辑组件边界（文件 → 组件的聚类），画出 ASCII Art 组件拓扑图
2. 核心设计思路：从调用链和 struct 传递路径中，归纳出 1 条核心设计模式（如 pipeline、event-driven、layered 等），用 3-5 句话描述
3. 核心技术矛盾：从数据结构选择（4-B）、并发策略（4-C）、接口设计（4-D）中，归纳出 1 个核心技术矛盾——面向上游的约束是什么，为此做了什么关键取舍
4. 设计动机推断：若 4-B 的字段注释和 4-C 的生命周期中有设计意图的线索，归纳出"为什么这样设计"的推断；无法从代码中推断的部分标注"推断依据不足"

**输出**（写入 `/tmp/arch-extract-topic-synth-<ts>.md`）：
- ASCII Art 组件拓扑图（从文件依赖聚类为逻辑组件）
- 核心设计思路（3-5 句）
- 1 个核心技术矛盾
- 设计动机推断（附推断依据和置信度）
```

**质量门禁**：组件拓扑图和核心设计思路已产出，核心技术矛盾已明确。

### 第 7 阶段：分析确认

**职责**：向用户展示架构推断 + 核对结果，确认设计意图

主会话汇总阶段 6（架构推断摘要）和阶段 5（核对结果摘要，若有），展示给用户：

1. **架构推断**：阶段 6 产出的组件拓扑、核心设计思路、核心技术矛盾
2. **文档-代码差异**（若有）：列出一致/矛盾/未验证条目
3. **待确认的问题**：需要用户判断的设计意图（如有）

使用 AskUserQuestion 确认关键判断。矛盾处以用户判断为准。

**质量门禁**：
- 用户明确确认了主题边界和输出路径 → 推进到第 8 阶段
- 用户要求调整范围 → 若缩小（剔除文件），直接重跑阶段 4-6；若扩大或重新定位，回退到阶段 1 重跑定位，再重跑阶段 4-6。阶段 2（文档发现）不重跑
- 用户对分析结果有疑问但能给出明确指示 → 记录指示，推进到第 8 阶段（writer 以用户指示为准覆盖分析结论）
- 用户无法判断 → 标记为"待确认"，推进到第 8 阶段（writer 在已知问题中记录差异，标注"需人工核实"）
- 若无文档-代码差异且无待确认问题 → 跳过 AskUserQuestion，自动推进到第 8 阶段（避免无意义的用户打断）

### 第 8 阶段：文档生成

**职责**：按 topic 模板生成完整文档

派遣 3 个 architect agent 并行（按章切分），然后 1 个 architect agent 合并。

**共享写作约束**（三个 agent 均须遵守）：
- 写作前 MUST 先读取模板：`skills/devforge-arch-extract-topic/templates/arch-reverse-topic.md`
- 合并后文档 ≤500 行。核心设计和关键机制是重点章节，篇幅自然倾斜；已知问题等非重点章节从简
- 安全/性能/错误语义融入关键机制描述，不独立成章
- 禁止"因为模板要求所以要有"的占位填充
- 从输入材料中提取事实写入，不凭空添加未经验证的技术陈述

**Agent A — 设计背景与问题 + 核心设计**：

```
**任务**：撰写"设计背景与问题"和"核心设计"两个章节

**输入**：
- 阶段 6 架构推断（组件拓扑、核心设计思路、核心技术矛盾、设计动机推断）
- 阶段 4 全部分析（A/B/C/D）——细节支撑
- 阶段 5 核对结果（若有）——社区文档中可能有设计背景的引用线索
- 阶段 7 用户确认的设计意图

**写作指引**：
- 核心技术矛盾直接采用阶段 6 的推断，若用户有补充说明则整合
- 组件拓扑图直接采用阶段 6 的 ASCII Art 图，不重画
- 核心流程端到端描述：综合阶段 4-A 的调用链和阶段 4-D 的测试调用序列，选取 1 条覆盖主要组件的完整路径

**输出**（写入 `/tmp/arch-extract-topic-sec1-<ts>.md`）：
- 设计背景与问题：技术背景 + 1 个核心技术矛盾 + 子系统定位
- 核心设计：整体设计思路概述 + ASCII Art 组件拓扑图 + 1 条核心流程端到端描述
```

**Agent B — 关键机制 + 数据结构**：

```
**任务**：撰写"关键机制"和"数据结构"两个章节

**输入**：
- 阶段 6 架构推断（核心设计思路）——辅助判断哪些机制是核心
- 阶段 4 全部分析（A/B/C/D）
- 阶段 5 核对结果（若有）

**写作指引**：
- 关键机制选择：优先选择核心流程（阶段 6）中涉及的关键步骤，和阶段 4-C 中涉及跨函数持有的锁所保护的逻辑
- 每个机制含：触发条件 + 执行步骤 + 边界条件（并发/容错/性能语义融入其中）
- 数据结构从阶段 4-B 的字段表和存储布局中提取，附存储布局（内存/磁盘），选择理由从阶段 6 的设计动机推断中提取

**输出**（写入 `/tmp/arch-extract-topic-sec2-<ts>.md`）：
- 关键机制：2-4 个，每个含触发条件 + 执行步骤 + 边界条件
- 数据结构：≥2 个核心数据结构，每个附字段概要 + 存储布局 + 选择理由
```

**Agent C — 接口 + 依赖 + 已知问题**：

```
**任务**：撰写"接口"、"依赖与交互"和"已知问题"三个章节

**输入**：
- 阶段 6 架构推断（组件拓扑图）——辅助判断上下游依赖
- 阶段 4-D 接口分析（入口函数接口表、错误码表、边界条件表）
- 阶段 4-A 调用链 ——辅助判断同步/异步交互方式
- 阶段 5 核对结果（若有）——矛盾条目写入已知问题；若阶段 7 用户确认了差异性质则一并记录，否则标注"需人工核实"

**写作指引**：
- 接口从阶段 4-D 的接口表中选取入口函数，含签名 + 前置条件 + 错误返回
- 依赖与交互：从阶段 6 组件拓扑图和阶段 4-A 调用链中提取上下游关系，标注同步/异步和耦合强度
- 已知问题：核对发现的矛盾记录"文档说 X，代码实际为 Y"；阶段 4 分析中标注"推断依据不足"的部分也纳入已知问题；无则写"未发现"

**输出**（写入 `/tmp/arch-extract-topic-sec3-<ts>.md`）：
- 接口：≥2 个入口函数，含签名 + 前置条件 + 错误返回
- 依赖与交互：上下游 + 同步/异步 + 耦合强度
- 已知问题：文档-代码矛盾 + 推断依据不足项；无则写"未发现"
```

**Agent D — 合并**（architect agent）：

```
**任务**：将三个章节文件合并为完整文档，做去重和术语统一。不做内容创作，不添加新的技术陈述。

**输入**：
- `/tmp/arch-extract-topic-sec1-<ts>.md`
- `/tmp/arch-extract-topic-sec2-<ts>.md`
- `/tmp/arch-extract-topic-sec3-<ts>.md`
- 模板：skills/devforge-arch-extract-topic/templates/arch-reverse-topic.md（参照自检清单）

**输出**（写入 `/tmp/arch-extract-topic-draft-<ts>.md`）：
- 完整的 topic 文档（≤500 行）
- 去掉章节间的重复内容（同一概念在多个章节出现时，保留首次出现的完整描述，后续章节仅引用）
- 术语统一（全文同一概念使用同一术语）
- 若合并后超过 500 行，优先精简非核心细节（示例、次要字段、冗余描述），保留核心技术内容
```

**质量门禁**：所有必填章节均已覆盖，文档 ≤500 行。

### 第 9 阶段：评审

**职责**：独立评审 + 修正循环（最多 3 轮）

派遣 3 个 architect-reviewer agent 并行（按视角切分）。所有 reviewer 必须能读取阶段 4 代码分析产出和阶段 6 架构推断，用于交叉验证。

**Agent A — 技术准确性**：

```
**评审视角**：
1. 技术陈述是否准确反映代码实现（对照阶段 4 分析产出交叉验证）
2. 组件拓扑和数据流是否正确（对照阶段 4-A 代码组织与控制流）
3. 数据结构描述是否与代码一致（对照阶段 4-B 数据结构与算法）
4. 并发/资源分析是否有遗漏或错误（对照阶段 4-C 并发与资源）
5. 接口描述是否与代码一致（对照阶段 4-D 接口与异常）
6. 核心技术矛盾和设计思路是否与阶段 6 架构推断一致

**被评审对象**：`/tmp/arch-extract-topic-draft-<ts>.md`
**交叉验证参考**（必须读取）：
  - `/tmp/arch-extract-topic-arch-<ts>.md`（阶段 4-A）
  - `/tmp/arch-extract-topic-data-<ts>.md`（阶段 4-B）
  - `/tmp/arch-extract-topic-concur-<ts>.md`（阶段 4-C）
  - `/tmp/arch-extract-topic-iface-<ts>.md`（阶段 4-D）
  - `/tmp/arch-extract-topic-synth-<ts>.md`（阶段 6）
**review_output_path**：`/tmp/arch-extract-topic-review-tech-<ts>.md`
```

**Agent B — 内容完整性**：

```
**评审视角**：
1. 是否覆盖了模板 `skills/devforge-arch-extract-topic/templates/arch-reverse-topic.md` 的所有必填章节
2. 各章节是否达到最低深度要求（对照模板各章节的"最低深度"指示）
3. 关键机制是否遗漏重要子机制（对照阶段 4 分析产出和阶段 6 核心设计思路检查）
4. 已知问题是否遗漏了阶段 5 核对发现的文档-代码差异

**被评审对象**：`/tmp/arch-extract-topic-draft-<ts>.md`
**交叉验证参考**：
  - 模板：`skills/devforge-arch-extract-topic/templates/arch-reverse-topic.md`
  - 阶段 5 核对结果（若有）
  - 阶段 4 全部分析产出（A/B/C/D）
  - 阶段 6 架构推断
**review_output_path**：`/tmp/arch-extract-topic-review-comp-<ts>.md`
```

**Agent C — 表达质量**：

```
**评审视角**：
1. 文档篇幅是否 ≤500 行
2. 图表（ASCII Art/Mermaid）是否清晰、正确
3. 术语是否统一
4. 是否出现"因为模板要求所以要有"的占位填充
5. 安全/性能/错误语义是否融入关键机制描述而非独立成章
6. 叙事性：章节间是否有逻辑递进（背景→设计→机制→数据→接口→依赖→问题），核心设计章节是否为关键机制章节做了铺垫，而非孤立罗列
7. 可读性：是否过度依赖条目罗列或表格对比——设计方案和思路应以文字阐述为主，表格仅用于结构化信息（字段表、接口表、锁清单等）；避免连续三个以上纯列表段落
8. 句式：避免单句超过 3 行的长句，复杂逻辑拆为短句

**被评审对象**：`/tmp/arch-extract-topic-draft-<ts>.md`
**review_output_path**：`/tmp/arch-extract-topic-review-expr-<ts>.md`
```

**评审修正循环**：
1. 汇总 3 个 agent 的问题清单
2. 存在任何 CRITICAL 问题 → **直接判定不通过**（CRITICAL 一票否决，不受缺陷密度阈值豁免）
3. 无 CRITICAL 且缺陷密度 ≤ 2.0 → 通过，进入第 10 阶段
4. 否则 → 主会话汇总三份评审报告，合并为修正清单（每条问题附所属评审报告路径），注入 1 个 architect agent 的派遣 prompt。agent 按清单逐项修正后，回到本阶段重新评审
5. 最多 3 轮。3 轮后仍未通过 → 标注残留问题，进入第 10 阶段

**问题分值**：CRITICAL=10, HIGH=3, MEDIUM=1, LOW=0.1
**缺陷密度** = 问题分数之和 / 章节数（7）

**注**：CRITICAL 问题在密度公式中占 10 分已远超 2.0 阈值，一票否决规则确保了 CRITICAL 不会因章节数多而侥幸通过。

### 第 10 阶段：写入

主会话将最终文档写入 `<output-docs>/architecture/<subsystem>/<topic>.md`（路径由阶段 3 确认）。

**终端汇报**：
- 文档路径
- 章节数、图示数
- 文档-代码核对结果（若有社区文档输入）
- 置信度（评审通过 / 带债通过）

---

## 补全模式

用模板自检清单扫描文档，识别缺失项，跑阶段 4-10 补全。

---

## 禁忌项

- 禁止跳过主题定位直接进入代码分析
- 禁止跳过范围确认（阶段 3 必须与用户交互）
- 禁止跳过评审循环直接输出
- 禁止安全/性能/错误语义独立成章（必须融入关键机制描述）
- 禁止将社区文档描述当作本产品的设计声明（必须标注"来自上游社区文档"）
- 禁止写"因为模板要求所以要有"的占位章节
- 禁止为凑深度而堆砌内容（宁缺毋滥）
- 最终文档控制在 500 行以内
- 禁止不读取模板就直接写作
