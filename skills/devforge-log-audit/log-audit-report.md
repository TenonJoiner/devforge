---
template-for: 日志审计报告
mandatory-sections:
  - 审计元数据
  - 级别使用分析
  - 频率分析
  - 问题清单
  - 审计结论
optional-sections:
  - 运行时日志统计
checklist-at-end: true
---

# 日志审计报告：<审计范围>

> 本模板定义日志审计报告的格式骨架。两个审计维度是固定契约——**级别合理性** 与 **打印频率**；具体检查项与判定基线内建于 `log-auditor` agent，项目特定级别定义由 skill 派遣 prompt 注入。

---

## 审计元数据

| 字段 | 值 |
|------|-----|
| 审计日期 | YYYY-MM-DD HH:MM |
| 审计范围 | <全仓批量整改 / MR 门禁 diff 范围> |
| 语言 | <skill 探测注入，多语言时列出全部> |
| 日志框架 | <探测到的框架，如 zap / slf4j / spdlog / 自定义宏> |
| 级别定义来源 | <配置文件路径 / 文档 / 源码反推>（探测结果） |
| 生产默认级别 | <探测到的生产默认级别，未知则标"未探测到"> |
| 运行时日志目录 | <--log-dir 路径，未提供则标"未提供，频率维度未审"> |
| 数字摘要 | CRITICAL N / HIGH N / MEDIUM N / LOW N |

---

## 级别使用分析

> 始终填写。无 `--log-dir` 时纯静态逐条核对级别合理性（禁止使用"频繁""洪泛""大量""高频"等频率措辞，对"可能高频也可能低频"的路径标注 ⚠️ 需运行时数据验证）；有 `--log-dir` 时结合运行时数据（级别分布 / 高频模板）作为实证线索，增强级别滥用判定。

- **整体印象**：<级别使用是否克制，有无系统性滥用倾向>
- **发现要点**：<高级别滥用、log-and-throw、printStackTrace 等，逐条进入下方问题清单>

---

## 频率分析

> 有 `--log-dir` 时填写——由 `analyze_logs.py` 量化，含影响推演（日均量、磁盘消耗）。无 `--log-dir` 则本节标注"频率维度未审：未提供 --log-dir"，不做静态臆测。

- **执行状态**：<已执行（提供了 --log-dir）/ 未审（未提供 --log-dir）>
- **量化结论**（仅已执行时）：<洪泛源、打印速率、Top-N 高频消息，逐条进入下方问题清单；详细统计见「运行时日志统计」节>

---

## 运行时日志统计（可选，仅 `--log-dir` 提供时）

> 由 `analyze_logs.py` 产出的量化结果。

| 项 | 值 |
|-----|-----|
| 分析文件数 / 总行数 | N 文件 / M 行 |
| 各级别计数 | <探测到的各级别，严重度低→高排列> = ... |
| 打印速率 | 稳态 X 行/秒，峰值 Y 行/秒（时间戳可解析时） |

**高频重复消息 Top-N：**

| 排名 | 消息模板 | 出现次数 | 占比 | 疑似源码打印点 |
|------|---------|---------|------|--------------|
| 1 | <归一化后的消息模板> | N | X% | <path:line，best-effort> |

---

## 问题清单

> **格式铁律**：四个级别统一使用下方单条格式，每条独立成条、带编号前缀（C/H/M/L+序号）。**禁止**将同级别问题合并为一段归类描述，**禁止**某些级别用表格而另一些用段落——所有级别格式一致。

> "所属维度"取值：`级别合理性` 或 `打印频率`。

> "说明"须覆盖三要素（缺一不可）：**是什么**（这条日志的事实）、**后果**（会导致什么影响）、**证据**（判断依据）。不少于 2 句话。

> "修复建议"须可操作（降级为 X / 移出循环 / 加 level guard / 加采样），不可只写"需要修复"。

### CRITICAL

**C1. <简短标题> — `<文件:行号 或 日志文件名>`**

- **所属维度**：级别合理性 | 打印频率
- **说明**：<这条日志是什么、会导致什么后果、证据是什么——至少 2 句>
- **修复建议**：<具体可操作方向，如降级为探测到的 X 级别 / 移出循环 / 加采样 / 加 level guard>

### HIGH

**H1. <简短标题> — `<文件:行号 或 日志文件名>`**

- **所属维度**：级别合理性 | 打印频率
- **说明**：<这条日志是什么、会导致什么后果、证据是什么——至少 2 句>
- **修复建议**：<具体可操作方向>

### MEDIUM

**M1. <简短标题> — `<文件:行号 或 日志文件名>`**

- **所属维度**：级别合理性 | 打印频率
- **说明**：<这条日志是什么、会导致什么后果、证据是什么——至少 2 句>
- **修复建议**：<具体可操作方向>

### LOW

**L1. <简短标题> — `<文件:行号 或 日志文件名>`**

- **所属维度**：级别合理性 | 打印频率
- **说明**：<这条日志是什么、会导致什么后果、证据是什么——至少 2 句>
- **修复建议**：<具体可操作方向>
- 范围外发现标注：`⚠️ 范围外发现，不阻塞本次合并`

---

## 审计结论

- [ ] 审计结论：**PASS** / **NEEDS-FIX**
- [ ] 无 CRITICAL（或 CRITICAL 已全部记录修复建议）
- [ ] HIGH 已逐条评估：接受修正 / 接受延期 / 拒绝
- [ ] 频率维度：提供 --log-dir 时量化结论已填；未提供时已标注"频率维度未审"

---

## 自检清单

> 只检查审计报告的格式完整性，不检查具体审计判断。

- [ ] 审计元数据字段全部填写（未探测到的项已显式标注"未探测到/未提供"）
- [ ] 级别使用分析始终已填写；频率分析有 `--log-dir` 时已填写，无则已标注未审
- [ ] 四级问题格式统一：全部为编号前缀+独立条目，无表格/段落混用，无同级别合并描述
- [ ] 每条问题包含：编号前缀（C/H/M/L+序号）/ 标题 / 所属维度 / 说明（≥2句，含三要素）/ 修复建议（具体可操作，非空泛）（缺一不可）
- [ ] 每条问题的"所属维度"为 `级别合理性` 或 `打印频率`
- [ ] 提供 --log-dir 时，频率分析与运行时日志统计节已填；未提供时频率分析标"未审"
- [ ] 审计结论已明确标注 PASS / NEEDS-FIX，数字摘要与问题清单一致
