## Context

<!-- 背景、现状、约束、利益相关方 -->

## Architecture Traceability

- 关联架构文档：<!-- docs/architecture/ 下的具体文件，或"不适用：<原因>" -->

## Goals / Non-Goals

**Goals:**
<!-- 本设计要达成什么 -->

**Non-Goals:**
<!-- 明确排除什么 -->

## Decisions

<!-- 关键技术选择及理由 -->
<!-- 必备结构性元素清单（每个 Decision 都按此填充）：
□ 选择了什么（一句话定调）
□ 候选方案 ≥ 2 个，每个标注标杆来源（research.md 中已研究的方案）
□ 候选对比维度（一致性 / 复杂度 / 性能 / 与本项目约束的匹配度，至少 4 维）
□ 不选其他候选的具体理由（每个候选都要回应，不能只列方案不给理由）
□ 演进性表态（未来扩展方向，或显式���注「本决策无演进性需求」）
□ 取舍代价 + 缓解措施
□ 未决问题拨入 Open Questions
合法回退：每项允许「显式标注本项不适用」作为合法填法 -->
<!-- 已被产品级架构约束确定的决策：直接引用架构文档，无需列备选 -->
<!-- 涉及并发或多节点交互时：在对应决策下声明并发模型（锁类型、粒度、顺序、happens-before） -->
<!-- 涉及多状态组件时：在对应决策下提供状态转换表（| 当前状态 | 事件 | 下一状态 | 动作 |） -->
<!-- 可读性约束：以叙述段为主，对比表辅助，避免「只有结论没有思路」的写法 -->
<!-- 强制出图条件：
- 模���结构、组件关系（≥3 个组件）→ 结构图（Mermaid graph 或 ASCII）
- 跨进程/跨节点交互 → 时序图（Mermaid sequenceDiagram）
- 有生命周期的对象（租约、连接、会话）→ 状态机图（Mermaid stateDiagram）
- 数据缓存、读写路径分离 → 数据流图（Mermaid flowchart）
- 类/数据结构关系 → 类图（Mermaid classDiagram） -->
<!-- 格式：
### Decision N: <决策主题>

**候选方案**（每个标注标杆来源）：
- 方案 A（来自 research.md 维度 X）：...
- 方案 B（来自 research.md 维度 X）：...

**对比**：
| 维度 | 方案 A | 方案 B |
|------|--------|--------|
| 复杂度 | ... | ... |
| 性能影响 | ... | ... |
| 可维护性 | ... | ... |
| 与本项目约束的匹配度 | ... | ... |

**结论**：选择方案 X 因为 Y。不选 A 因为 ...，不选 B 因为 ...

**取舍代价**：...
**缓解措施**：...
**演进性**：... 或「本决策无演进性需求」 -->

## Interface Changes

<!-- 如涉及跨子系统接口，列出变更的接口定义及影响 -->
<!-- 格式：
### 接口: <接口名称>
- 变更类型：新增 / 修改 / 废弃
- 函数签名 / 数据结构 / 协议格式：...
- 兼容性：向前兼容 / 向后兼容 / 破坏性变更 -->

## Risks / Trade-offs

<!-- 格式：[风险] → [缓解措施] -->
<!-- 每个 Risk 必须有对应的 Mitigation，不能只列风险不给方案 -->

## Migration Plan

<!-- 部署步骤、回滚策略（如适用） -->

## Open Questions

<!-- 待解决的决策或未知项 -->
