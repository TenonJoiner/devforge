<!-- proposal.md 篇幅引导：50~100 行；复杂变更可适当扩展，聚焦 WHY 与 WHAT，不写实现细节。 -->

## Why

<!-- 1-2 段叙事：让读者快速理解"为什么要做这件事"，建立共情 -->
<!-- 不要只是罗列现象，要讲清楚：问题的本质是什么、谁受到了影响、不解决会怎样 -->

<!-- bugfix / 性能优化 / 技术债务清理 时： -->
<!-- 分析根因，而非描述表面现象 -->
<!-- 量化影响：用数据说明严重程度（如性能下降百分比、故障频率） -->
<!-- 如果无法量化，说明定性影响和不解决的后果 -->

<!-- 新特性 / 能力扩展 时： -->
<!-- 业务价值：解决什么用户痛点，创造什么价值 -->
<!-- 用户场景：谁在什么场景下需要这个功能 -->
<!-- 不做的后果：如果本次不做，会有什么影响 -->

## What Changes

<!-- 变更清单，说明新增、修改或移除的"能力"或"行为"，不写具体源码实现细节。 -->
<!-- 内部重构类变更描述对外可见的质量属性变化（如延迟、吞吐量、兼容性），不描述具体实现动作。 -->
<!-- 不要在这里列出：修改哪个源文件、替换哪个函数、新增哪个参数。这些属于 design / tasks 阶段。 -->
<!-- 破坏性变更标记 **BREAKING** -->

[1-3 句话 overview：说明本次变更的整体方向，不重复下方清单条目。]

<!-- 以下为占位符示例，实际生成时替换为具体内容 -->

- [新增/修改/移除] [能力或行为]：[一句话描述其对外的可见影响]
- [新增/修改/移除] [能力或行为]：[一句话描述其对外的可见影响] **BREAKING**

## Capabilities

### New Capabilities

<!--
  新增能力列表。每个 capability 都是对外部调用方/用户可见的"行为能力"或"质量属性承诺"，不是内部实现动作。
  - 行为能力：如 "支持快照回滚"
  - 质量属性承诺：如 "P99 写入延迟 < 1ms"、"与 v1.x 客户端兼容"
  禁止将实现动作（升级依赖、重构模块、替换数据结构）作为 capability 名。
  质量属性型 capability 必须：绑定功能领域、可量化、对外部可见。
  命名规范：前缀聚集，段数灵活。
  - 从功能特性维度命名，不从模块/组件维度命名
  - 使用共享前缀让相关能力聚集（如 replication-*、storage-*）
  - 两段：{feature}-{capability}（如 replication-log-shipping）
  - 三段：{feature}-{area}-{capability}（如 replication-consensus-leader-election）
  - 技术名不推荐（保证稳定性），但不禁止
  - 禁止斜杠、and、or 连接多个概念；禁止组件类名作为 capability 名
  - 粒度判断：功能完整性、开发独立性、依赖耦合、演进稳定性
  每个 capability 生成 specs/<capability-name>/spec.md

  bugfix / 内部优化类 capability 禁止以动作命名（如 `fix-*`、`refactor-*`、`upgrade-*`），
  应转换为修复后对外承诺的行为或质量属性：
  - 错误：`fix-compactor-memory-leak` → 正确：`storage-compaction-memory-bounded`
  - 错误：`refactor-leader-election` → 正确：`replication-consensus-failover-latency`
  若重构仅为消除 bug 根因而无法独立验收，应与 bugfix 合并为同一个质量属性 capability。
-->
- `<capability-name>`: <brief description>

### Modified Capabilities

<!-- 已有能力的行为变更或质量属性提升（非纯实现动作）。 -->
<!-- 使用 openspec/specs/ 中的已有 spec 名称。无需求变更则留空 -->

<!-- 以下为占位符示例，实际生成时替换为具体内容 -->

- `<capability-name>`: [行为变更描述]（关联已有 spec `openspec/specs/<capability-name>/spec.md`）

## Acceptance Criteria

<!-- 从外部视角（用户/客户/测试者）定义「这个变更做成什么样算成功」。 -->
<!-- 只写高层次的、可验证的行为结果和质量门槛，不展开具体测试步骤。 -->
<!-- 内部重构的验收标准应为可量化的质量属性（性能基线、兼容性回归、可观测性信号）。 -->
<!-- 可写"通过某测试集"作为验收项，但只写目标和阈值；具体新增/回归哪些测试类别，写在 Impact 的测试影响中。 -->
<!-- 详细的验收条件、测试用例在对应 capability 的 spec.md 和 tasks.md 中细化。 -->

<!-- 以下为占位符示例，实际生成时替换为具体内容 -->

- [外部可见行为]：[可验证的结果]
- [外部可见行为]：[可验证的结果]
- [质量属性]：[量化或明确的判定标准]

## Impact

<!-- 列出本 proposal 影响的范围。不需要展开详细设计，只需识别受影响的对象和变化方向。 -->
<!-- 常见维度：API/协议/数据格式、依赖、部署/配置/监控、兼容性/升级路径。 -->
<!-- 详细的兼容性、可靠性、可观测性要求，后续在对应 capability 的 spec.md NFR 章节中细化。 -->

<!-- 以下为占位符示例，实际生成时替换为具体内容 -->

- **API/协议/数据格式影响**：[外部可见的接口/协议/格式] 将 [变更内容]，兼容性为 [向前兼容/向后兼容/破坏性变更]
- **依赖影响**：[新增/升级/移除 依赖包]
- **部署/配置/监控/升级影响**：[部署方式、配置项、监控指标、升级/回滚路径的变化]
- **测试影响**：[新增 / 回归 / 修改的测试类别]，对应 Acceptance Criteria 中的 [具体标准]
