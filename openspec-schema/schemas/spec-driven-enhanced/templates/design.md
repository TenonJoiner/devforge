<!-- Complexity: light | standard -->
<!--
  复杂度档位判定（满足任一"复杂"条件即为 standard，全部不满足为 light）：
  - 变更范围：跨模块 / 跨子系统
  - 接口影响：有对外接口变更
  - 决策密度：≥3 个关键决策
  - 并发状态：涉及生命周期对象或并发模型
  - 升级影响：涉及兼容性 / 迁移

  声明档位决定下文各章节的"必填/可选"约束；spec-review 会核对声明档位与实际复杂度是否匹配。
  设计文档的粗细原则：叙述部分（## Design 方案描述）保持粗，避免陷入实现细节；契约部分（## Decisions / ## Contracts / ## Implementation Constraints）必须细、必须可锁定。
-->

<!-- 篇幅引导：Light 档 ≤6000 字符（约 4~5 页）；Standard 档 ≤20000 字符（约 14 页），复杂设计可适当扩展，将篇幅留给方案与决策本身。 -->

## Context

<!-- 适用档位：Light=必填 / Standard=必填 -->
<!-- 背景、现状、约束。约束中引用 spec.md 的非功能需求，并说明本设计如何满足或为何豁免。 -->

## Goals / Non-Goals

<!-- 适用档位：Light=必填 / Standard=必填 -->

**Goals:**
<!-- 本设计要达成的 2-5 个具体目标 -->

**Non-Goals:**
<!-- 明确排除的范围（特性范围层，即"本特性不做什么功能"；实现方式层的排除见 ## Implementation Constraints） -->

## Design

<!-- 适用档位：Light=必填 / Standard=必填 -->

<!--
  【粗细原则】本章节为方案叙述（给人看思路），聚焦设计意图与整体方案，不要陷入代码实现细节（不写源码、函数签名、代码样例）。
  该限制仅适用于方案叙述（组件协作、数据流、设计思路）；关键决策点不属于"实现细节"，必须在 ## Decisions 与 ## Contracts 章节显式化、可锁定。
-->

<!-- 1-2 段总体性方案概要：整体方向、核心思路、关键组件、最终交付的系统行为变化。 -->

<!-- 随后从整体到局部逐层阐述本特性的设计方案和思路，按需覆盖：需求实现方式、关键组件协作、数据/控制流、关键决策点/边界处理、外部接口边界等。在需要直观展示的地方可插入图（架构图/数据流图/状态机图/时序图等）辅助理解。文字本身应完整表达设计，不依赖图也能读懂。简单设计可只用文字。不要陷入代码实现细节。 -->

<!--
  多仓场景（可选）：若本次变更涉及多个并列仓库，按子系统分二级章节，例如：

  ### 子系统: replication
  - 涉及仓库: storage, meta
  - storage 仓库职责: ...
  - meta 仓库职责: ...
  - 跨仓库数据流 / 接口边界: ...

  子系统是逻辑边界，仓库是物理部署；二者不要混为一谈。
-->

```
<!-- 可选：方案图 -->
```

<!-- 围绕上图的设计方案文字说明 -->

## Decisions

<!-- 适用档位：Light=可选（0-2 个决策） / Standard=必填 -->

<!-- 记录本特性设计中的关键决策，每个决策列出 2-3 个候选方案，每个方案用 2-3 句话言简意赅地概括核心优缺点，不做大段展开。已被架构约束确定的直接引用。design 章节已有的内容避免冗余阐述，直接引用。 -->

<!--
  【Standard 档强制】结论部分必须包含三要素：
  1. **锁定声明**：明确"实现必须按 X 方案"，不是"推荐 X"
  2. **显式否决**：被否决方案的具体理由（不选 A 因为 ...，不选 B 因为 ...）
  3. **量化约束**（如适用）：性能数字、空间公式、QPS 上限等
-->

<!--
  【决策类别 checklist】（Standard 档逐项自检：判断本设计是否涉及该类别；涉及 → 写 Decision 锁定；不涉及 → 跳过，无须说明）：
  - 数据结构选型（索引、缓存、队列、堆、树）
  - 算法选型（排序、查找、淘汰、压缩、加密）
  - 并发原语与粒度（锁类型、锁粒度、无锁方案）
  - 状态机设计（有生命周期对象时：租约、连接、会话）
  - 资源管理策略（内存池、连接池、缓冲区）
  - 错误处理与重试策略（含超时、边界条件）
  - 兼容性策略（协议版本、序列化格式、回滚点）
-->

### Decision 1: [决策主题]

**问题**：

**候选方案分析**：

**结论**：（Standard 档必须包含锁定声明 + 显式否决 + 量化约束）

## Contracts

<!-- 适用档位：Light=可选 / Standard=必填 -->

<!--
  本章节定义本特性的关键契约——包括跨子系统接口变更与特性内核心抽象。此处允许且要求细到函数签名级别（属于"允许细"的契约部分，与 ## Design 章节的"叙述粗"形成分层差异化）。

  覆盖范围：
  - 跨子系统接口变更
  - 特性内核心抽象：新增 trait / interface / 抽象类（可到函数签名级别）
  - 核心数据结构的 schema（不是字段列表，是"为什么是这个形状"）
  - 错误码 / 异常类型
  - 对外可见的配置项

  系统级兼容性问题在 ## Upgrade Impact 中统一说明。
-->

整体影响范围：

### 契约: [名称]

- 变更类型：[新增 / 修改 / 废弃]
- 契约类型：[跨子系统接口 / 内部抽象 / 数据结构 / 错误码 / 配置项]
- 函数签名 / 数据结构 / 协议格式
- 选型理由：（为什么是这个形状，不是其他形状）

## Implementation Constraints

<!-- 适用档位：Light=可选 / Standard=必填 -->

<!--
  实现层排除项——压缩 AI 实现自由度的最强手段。明确列出本特性在实现层面"不做 / 不用 / 必须遵守 / 不改变"的约束。
  与 Goals/Non-Goals 的区别：Non-Goals 是特性范围层（不做什么功能），本章节是实现方式层（不用什么手段）。
-->

- **不使用**：<!-- 例如：全局锁 / 新第三方库 / 反射 / ... -->
- **必须遵守**：<!-- 例如：现有磁盘格式 / 错误码规范 / 日志级别约定 / ... -->
- **不改变**：<!-- 例如：对外协议 / 持久化 schema / ... -->

## Upgrade Impact

<!-- 适用档位：Light=可选 / Standard=必填 -->

<!-- 说明本设计对系统升级的具体影响，对应 spec.md 中 Upgrade Compatibility NFR 的量化目标。无影响时可写「不涉及」。 -->

- 升级风险判定：[无风险 / 低风险 / 高风险]
- 对升级流程的要求：是否需要升级模块介入、元数据/协议/配置变更、兼容策略、回滚安全性

## Risks / Trade-offs

<!-- 适用档位：Light=必填 / Standard=必填 -->

<!-- 每个风险必须有缓解措施。格式：[风险] → [缓解措施] -->

- **[风险 1]**：[具体描述] → [缓解措施]

## Open Questions

<!-- 适用档位：Light=必填 / Standard=必填 -->

<!-- 待解决的决策或未知项 -->

- **[问题 1]**：[描述] → [当前状态]
